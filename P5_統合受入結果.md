# P5 統合受入結果

Phase 5「マップと探索基盤」の工程ごとの記録。正本仕様は `Momotaro_P5_Detailed_Spec_v1.1.md`、
作業ルールは `CLAUDE.md`。P4 の記録は `P4_統合受入結果.md` に残し、本書へ統合しない。

---

## 記録 001：P5-00 着手（正本・ブランチ・裁定）

作成日：2026-09-21　担当：Claude（claude-opus-5、スレッド 2 本目）

### 1. 現在地

| 項目 | 値 |
|---|---|
| 作業ブランチ | `phase/5-world-exploration` |
| 親 SHA | `4a7650b`（`phase/4-companions-foundation` の先端、"P4"） |
| 本工程のコミット | `cca1876`（"P5事前"、オーナーが Windows 側で作成） |
| main への統合 | 未。P4 修正済み先端を親とする依存ブランチとして開始（仕様書 §0.2） |
| 実装正本 | `Momotaro_P5_Detailed_Spec_v1.1.md` |
| 未コミット | 仕様書 v1.0 削除 → v1.1 追加、引き継ぎ書追加、本書、`CLAUDE.md` 更新 |

P4 のユーザー試遊は 2026-09-21 にオーナーが実施し、**問題は発見されなかった**。
仕様書 §0.2 の「P5 の着手をもって P4 受入済みとはしない」は、これをもって解消する。

### 2. P4 残件（R3-01〜05）の再照合

各修正が実在することをコードで確認した。同じ修正を重ねない（仕様書 §0.2）。

| ID | 確認した実体 |
|---|---|
| R3-01 | 完了通知後の依頼同一性照合（実体参照＋実行世代）。`InvestigationCoordinator` |
| R3-02 | `Scripts/Gameplay/Combat/IIncomingHitObserver.cs`（`IIncomingHitObserver` / `IIncomingHitSource`）。配信元 `PlayerVitalsHolder`、購読 `InvestigationCoordinator` / `CompanionInvestigationController` |
| R3-03 | `Scripts/Gameplay/Companion/ICompanionInvestigationState.cs`。参照元は `CompanionCombatController` / `CompanionDefenseController` / `CompanionFollowController` |
| R3-04 | `InvestigationRequest.MoveSpeed`（受付時 Snapshot） |
| R3-05 | P06 の受入条件が守護の到達順契約と一致。実 Scene の重なりテスト 1 本 |

必須照合（`verify-required-tests`）による再確認は、Editor ブリッジ復旧後に §6 で実施する。

### 3. オーナー裁定（本スレッド冒頭）

1. **v1.1 残指摘 6 点は Claude の裁定で着手**する。GPT の v1.2 を待たない。裁定内容は §4 に記録し、GPT へは報告として渡す。
2. **PlayMode テストと Scene 置換の常時許可**。`CLAUDE.md`「PlayMode テストと Scene 破壊の常時許可」を本作業のあいだ適用する。
   オーナーはそのあいだ Unity Editor を触らない。実行したこと・結果・所要時間は本書に残す。
3. **コミット署名**を現行モデル名へ更新し、`Claude-Session:` はスレッドごとの可変値とする。`CLAUDE.md` を更新済み。

### 4. v1.1 残指摘 6 点の裁定

いずれも設計の方向を変えない。実装前に決めるのが 1〜3、運用で足すのが 4〜6。

#### 裁定 1（E28）：対象型の発見も反射にする

手書きの「対象型リスト」はテストが嘘をつく。マーカー型 `ITransferableRuntime`（仮称）を Gameplay
アセンブリから反射で走査し、台帳の型集合と**双方向**に照合する。台帳にあって型が無い／型があって台帳に
無い、のどちらも失敗させる。

併せて分類規則へ次を明記する。**`readonly` は不変を意味しない。** `CompanionVitals._flinch` が実例で、
`readonly` フィールドが可変の内部 Runtime を指す。分類は「フィールド修飾子」ではなく
「参照先が可変時間値を持つか」で行い、入れ子の時間所有型まで辿る。

#### 裁定 2（`IPlayerHurtReaction`）：契約を拡張しない

調査結果：

- `IPlayerHurtReaction` のメンバは `IsHurt` / `IsPostHitInvincible` / `BeginHurt` の 3 つだけ。
- 消費者は `PlayerVitalsHolder` と `PlayerStateController` の 2 箇所で、**どちらも真偽値しか見ていない**。
- 残り時間の実体は pure class `HitReactionState`（`_hurtRemaining` / `_invincibleRemaining`）で、
  `HurtRemaining` と `InvincibleRemaining` を**既に public で持っている**。
- `PlayerHitReaction` は `HurtRemaining` を公開済み。無敵は `PostHitInvincibleSeconds`（**設定値**）
  しか公開しておらず、残りが外から読めないだけ。

したがって「既定実装付きメンバ」も「Snapshot 専用の狭い契約を別に立てる」も不要で、**`IPlayerHurtReaction`
には手を触れない**。仕様書 §4.5 の「状態を所有するコンポーネントまたは純粋 Runtime クラス 1 つにつき
Snapshot 1 組」にそのまま従い、

- `HitReactionState` に `ExportTransferSnapshot` / `TryImportTransferSnapshot` を置く（pure class、
  MonoBehaviour 非依存でテストできる既存方針どおり）。
- `PlayerHitReaction` はそれを合成し、持ち越しマーカー契約 `ITransferableRuntime` だけを実装する。
- 検証用に `InvincibleRemaining` の委譲 getter を足す。**読み取り専用メンバの追加なので破壊的変更ではない。**

波及：実装の破壊 0 箇所、`PlayerHitReactionPrefabWiringTests` と `PlayerHurtReceiverTests` は無変更。

#### 裁定 3（V05）：不正 Fixture を全件実行の巻き添えにしない

`ProjectAssetIntegrityTests` が `ProjectDataValidator.RunAll()` でプロジェクト全体の合格を要求するため、
`finally` が回らずに不正 Asset が残ると**以後すべての全件実行が落ちる**。次の 3 点をセットで守る。

1. 不正 Fixture のファイル名に既知の接頭辞（`__P5V05Fixture_`）を付ける。
2. `[OneTimeSetUp]` で**その接頭辞に一致する残骸を先に削除**してから作る。前回の異常終了から自力で回復する。
3. `finally` に加えて `[OneTimeTearDown]` でも同じ掃除を行う。

接頭辞は 1 箇所の定数に置き、掃除側とファイル生成側が同じ定数を参照する。

#### 裁定 4（台帳）：P5-03a で凍結しない

持ち越し台帳 `P5_ActorTransferInventory.md` は P5-03a で作るが**凍結しない**。P5-05（再探索間隔・停滞
時間）や P5-07（Encounter）で可変時間値が増える。E28 が後工程で落ちるのは**正常な動作**であり、
そのとき台帳を更新するのが正しい対応。この旨を台帳の冒頭に明記し、E28 の失敗メッセージにも
「台帳を更新せよ」と書く。

#### 裁定 5（Away）：gameplay 経路を新設しない

調査結果：**出荷コードに `CompanionState.Away` を要求する経路は存在しない。**
`CompanionStateMachine` は `reason == Left` の Away をどの状態からも許すが、`RequestState(Away, Left)` の
呼び出しは Tests 側 6 ファイルだけ。非テストの唯一の入口は `CompanionActor` の初期状態
SerializeField（未加入開始用）。

裁定：P5 で Away を作る gameplay 経路を**新設しない**。仕様書 §1.3 が仲間切替を対象外とし、`CLAUDE.md` の
「未使用機能の実処理は先回りして作らない」にも反するため。§4.6 の Away 行は**契約として維持**し
（Away を維持する・通常表示と戦闘参加を有効化しない）、E29 は §4.6 が明示的に認めている
「初期化中の専用 Restore API」と `CompanionActor` の初期状態で検証する。

実 gameplay での Away 発生は **P8（猿・雉、切替、複数仲間の状態持ち越し）へ残件登録**し、
`P5_後続課題.md` に「契約は実装済み・発生経路は未実装」と明記する。

#### 裁定 6（§4.5 と §6.1）：§6.1 が正本

§4.5 の Capture 前提（Break 中・Hurt 中は受付拒否 等）と §6.1 の受付拒否条件が二重管理になっている。
**§6.1 を正本とし、§4.5 はその帰結**として扱う。実装は受付判定を §6.1 に対応する 1 箇所へ集約し、
§4.5 側は「§6.1 が拒否するので Capture 時にこの状態は来ない」という事前条件の表明に留める。
E08 が §6.1 の表を検査し、§4.5 側は別途の受付判定を持たない。

### 5. この工程で変更したファイル

| ファイル | 変更 |
|---|---|
| `CLAUDE.md` | コミット署名をモデル名・セッション URL の可変記述へ。P5 着手時点の実値を併記 |
| `Momotaro_P5_Detailed_Spec_v1.0.md` | 削除（v1.1 へ置換） |
| `Momotaro_P5_Detailed_Spec_v1.1.md` | 追加。実装正本 |
| `引き継ぎ書_P4完了_P5移行.md` | 追加 |
| `P5_統合受入結果.md` | 本書。新規 |
| `P5RequiredTests.json` | 新規。要求 54 件を登録（テストは未実装） |

`P4RequiredTests.json` は**維持**する（置換しない。仕様書 §15.5）。

### 6. 検証

オーナーが Unity Editor を閉じていたためブリッジが停止していた（`aliveAt` が 09:57:41Z で停止）。
10:28Z に再起動を確認し、`phase/5-world-exploration` 上で分岐直後の基準を取り直した。
常時許可のもと、EditMode 全件（Scene を置換する）と PlayMode 全件を確認なしで実行している。

| 実行 | id | 結果 | 終端 | 所要 |
|---|---|---|---|---|
| `compile-status`（force） | `cs1` | ok。**警告 0 件** | — | — |
| EditMode 全件（filter なし） | `regE3` | **1624 / 1624 Passed、Skip 0** | `completed` | 67.8 秒 |
| PlayMode 全件（filter なし） | `regP3` | **72 / 72 Passed、Skip 0** | `completed` | 60.5 秒 |
| `validate-project-data` | `vd4` | ok。Data 検証すべて通過 | — | — |
| `verify-required-tests`（P4、`runIds=regE3,regP3`） | `vt4` | **必須 226 / 226 Passed、未説明の Skip なし** | — | — |

- EditMode・PlayMode とも filter を付けない全件実行のため `expected` が入り、**予定 1624 / 実測 1624**、
  **予定 72 / 実測 72** が一致した。`termination` も両方 `completed`（`CLAUDE.md`「完走は termination で判定する」）。
- 実行記録は `_bridge/runs/regE3.json` / `_bridge/runs/regP3.json` に残る（`result.json` の上書きを受けない）。
- 最終確認：`isPlaying == false`、`isCompiling == false`。

**分岐直後の基準は P4 先端（`4a7650b`）と同一で、退行はない。** P5 の実装はこの基準の上から始める。

### 7. P5RequiredTests.json の内訳

仕様書 §15 の 3 つの表から要求 ID とメソッド名を機械的に抽出し、登録内容と突き合わせた結果、
**仕様書にあって一覧に無いもの 0 件・一覧にあって仕様書に無いもの 0 件・メソッド名の不一致 0 件**。
クラス割当（§15.1）の誤りも 0 件。

| mode / クラス | 件数 |
|---|---|
| `Momotaro.Tests.EditMode.P5ContractTests`（E01〜E30） | 30 |
| `Momotaro.Tests.EditMode.P5ValidatorTests`（V01〜V05） | 5 |
| `Momotaro.Tests.PlayMode.P5ExplorationPlayTests`（P01〜P19） | 19 |
| 合計 | **54** |

工程別：P5-01 が 5、P5-02 が 1、P5-03a が 4、P5-03b が 9、P5-04 が 6、P5-05 が 5、P5-06 が 2、
P5-07 が 7、P5-08 が 5、P5-09 が 6、P5-10 が 4。

**54 件はすべて未実装で、`implemented` は全件 `false`。** 現時点で Passed のものは無く、
P5 manifest での照合は不合格になるのが正しい状態である（仕様書 §14「未実施を Skip 成功に置き換えない」）。

### 8. 既知の残件

- 照合 Runner は `P4RequiredTests.json` 固定。P4／P5 manifest の明示選択は P5-09 で対応する（仕様書 §16.1）。
  それまで P5 の照合は実行できない。
- Away の実 gameplay 発生経路は未実装（裁定 5）。契約のみ。`P5_後続課題.md` へ P8 残件として記載する。
- `P5_ActorTransferInventory.md`（持ち越し台帳）は P5-03a で作成する。未作成。
- `README_Phase5_探索試遊版.md` / `P5_後続課題.md` は P5-10 で作成する。未作成。
- クラウド側の tar 作成スクリプトは前スレッドから引き継がれなかったため作り直した。
  PC 側 `$HOME/tools/`（`br.sh` / `wait.sh` / `tests.sh` / `op.sh` / `apply.sh`）も本スレッドで再作成済み。

### 9. 次の一手

P5-01（Session／Area State、Holder／Record 注入、ローカル互換）。要求 P5-E01〜E05。
徳の二重保持が無いことが工程ゲート。

---

## 記録 002：P5-01（Session／Area State、Holder／Record 注入、ローカル互換）

作成日：2026-09-21　担当：Claude（claude-opus-5）　工程ゲート：**通過**

### 1. 何を作ったか

仕様書 §4.1〜§4.3。徳の正本を Session の 1 個へ寄せ、Scene 上の Holder を「窓口」に変えた。
P3.5／P4 の試遊 Scene は未注入のまま従来どおり動く（ローカル Fallback）。

| ファイル | 種別 | 内容 |
|---|---|---|
| `Scripts/Gameplay/Session/GameSessionState.cs` | 新規 | 本編型 Session の純粋 State。`PlayerProgressState` を **1 個**所有。Area 辞書・訪問済み・加入・再出現周期 |
| `Scripts/Gameplay/Session/AreaRuntimeState.cs` | 新規 | 1 エリア分の世界状態。調査記録・開通 FlagId・Encounter クリア記録 |
| `Scripts/Gameplay/Companion/Investigation/IInvestigationRecordSink.cs` | 新規 | 調査記録への**狭い書込み契約**（§4.3）。`Count` と `TryMarkInvestigated` だけ |
| `Scripts/Gameplay/Progression/PlayerProgressHolder.cs` | 改修 | 外部 `Bind`、ローカル Fallback、共有 State 保護 |
| `Scripts/Gameplay/Companion/Investigation/InvestigationRecordHolder.cs` | 改修 | 外部 `Bind`、ローカル Fallback、`TryMarkInvestigated` を Holder 経由へ |
| `Scripts/Gameplay/Companion/Investigation/InvestigationRecord.cs` | 改修 | `IInvestigationRecordSink` を実装（メンバは既存のまま） |
| `Scripts/Gameplay/Companion/Investigation/InvestigationCoordinator.cs` | 改修 | 完了確定を `_record.Record.TryMark…` から `_record.TryMark…` へ（使用済み判定を通す） |
| `Tests/EditMode/P5ContractTests.cs` | 新規 | E01〜E05 |

### 2. 設計上の判断

**徳を二重に持たせない（工程ゲート）。** `PlayerProgressHolder` は `_localState`（自前）と `_boundState`（外部）を
持つが、`State` はどちらか一方だけを返す。E01 が `LocalState.Virtue == 0` を検査し、注入後にローカルへ
書き込んでいないことを固定した。`AreaRuntimeState` に徳を持たせていない（§4.2 の「World 状態用クラスへ
Virtue を複製しない」）。

**「読み取り」は使用済みにしない。** Bind 保護の判定に使う `_used` は `Grant` と `ResetProgress` でだけ立つ。
HUD が初期化前に `Virtue` を読んでも Bind 不能にならない（§4.2）。E02 がこれを明示的に検査する。

**共有 State は Scene 側から Reset できない。** `ResetProgress()` は `bool` を返すようにし、外部 Bind 済みなら
拒否して `false` を返す。未注入のローカル State では従来どおり動くため、既存の
`ProgressHolder_ResetProgress_ClearsVirtue` と `ResetProgress_ReturnsTotalToZero` はそのまま通る。

**`VirtueChanged` の契約は変えていない。** 「徳が実際に変化した瞬間のみ発火」のまま。実装中に一度
「GrantOnce 記録だけの消去でも通知する」形にしかけたが、徳が 0 のまま 0 を通知することになるため戻した。
field-like event の名前も `VirtueChanged` のままで、反射でバッキングフィールドを数える
`CombatHudViewModelTests` に影響しない。

**再出現周期は「いつクリアしたか」で持つ。** `AreaRuntimeState` はクリア済み Encounter を
`Dictionary<StableId,int>`（クリアした周期）で保持し、`IsEncounterCleared` は現在の周期と一致するときだけ
true を返す。`AdvanceRespawnCycle()` は周期を進めたうえで全 Area の記録を明示的に空にする。
どちらか一方でも成立するが、両方やることで「周期だけ進めて記録が残る」「記録だけ消して周期が古い」の
どちらの実装ミスも E05 が捕まえる。

### 3. テストが本当に欠陥を捕まえるかの確認（`CLAUDE.md` の手順）

「条件を足す」型の修正なので、**ガードだけを外した版を実機でビルドして落ちることを確認**した。
2 つのガードは同じテストの別の箇所で検査されるため、**1 つずつ**外して個別に確認している。

| 注入した欠陥 | 実行 | 結果 |
|---|---|---|
| `Bind` の使用後拒否（`if (_used)` → `if (false)`） | `p5neg` | E02 が失敗。「使用後の別参照への差し替えは拒否する。Expected: False But was: True」 |
| `ResetProgress` の共有保護（`if (_boundState != null)` → `if (false)`） | `p5neg2` | E02 が失敗。「Bind 済みの共有 State は Reset を拒否する。Expected: False But was: True」 |

どちらも**末尾の副次的な判定ではなく、その欠陥そのものを見ている Assert** で落ちた。判定を寄せ直す必要は無かった。
確認後に原本を復元し、差分が無いこと（`diff` 一致）とコンパイル警告 0 件を確認している。

### 4. 検証

| 実行 | id | 結果 | 終端 | 所要 |
|---|---|---|---|---|
| `compile-status`（force、復元後） | `cs5` | ok。**警告 0 件** | — | — |
| 絞込み `P5ContractTests`（`minPassed: 5`） | `p5e1` | 5 / 5 Passed | `completed` | 1.0 秒 |
| EditMode 全件 | `regE4` | **1629 / 1629 Passed、Skip 0** | `completed` | 66.0 秒 |
| PlayMode 全件 | `regP4` | **72 / 72 Passed、Skip 0** | `completed` | 60.7 秒 |
| `validate-project-data` | `vd5` | すべて通過 | — | — |
| `verify-required-tests`（P4、`runIds=regE4,regP4`） | `vt5` | **226 / 226 Passed、未説明の Skip なし** | — | — |

**EditMode は 1624 → 1629（＋5）で、増分は新規テストちょうど。既存の退行は 0。**
PlayMode は 72 のまま変化なし。両方 filter なしなので `expected` と実測が一致し、`termination` は `completed`。
最終確認：`isPlaying == false`。

書込み → `compile-status` → テストの順を毎回守っている（古いアセンブリを検査しない）。

### 5. 必須テスト一覧の更新

`P5RequiredTests.json` の P5-E01〜E05 を `implemented: true` にした。**実装済みは 5 / 54。**
残り 49 件は未実装のままで、Skip 成功や `allowedSkips` へ置き換えていない。

### 6. 残件・次工程への申し送り

- `GameSessionState` に全体 Reset を**置いていない**。新規開始は新しい実体を作る（§9.2）。E26 で固定する。
- `AreaRuntimeState.ClearNormalEncounterRecords()` は `internal`。呼べるのは `AdvanceRespawnCycle()` 経由だけで、
  Scene 側から通常 Encounter の記録を個別に消せない。
- Session を実際に生成・所有するのは Infrastructure（Bootstrap／Game Session 層）で、**P5-03b の作業**。
  本工程は Gameplay 側の純粋 State と注入口までで、まだ誰も `GameSessionState` を生成していない。
- 犬丸加入は `Recruit(StableId)` の語彙だけ置いた。出撃対象の選択は P8（仲間切替）まで不要なので実処理を作っていない。

### 7. 次の一手

P5-02（仮地形、Area／Entry カタログ、最小 Builder）。要求 P5-E25、V01〜V02 の該当部分。
または P5-03a（持ち越し台帳と Snapshot／Import）。仕様書 §14 の依存では P5-02 と P5-03a は並行可能。

---

## 記録 003：P5-03a（持ち越し台帳、Snapshot／Import、Down／Away 復元）

作成日：2026-09-21　担当：Claude（claude-opus-5）　工程ゲート：**一部未達（E07 のみ未着手）**

### 1. 到達点

仕様書 §4.4〜§4.6。**E06・E28・E29 は実装・合格。E07（Loading ゲート）は未着手**で、理由は §7。

| 要求 | 状態 |
|---|---|
| P5-E06 `TransferSnapshot_CapturesAfterCancellationAndRestoresVitals` | Passed |
| P5-E07 `LoadingGate_FreezesClocksAndStopsDirectTicks` | **未実装** |
| P5-E28 `TransferInventory_ClassifiesEveryMutableRuntimeField` | Passed |
| P5-E29 `CompanionRestore_ReconcilesDownAwayWithoutIllegalTransitions` | Passed |

### 2. 作ったもの

| ファイル | 種別 | 内容 |
|---|---|---|
| `Scripts/Gameplay/Transfer/ITransferableRuntime.cs` | 新規 | マーカー契約と `ExportTransferSnapshot`／`TryImportTransferSnapshot` 1 組 |
| `Scripts/Gameplay/Transfer/TransferValue.cs` | 新規 | 値域検証（NaN・Infinity・負の残り時間・HP 範囲） |
| `Scripts/Gameplay/Transfer/PlayerTransferSnapshots.cs` | 新規 | Vital／Stamina／HitReaction／PlayerVitals の Snapshot |
| `Scripts/Gameplay/Transfer/CompanionTransferSnapshots.cs` | 新規 | Flinch／CompanionVitals／Guard／Evade／Defense／Combat／Guardian／Actor の Snapshot |
| `P5_ActorTransferInventory.md` | 新規 | **持ち越し台帳**。15 型・145 行の分類 |
| `Vital` / `StaminaState` / `HitReactionState` / `FlinchState` | 改修 | Export／Import を実装 |
| `EnemyGuardAbility` / `EnemyEvadeAbility` | 改修 | 同上（仲間の防御が流用している純粋ロジック） |
| `CompanionVitals` | 改修 | 同上。入れ子の `FlinchState` を合成 |
| `PlayerVitalsHolder` / `PlayerHitReaction` | 改修 | 同上。`PostHitInvincibleRemaining` を公開 |
| `CompanionCombatController` / `CompanionDefenseController` / `CompanionGuardianController` / `CompanionActor` | 改修 | 同上 |
| `CompanionStateChangeReason` | 改修 | `Restored = 19` を追加 |
| `CompanionStateMachine` | 改修 | `Restored` を Away 遷移と Down／Stagger からの離脱理由として許可 |
| `CompanionStateArbiter` | 改修 | **`TryRestoreState`**（§4.6 の初期化中専用 Restore API） |
| `Tests/EditMode/P5ContractTests.cs` | 追記 | E06・E28・E29 |

### 3. 裁定 2 の実装（`IPlayerHurtReaction` を触らずに済ませた）

記録 001 §4 の裁定どおり、**契約は 1 文字も変えていない**。実体は pure class `HitReactionState` で、
そこに Export／Import を置き、`PlayerHitReaction` が合成して `ITransferableRuntime` だけを実装する。
追加したのは読み取り専用の `PostHitInvincibleRemaining` 1 つで、破壊的変更ではない。
`PlayerVitalsHolder` と `PlayerStateController` は無変更、既存テスト 2 ファイルも無変更。

### 4. 裁定 1 の実装（対象型の発見も反射）

E28 は**手書きの型リストを持たない**。`ITransferableRuntime` を実装する型を Gameplay アセンブリから
反射で集め、台帳と双方向に照合する。検査は 4 つ。

1. 反射で見つかった対象型に台帳の節があるか
2. 台帳の節が実在する型か、行が実在するフィールドを指すか（改名・削除の取り残しを検出）
3. `合成` 分類の参照先が台帳の別節にあるか（**入れ子の時間所有型**を辿る）
4. **未分類のフィールドが無いか**

自動プロパティのバッキングフィールド（`<IsDown>k__BackingField`）はプロパティ名へ正規化して扱う。
台帳の行は Unity 上で実際にフィールドを列挙して作った（一時ダンプテストを書いて実機で採取し、作業後に撤去）。
推測で書いていないので、初回から一致した。

**「`readonly` は不変を意味しない」を台帳の読み方へ明記**し、`CompanionVitals._flinch` を実例として
`合成` に分類した。この 1 行が無いと、ひるみの蓄積と残り時間が丸ごと持ち越し対象から落ちる。

### 5. 裁定 5 の実装（Away）

Away を作る gameplay 経路は**新設していない**。`CompanionStateArbiter.TryRestoreState` は
§4.6 が認める「初期化中の専用 Restore API」で、状態ごとに経路を分ける。

- Away → 退場と同じ遷移（`RequestState`）。**`ForceHit` へ渡さない**（非対応の状態要求。§4.6）
- Down／Stagger → 被弾由来の強制状態として `ForceHitState`。ただし理由は専用の `Restored`
- Follow → 通常遷移
- 攻撃中・探索中などの行動状態 → **拒否**（到着時に行動の途中へ復元しない）

理由を `Restored` にしたのは、購読者が実被弾・実復帰と区別できるようにするため。
`Defeated`／`Staggered` を流用すると、被弾演出や SE が到着のたびに鳴る。E29 がこれを検査する。

### 6. テストが本当に欠陥を捕まえるかの確認（`CLAUDE.md` の手順）

| 注入した欠陥 | 実行 | 結果 |
|---|---|---|
| `EnemyGuardAbility` へ台帳に無い可変フィールドを 1 本足す | `negA` | E28 が失敗。「**未分類のフィールド: …EnemyGuardAbility._undocumentedTimer（Single）**」と場所を名指しした |
| Down／Stagger の復元理由を `Restored` → `Defeated` にする | `negB` | E29 が失敗。「**Stagger：撃破を偽造しない。Expected: not collection containing Defeated**」 |

どちらも末尾の副次的な判定ではなく、その欠陥そのものを見ている Assert で落ちた。
E28 の失敗文は「どの型のどのフィールドが未分類か」をそのまま出すので、後工程で落ちたときに何をすればよいかが分かる
（裁定 4 の「落ちたら台帳を更新する」が実行可能になっている）。復元後、両ファイルとも原本と `diff` 一致・警告 0 件を確認。

### 7. E07（Loading ゲート）を本工程に入れなかった理由

E07 は「**直接 Tick しても**時計・Move・Warp・攻撃を進めない」ことを要求する。
`Update()` 側の分岐では足りず、各駆動系の `Tick` の内側に停止判定を置く必要がある
（`CLAUDE.md`「時間は外部注入する。Update は Tick を呼ぶだけ」という構造のため）。

対象は `PlayerStateController` / `PlayerVitalsHolder` / `CompanionCombatController` / `CompanionFollowController` /
`CompanionMotor` / `CompanionGuardianController` / `CompanionDefenseController` / `CompanionVitals` ほかに跨がる。
これは**遷移の停止窓口そのもの**で、実体は SceneFlow 側（P5-03b §6.2 手順 3）にある。

仕様書 §14 が「複数工程にまたがる通しテストは必要な実装が揃った時点で実行し、それまでは未実施と記録する」
「工程順は依存を満たせば一部前後してよい」と定めているので、**E07 は P5-03b の冒頭で、Loading ゲートの導入と同時に実装する**。
`P5RequiredTests.json` の P5-E07 は `implemented: false` のまま残してあり、Skip 成功に置き換えていない。

### 8. 検証

| 実行 | id | 結果 | 終端 | 所要 |
|---|---|---|---|---|
| `compile-status`（force、復元後） | `csK` | ok。**警告 0 件** | — | — |
| 絞込み `P5ContractTests` | `p5e29` | 8 / 8 Passed | `completed` | 0.9 秒 |
| EditMode 全件 | `regE5` | **1632 / 1632 Passed、Skip 0** | `completed` | 70.0 秒 |
| PlayMode 全件 | `regP5` | **72 / 72 Passed、Skip 0** | `completed` | 60.4 秒 |
| `validate-project-data` | `vd6` | すべて通過 | — | — |
| `verify-required-tests`（P4） | `vt6` | **226 / 226 Passed、未説明の Skip なし** | — | — |

**1629 → 1632（＋3）で、増分は新規テストちょうど。既存の退行は 0。** PlayMode は 72 のまま。
両方 filter なしで `expected` と実測が一致し、`termination` は `completed`。最終確認：`isPlaying == false`。

一時的に作った `__TransferFieldDumpTests.cs` は `_to_delete/p5_03a/` へ退避済み（PC 側シェルは削除できないため）。

### 9. 必須テスト一覧の更新

P5-E06・E28・E29 を `implemented: true` にした。**実装済みは 8 / 54。**

### 10. 残件・次工程への申し送り

- **P5-E07（Loading ゲート）は未実装。** P5-03b の冒頭で実装する（§7）。
- 台帳は**凍結していない**。P5-05・P5-07 で可変時間値が増えたら E28 が落ちるので、そのとき台帳を更新する（裁定 4）。
  この方針は台帳の冒頭にも書いてある。
- `AreaTransferSnapshot`（Actor 一式をまとめる遷移用の器）は**まだ無い**。単位ごとの Snapshot までが本工程で、
  それを束ねて運ぶのは P5-03b の遷移手順（§6.2 手順 5）の仕事。
- `PlayerVitals.Stamina` と `PlayerVitalsHolder._stamina`（`StaminaState`）が二重に存在する。
  現行の実体は後者で、前者は表示用。台帳に両方を載せて区別を明記したが、**将来の整理候補**として残す。
- Away の実 gameplay 発生経路は未実装のまま（裁定 5）。契約と復元 API のみ。P8 へ。

### 11. 次の一手

P5-03b（SceneFlow 拡張、明示注入、Ready・世代付き復旧）。冒頭で Loading ゲートを入れて **E07 を回収**し、
続けて E08〜E10、P01〜P04、P12〜P13。あるいは P5-02（仮地形・カタログ・最小 Builder）を先に片付けてもよい。

---

## 記録 004：P5-02（仮地形、Area／Entry カタログ、最小 Builder）

作成日：2026-09-23　担当：Claude（claude-opus-5）　工程ゲート：**通過**

### 1. 到達点

仕様書 §3／§13.1／§13.3。**A／B を単独で開いて検査できる**状態になった。

| 要求 | 状態 |
|---|---|
| P5-E25 `AreaCatalog_ResolvesEntriesAndRejectsUnsupportedFloor` | Passed |
| （補助）`AreaCatalog_ShippedPhase5CatalogResolves` | Passed。出荷 Data から構築 |
| （補助）`GeneratedAreaScenes_LoadWithEntriesMatchingCatalog` | Passed。**実 Scene を読み直して**検査 |

V01〜V02 の本体（全必須配線・壊れた Fixture の診断）は P5-09。本工程は「該当部分」＝カタログとカタログ経由の解決まで。

### 2. 作ったもの

| ファイル／生成物 | 種別 | 内容 |
|---|---|---|
| `Scripts/Core/World/CardinalDirection.cs` | 新規 | XZ 平面の 4 方向。Data が使うので Core に置く |
| `Scripts/Data/World/AreaEntryDefinition.cs` | 新規 | 入口 1 つの定義（ID と向き） |
| `Scripts/Data/World/AreaDefinition.cs` | 新規 | エリア定義。Floor=0 制限を `Validate` でも判定 |
| `Scripts/Data/World/AreaCatalogData.cs` | 新規 | カタログ。死亡再開点の正本 |
| `Scripts/Gameplay/Session/AreaCatalog.cs` | 新規 | 不変 Snapshot と解決 API |
| `Scripts/Gameplay/Session/AreaRoot.cs` | 新規 | Scene の根。AreaId と入口の明示参照 |
| `Scripts/Gameplay/Session/AreaEntryPoint.cs` | 新規 | Scene 上の入口（到着位置と代替候補） |
| `Scripts/Editor/Phase5/Phase5Layout.cs` | 新規 | 寸法・座標の定数 |
| `Scripts/Editor/Phase5/Phase5AreaIds.cs` | 新規 | 安定 ID と出力先 |
| `Scripts/Editor/Phase5/Phase5Placeholder.cs` | 新規 | 単色 Material・箱・ラベルの生成補助 |
| `Scripts/Editor/Phase5/Phase5ExplorationBuilder.cs` | 新規 | 3 Scene・Data・仮地形の生成 |
| `Scripts/Editor/Bridge/EditorBridgeOperations.cs` | 改修 | `build-exploration-trial` を許可リストへ追加 |
| `Scenes/Tests/Phase5/` | 生成 | `SCN_Phase5_AreaA` / `AreaB` / `ExplorationTrial` |
| `Data/Tests/Phase5/` | 生成 | `SO_Area_P5_A` / `SO_Area_P5_B` / `SO_AreaCatalog_P5` |
| `Art/Environment/Placeholder/Phase5/` | 生成 | 単色 Material 13 件 |
| `ProjectSettings/EditorBuildSettings.asset` | 改修 | 3 Scene を追記（既存項目は維持） |

### 3. 設計上の判断

**座標は Scene、ID と向きは Data。** §3.1 は「各 Entry は到着位置・4 方向の向き・代替配置候補を持つ」と書いているが、
座標を Data と Scene の両方に置くと地形を動かしたときに黙って食い違う。そこで
`AreaEntryPoint`（Scene）が到着位置と代替候補を Transform で持ち、`AreaEntryDefinition`（Data）が ID と向きを持つ。
補助テストが Data の入口一覧と Scene 上の入口の 1 対 1 対応を検査するので、片方だけ増やすと落ちる。

**FloorId は推定も補正もしない。** `AreaCatalog.TryBuild` は非 0 を理由付きで拒否し、
**部分的に使えるカタログを返さない**（1 件でも不整合があれば `catalog` は null）。`AreaDefinition.Validate` も
同じ判断をするので、Bridge の `validate-project-data` からも同じ結果になる。

**死亡再開点が解決できないカタログは構築しない。** ここが解けないと敗北から復帰できず、
実機で初めて気付くことになる。E25 が明示的に検査する。

**Builder は未保存 Scene があれば何もしない。** 実処理はダイアログ無しの `BuildAll()` に分け、メニューは薄いラッパー。
ブリッジへは `build-exploration-trial` として登録した。`Momotaro.EditorBridge` は `Momotaro.Editor` を参照しないので、
既存の Scene 生成と同じく**反射で呼ぶ**（最初に直接参照で書いて CS0234 になり、既存の作法へ合わせ直した）。

**カタログ Data は `GameDataAsset` 派生のメイン Asset として作った**（§13.4）。
これにより `ProjectDataValidator.CollectAllDataAssets()` の収集範囲（フォルダ非限定）へ自然に入る。
実際に `validate-project-data` が生成直後の 3 件を含めて通過することを確認した。

### 4. 仮地形

| | エリア A | エリア B |
|---|---|---|
| 広さ | 24 × 18 | 28 × 22 |
| 入口 | `area_p5_a_start`（北向き・死亡再開点）、`area_p5_a_from_b`（西向き） | `area_p5_b_from_a`（東向き・直開きの既定入口） |
| 地形 | 外壁、L 字通路の仕切り壁、壁越し調査用の衝立、水場 | 外壁、アリーナ境界の外枠 |
| 目印 | 門・レバー・調査・調査(壁越し)・B へ | A へ（扉）・調査・遭遇・戦闘復帰点・出現点 ×3 |

- **L 字通路**：西の大部屋と東の通路を仕切り壁で分け、南側だけ開けた。直線追従では仕切りに当たり、
  NavMesh 経路なら南の開口を回って到達できる（§3.2）。P5-05 の P11 がここを使う。
- **水場**：見た目は薄い板、通行止めは**透明な境界**で分けた（§3.3「落下や水泳は実装しない」）。
- 通路幅は 4.0（最大通行 Actor の直径＋0.4 以上。§3.2）。
- 「エリア A／B」「門」「レバー」「調査」「戦闘区域」は **TextMesh のラベルと色分けした箱**で識別できる。
  Gizmos だけを表示に使っていない（§3.2）。
- 目印は**当たり判定なしの箱とラベルだけ**で、実処理は持たせていない。門・レバーは P5-04、
  アリーナ封鎖と出現は P5-07 で、この位置へ機能を足す。先回りして空の配線を置かない。

### 5. テストが本当に欠陥を捕まえるかの確認（`CLAUDE.md` の手順）

| 注入した欠陥 | 実行 | 結果 |
|---|---|---|
| `AreaCatalog.TryBuild` の FloorId 判定を外す | `negC` | E25 が失敗。「**非 0 の FloorId を含むカタログは構築しない。Expected: False But was: True**」 |

核心の Assert で落ちた。復元後、原本と `diff` 一致・警告 0 件を確認。

**実 Scene を読み直す検査も入れた。** 保存した Scene を開き直すまで Missing Script は現れない
（`CLAUDE.md` が名指しする事故で、`InvestigationRecordHolder` と `CompanionRosterContext` で実際に起きている）。
Builder 直後の検査だけでは通ってしまうので、`GeneratedAreaScenes_LoadWithEntriesMatchingCatalog` が
実ファイルから開き直して `AreaRoot`・入口・代替候補が生きていることを見る。
ガードは F01 の方針どおり「**未保存の変更があるときだけ** Skip」で、絞込み実行では Skip 0 で走った。

### 6. 検証

| 実行 | id | 結果 | 終端 | 所要 |
|---|---|---|---|---|
| `compile-status`（force、復元後） | `csU` | ok。**警告 0 件** | — | — |
| `run-op build-exploration-trial`（1 回目） | `bx1` | ok。Data 3・Scene 3・Build Settings +3 | — | — |
| `run-op build-exploration-trial`（2 回目） | `bx2` | ok。**Build Settings +0**。重複生成なし | — | — |
| 絞込み `P5ContractTests` | `p5f` | 11 / 11 Passed、**Skip 0** | `completed` | 1.9 秒 |
| EditMode 全件 | `regE6` | **1635 / 1635 Passed、Skip 0** | `completed` | 70.0 秒 |
| PlayMode 全件 | `regP6` | **72 / 72 Passed、Skip 0** | `completed` | 64.8 秒 |
| `validate-project-data` | `vd8` | すべて通過（Phase5 の 3 件を含む） | — | — |
| `verify-required-tests`（P4） | `vt8` | **226 / 226 Passed、未説明の Skip なし** | — | — |

**1632 → 1635（＋3）で、増分は新規テストちょうど。既存の退行は 0。** PlayMode は 72 のまま。
2 回目の生成で Build Settings の追加が 0 件だったことが、再生成の冪等性（V03 の一部）を示している。
最終確認：`isPlaying == false`。

### 7. 途中で踏んだ問題（`.git/index.lock`）

作業中の `git status` が `.git/index.lock` を作り、**PC 側シェルはファイルを削除できないためロックが残った**。
このままだと Windows 側の git がすべて止まる。`_to_delete/git_locks/` へ `mv` して退避し、解消を確認した。

再発を防ぐため、`CLAUDE.md` の git の節へ **`git --no-optional-locks status` を使う**ことを追記した。
`--no-optional-locks` なら索引を書かないのでロックを作らない（実際に確認済み）。
`git log` / `git show` / `git diff` は索引を書かないのでそのままでよい。

### 8. 必須テスト一覧の更新

P5-E25 を `implemented: true` にし、`supportingTests` へ補助 2 本の完全名を書いた。**実装済みは 9 / 54。**

### 9. 残件・次工程への申し送り

- **P5-E07（Loading ゲート）は引き続き未実装。** P5-03b の冒頭で回収する（記録 003 §7）。
- 統合起動 Scene は**目印だけ**。Session の生成と A への自動遷移は P5-03b。
- `AreaRoot` が持つのは AreaId と入口まで。カメラ領域（P5-06）、仕掛け（P5-04）、Encounter 定義（P5-07）は各工程で足す。
- NavMesh はまだベイクしていない。P5-05 で、L 字通路と門の通行状態を含めてベイク・保存する（§10.2）。
- P5 専用 Validator（Scene／Asset）は未着手。V01〜V05 は P5-09。
- 生成した仮 Material 13 件は `Art/Environment/Placeholder/Phase5/` にあり、既存 Prototype には触れていない（§13.1）。

### 10. 次の一手

P5-03b（SceneFlow 拡張、明示注入、Ready・世代付き復旧）。冒頭で Loading ゲートを入れて E07 を回収し、
続けて E08〜E10、P01〜P04、P12〜P13。P5-02 で A／B の Scene と入口が揃ったので、実 Scene を使う PlayMode が書ける。

---

## 記録 005：P5-03b-1（Loading ゲートと遷移契約）

作成日：2026-09-23　担当：Claude（claude-opus-5）　工程ゲート：**通過**

P5-03b はこれまでの 2〜3 倍の規模のため、オーナー判断で 2 つに分けた。
本記録は前半＝**Gameplay 側の契約と E07〜E10**。後半（Infrastructure の SceneFlow 拡張と PlayMode 6 本）は記録 006。

### 1. 到達点

| 要求 | 状態 |
|---|---|
| P5-E07 `LoadingGate_FreezesClocksAndStopsDirectTicks` | Passed。**記録 003 §7 で先送りした分を回収** |
| P5-E08 `AreaTransition_RejectsInvalidModesAndActions` | Passed |
| P5-E09 `Transition_ReentryAndStaleCompletionCannotReleaseNewRun` | Passed |
| P5-E10 `TransitionFailure_HasBoundedRecoveryWithoutDuplicateLoads` | Passed |

P01〜P04・P12〜P13（実 Scene の PlayMode）は記録 006。

### 2. 作ったもの

| ファイル | 種別 | 内容 |
|---|---|---|
| `Scripts/Gameplay/Session/GameplayClockGate.cs` | 新規 | Loading ゲートと供給点 |
| `Scripts/Gameplay/Session/AreaTransitionContracts.cs` | 新規 | 段階・拒否理由・要求・受付条件・ロード観測の契約 |
| `Scripts/Gameplay/Session/AreaTransitionCoordinator.cs` | 新規 | 受付判定・世代管理・失敗復旧の純粋な調停役 |
| 駆動系 16 ファイル | 改修 | 各 `Tick` の入口へゲート判定を追加 |
| `Tests/EditMode/P5ContractTests.cs` | 追記 | E07〜E10 |

ゲートを入れた駆動系：`PlayerStateController` / `PlayerVitalsHolder` / `PlayerHitReaction` /
`CompanionCombatController` / `CompanionDefenseController` / `CompanionFollowController` /
`CompanionGuardianController` / `CompanionHitReceiver` / `CompanionInvestigationController` /
`CompanionTargetTracker` / `CompanionMotor`（WarpTo・SetMoveTarget・FixedUpdate）/
`CompanionMovementArbiter.Submit` / `EnemyAttackController` / `EnemyDefenseController` /
`EnemyBrain` / `EnemyThreatTracker`。

### 3. 設計上の判断

**ゲートは `Tick` の内側に置く。** 本プロジェクトは「`Update()` は `Tick(Time.deltaTime)` を呼ぶだけ」という形で
時間を外部注入している（`CLAUDE.md`）。だから `Update()` 側だけで止めても、**テストや他の駆動から `Tick` を
直接呼べば進んでしまう**。E07 の「直接 Tick でも進めない」はこの穴を指しているので、判定を各 `Tick` の入口へ置いた。

**純粋クラスには入れない。** `StaminaState`・`HitReactionState`・`FlinchState` などは
「渡された deltaTime をそのまま進める」のが契約で、そこへ暗黙の停止を混ぜると決定的な検証ができなくなる。
止めるのは<b>時間を配る側</b>＝ MonoBehaviour の駆動系だけにした。

**既定は「凍結していない」。** `CompanionActivityProvider` は未設定で停止側へ倒すが、こちらは逆。
遷移という<b>一時的な事象</b>を表すので、既定を凍結にするとゲートを知らない P3.5／P4 の試遊 Scene が丸ごと止まる。
「供給元が無い＝遷移していない」が正しい既定で、この向きの違いはコード内に理由付きで書いた。

**受付条件の正本は §6.1 ひとつ。** `IAreaTransitionConditions` という狭い契約に集約し、
判定順は「目的地 → AreaReady → モード → 生存 → **戦闘** → 主人公の行動中 → 排他」。
戦闘を主人公の行動中より先に見るのは §8.3 の競合表が「戦闘開始を先に確定」と定めているため。

**タイムアウトは諦めではない。** Unity の非同期ロードはキャンセルできないので、
監視が切れても操作自体は生きている。`TickUnscaled` はタイムアウト後も<b>同じ操作を観測し続け</b>、
遅れて完了したら復旧を 1 回だけ始める。**排他はここで解除しない**（解除すると生きている古いロードの上に
新しいロードが乗る）。E10 が `LoadStartCount` が 1 のままであることまで見る。

### 4. テストが本当に欠陥を捕まえるかの確認（`CLAUDE.md` の手順）

| 注入した欠陥 | 実行 | 結果 |
|---|---|---|
| `PlayerHitReaction.Tick` の内側のゲートを外す | `negD` | E07 が失敗。「凍結中は直接 Tick しても時計が進まない。Expected: 0.5 But was: 0.1」 |
| `Release` の世代一致の確認を外す | `negE` | **E09 が落ちなかった**（§5 参照） |
| 同上（テストを直したあと） | `negE2` | E09 が失敗。「旧 finally は新世代の排他を解除しない。Expected: False But was: True」 |

### 5. E09 のテストを作り直した（判定を核心へ寄せた）

世代一致の確認を外す欠陥を入れたところ、**E09 が緑のまま通ってしまった。**

原因ははっきりしている。古い世代の通知をまとめて `Preparing` の時点で投げていたため、
`Release` も `NotifyReady` も `NotifyBinding` も<b>段階の判定だけで弾かれていた</b>。
世代の判定が外れていても結果が変わらない書き方になっていた。

`CLAUDE.md`「落ち方が末尾の副次的な判定だったら、判定を核心へ寄せ直す」に従い、
各通知を「**段階は合っているのに世代だけ古い**」状況でぶつけるよう組み直した。

- 段階 `Preparing` で世代違いの `NotifyLoadStarted` / `NotifyFailed`
- 段階 `Loading` で世代違いの `NotifyBinding`
- 段階 `Binding` で世代違いの `NotifyReady`
- 段階 `Ready` で世代違いの `Release` ← **本丸**

作り直した版は同じ欠陥で正しく落ちた。**最初の版は E09 の名前どおりのことを検証していなかった**ので、
これは書き直して正解だった。

### 6. 検証

| 実行 | id | 結果 | 終端 | 所要 |
|---|---|---|---|---|
| `compile-status`（force、復元後） | `cs13` | ok。**警告 0 件** | — | — |
| 絞込み `P5ContractTests` | `p5g` | 15 / 15 Passed、Skip 0 | `completed` | 1.9 秒 |
| EditMode 全件 | `regE7` | **1639 / 1639 Passed、Skip 0** | `completed` | 68.3 秒 |
| PlayMode 全件 | `regP7` | **72 / 72 Passed、Skip 0** | `completed` | 60.4 秒 |
| `validate-project-data` | `vd9` | すべて通過 | — | — |
| `verify-required-tests`（P4） | `vt9` | **226 / 226 Passed、未説明の Skip なし** | — | — |

**1635 → 1639（＋4）で、増分は新規テストちょうど。既存の退行は 0。**
ゲートを 16 ファイルの駆動系へ入れたにもかかわらず PlayMode が 72 / 72 のままなのは、
既定が「凍結していない」で、ゲートを知らない既存 Scene の挙動が変わらないため。
最終確認：`isPlaying == false`。

### 7. 途中で踏んだ問題（静的レジストリの残留）

E07 のリグが `PerceptionTargetRegistry` へ敵を登録したまま TearDown で GameObject だけを破棄したため、
**次のテストが `MissingReferenceException` で落ちた**。`CLAUDE.md` が警告している静的登録の残留そのもの。

`P5ContractTests` へ `[SetUp]` と `[TearDown]` を足し、`PerceptionTargetRegistry.Clear()` と
`GameplayClockProvider.Current = null` を必ず行うようにした。E06 の中に書いていたその場の `Clear` は
TearDown へ集約して外した。

### 8. 必須テスト一覧の更新

P5-E07〜E10 を `implemented: true` にした。**実装済みは 13 / 54。**

### 9. 残件・次工程への申し送り（記録 006 へ）

- `AreaTransitionCoordinator` は<b>純粋な調停役</b>で、実際の Scene ロードはまだ誰もしていない。
  `IAreaLoadOperation` を実装して繋ぐのは Infrastructure（`SceneFlowManager` の拡張）の仕事。
- 既存 `SceneFlowManager` は真偽値 1 つの `TransitionGuard` しか持たない。世代・失敗・復旧・AreaReady 待ちを
  本調停役へ委譲する形に作り替える。既存 Launcher 経路は維持する（§12.1）。
- `IAreaTransitionConditions` の実装（Scene 側の状態を読む）は未着手。
- `AreaTransferSnapshot`（Actor 一式を束ねる器）も未着手。記録 003 §10 のとおり §6.2 手順 5 の仕事。
- Gameplay 時計を止めても **Presentation の暗転と Error UI は unscaled で動く**必要がある（§6.3）。
  これはゲートを見ないので現状のままでよいが、P5-06 の UI 実装時に踏まないよう記録しておく。

### 10. 次の一手

記録 006＝P5-03b-2。`SceneFlowManager` を世代付き・失敗復旧つきへ拡張し、
`IAreaTransitionConditions` と `IAreaLoadOperation` を Scene／Unity 側で実装して、
P01〜P04・P12〜P13 を実 Scene で通す。P5-02 で A／B の Scene と入口が揃っているので材料はある。

---

## 記録 006：P5-03b-2（SceneFlow 拡張と実 Scene 遷移）

作成日：2026-09-23　担当：Claude（claude-opus-5）　工程ゲート：**一部未達（P02・P03・P04 が残り）**

### 1. 到達点

**A ↔ B の実 Scene 遷移が動くようになった。** Session が 1 個であること、失敗しても進行を失わないこと、
古い実行世代が新しい遷移を壊さないことを実 Scene で確認した。

| 要求 | 状態 |
|---|---|
| P5-P01 `BootstrapAndDirectOpen_CreateOneReadySession` | Passed |
| P5-P12 `InvalidDestination_RecoversWithoutLosingSession` | Passed |
| P5-P13 `CompletionCallback_CanRequestTravelWithoutOldRunCorruption` | Passed |
| P5-P02 `FirstRewardAfterLoad_ReachesInjectedStateAndHud` | **未実装**（HUD が Area Scene に無い） |
| P5-P03 `AreaRoundTrip_PreservesProgressVitalsAndDownTimers` | **未実装**（Actor 値の束が無い） |
| P5-P04 `HeldExitInput_DoesNotBounceBackOrStartEncounter` | **未実装**（出入口 Trigger と入力が無い） |

### 2. 作ったもの

| ファイル | 種別 | 内容 |
|---|---|---|
| `Gameplay/Session/GameSessionProvider.cs` | 新規 | Session の供給点 |
| `Gameplay/Session/AreaContext.cs` | 新規 | AreaReady と現在エリア。失敗時の再開も持つ |
| `Gameplay/Session/AreaTransitionConditionsSource.cs` | 新規 | §6.1 の受付条件を Scene から読む |
| `Infrastructure/World/GameSessionBootService.cs` | 新規 | Session を 1 個だけ所有する常駐サービス |
| `Infrastructure/World/AreaSceneLoadOperation.cs` | 新規 | `AsyncOperation` を観測可能な形に包む |
| `Infrastructure/World/AreaTransitionService.cs` | 新規 | §6.2 の実行。判断は Gameplay の調停役 |
| `Infrastructure/World/AreaInitializer.cs` | 新規 | §5.1 の初期化順 |
| `Infrastructure/Bootstrap/BootstrapServices.cs` | 新規 | 常駐サービスを型で引く口 |
| `Infrastructure/Bootstrap/BootstrapRoot.cs` | 改修 | Session／遷移サービスの登録、失敗理由の公開 |
| `Infrastructure/Bootstrap/ServiceRegistry.cs` | 改修 | `Get<T>()` と `LastFailure` |
| `Gameplay/Player/PlayerStateController.cs` | 改修 | `IsFreeToTravel`（§6.1 の平常状態） |
| `Editor/Phase5/Phase5ExplorationBuilder.cs` | 改修 | AreaSystems と主人公を Area Scene へ配置 |
| `Tests/PlayMode/P5ExplorationPlayTests.cs` | 新規 | P01・P12・P13 |

### 3. 設計上の判断

**判断は Gameplay、実行は Infrastructure。** 受付条件・世代・タイムアウト・復旧回数といった規則は
`AreaTransitionCoordinator`（純粋クラス、E08〜E10 で検証済み）にあり、`AreaTransitionService` は
Unity への橋渡しだけをする。Coroutine は遷移をまたいで生き残りうるので、各段の通知に必ず
自分の `TransitionId` を添え、調停役が一致を確かめる。

**`IsFreeToTravel` は許可リストで書いた。** `PlayerState` が `Idle`／`Move` のときだけ true。
拒否リストにすると、新しい行動状態を足したときに<b>既定で通ってしまう</b>穴ができる。

**Session は遅延生成。** 起動しただけでは作らず、本編型 Area の初期化が要求したときだけ作る。
P3.5／P4 の試遊 Scene が本編 Session を自動注入されない（§5.2）ためで、実際 PlayMode 全 75 件のうち
既存 72 件は Session に触れないまま通っている。

### 4. テストが出した欠陥 3 件

実 Scene で動かして初めて出たもの。いずれも仕様書に書いてあるのに実装していなかった。

**(a) 失敗しても旧 Area の活動が戻らない（§6.3 の 2 行目）。**
受理の時点で活動を閉じる（手順 3）ので、ロードに失敗したまま放置すると
「その場に留まるが何も操作できない」状態になっていた。
`AreaContext.ReopenAfterFailedTransition()` を足し、旧 Scene が生きている失敗経路で呼ぶようにした。
初期化のやり直しではないので `ReadyCount` は増やさず `ReopenCount` で別に数える。

**(b) 再入の拒否理由が誤っていた。** 遷移中の再要求が `AlreadyTransitioning` ではなく `NotReady` を返していた。
受理で活動を閉じる＝ `AreaReady` が false になるため、条件を先に見ると再入の理由が
「まだ準備できていない」にすり替わる。呼び出し側が原因を取り違えるので、
排他の判定を先頭へ移した（§6.2 手順 2「外部通知より先に再入を拒否する」）。E08 にもこの検査を足した。

**(c) 受付条件に主人公が必要だった。** Area Scene に主人公が居ないと
`IsPlayerAlive` が false・`IsPlayerBusy` が true になり、遷移が一切通らない。
これは<b>意図した安全側の挙動</b>（R2-08「未配線を許可側へ戻さない」と同じ）なので条件は緩めず、
Builder が主人公 Prefab を入口へ配置するようにした。

### 5. テストが本当に欠陥を捕まえるかの確認（`CLAUDE.md` の手順）

| 注入した欠陥 | 実行 | 結果 |
|---|---|---|
| 失敗時に旧 Area の活動を戻さない | `negF` | P12 が失敗。「失敗のあとも新しい要求を受け付ける。理由=NotReady」 |

(a) は<b>テストが先に見つけた欠陥</b>なので、実装前に赤を確認していることになる。
復元後、原本と `diff` 一致・警告 0 件を確認。

### 6. 途中で踏んだ問題（常駐の持ち越し）

`BootstrapRoot` は `DontDestroyOnLoad` なので、**前のテスト実行が残した実体が生きていると
新しい方が「重複」として Awake で破棄され、初期化が一切走らない**。
`BootstrapSucceeded` が false のままなのに理由が空、という分かりにくい落ち方をした。

既存の `CompanionTrialScenePlayTests` は SetUp で古い実体を先に破棄していた。同じ手順へ揃え、
併せて `ServiceRegistry.LastFailure` と `BootstrapRoot.BootstrapFailure` を足して
「どのサービスがなぜ止めたか」がテストの失敗文に出るようにした。

TearDown の順序も直した。常駐を消してから Scene を替えると、残った `AreaInitializer` が
サービス不在で初期化に失敗してエラーログを出す。**先に Area Scene から抜けてから**常駐を消す。

### 7. 検証

| 実行 | id | 結果 | 終端 | 所要 |
|---|---|---|---|---|
| `compile-status`（force、復元後） | `cs26` | ok。**警告 0 件** | — | — |
| `run-op build-exploration-trial` | `bx4` | ok。Build Settings +0（冪等） | — | — |
| 絞込み `P5ExplorationPlayTests` | `p5p5` | 3 / 3 Passed、Skip 0 | `completed` | 6.6 秒 |
| EditMode 全件 | `regE8` | **1639 / 1639 Passed、Skip 0** | `completed` | 46.7 秒 |
| PlayMode 全件 | `regP8` | **75 / 75 Passed、Skip 0** | `completed` | 78.9 秒 |
| `validate-project-data` | `vd10` | すべて通過 | — | — |
| `verify-required-tests`（P4） | `vt10` | **226 / 226 Passed、未説明の Skip なし** | — | — |

**PlayMode は 72 → 75（＋3）で、増分は新規テストちょうど。既存の退行は 0。**
EditMode は 1639 のまま（E08 は中身を足しただけ）。最終確認：`isPlaying == false`。

### 8. 必須テスト一覧の更新

P5-P01・P12・P13 を `implemented: true` にした。**実装済みは 16 / 54。**

### 9. 残件

- **P02・P03・P04 が未実装。** それぞれ前提が足りない。
  - P02：Area Scene に HUD（`CombatPlayHud` 相当）が無い。到着直後の報酬が HUD へ映ることを見る要求なので、HUD の配置が先。
  - P03：`AreaTransferSnapshot`（Actor 値を束ねる器）が無い。単位ごとの Snapshot は P5-03a で揃っているので、
    それを束ねて §6.2 手順 5 で採取・到着時に適用する経路を作れば書ける。犬丸も Area Scene へ載せる必要がある。
  - P04：開放出入口の Trigger と方向入力（0.15 秒連続）が無い。§6.1 の入口条件なので P5-04 の Interact と近い。
- `AreaInitializer.PlaceArrivals` は主人公を入口へ置くだけ。向き（`CardinalDirection`）の適用と
  犬丸の隊列配置は未実装。
- `AreaPendingArrival` は Scene をまたぐ static だが、持つのは ID 2 つと完了フラグだけに留めてある。
- `AreaTransitionService.LoadOverride` はテスト専用の差し替え口（P12 が不備を注入するため）。
  Validator でこれが null であることを検査するのは P5-09 の候補。

### 10. 次の一手

P5-03b の残り（P02・P03・P04）か、P5-04（Interact 単一選択・調査 Adapter・扉・レバー）。
P04 は出入口の入力を扱うので P5-04 と同時にやると重複が少ない。
P03 は `AreaTransferSnapshot` を作れば通るので、単独でも進められる。

---

## 記録 007：P5-03b-3（P02・P03・P04）— **P5-03b 完了**

作成日：2026-09-23　担当：Claude（claude-opus-5）　工程ゲート：**通過**

### 1. 到達点

**P5-03b の 6 本がすべて緑になった。** 仕様書 §4（状態の所有者と生存期間）・§5（起動）・§6（遷移）が
実 Scene で通しで動く。

| 要求 | 状態 |
|---|---|
| P5-P02 `FirstRewardAfterLoad_ReachesInjectedStateAndHud` | Passed |
| P5-P03 `AreaRoundTrip_PreservesProgressVitalsAndDownTimers` | Passed |
| P5-P04 `HeldExitInput_DoesNotBounceBackOrStartEncounter` | Passed |

### 2. 作ったもの

| ファイル | 種別 | 内容 |
|---|---|---|
| `Gameplay/Transfer/AreaTransferSnapshot.cs` | 新規 | Actor 値を束ねる不変 Snapshot（§4.4） |
| `Gameplay/Session/AreaActorTransferPort.cs` | 新規 | 止める→採る／復元／到着配置の窓口 |
| `Gameplay/Session/AreaExitGate.cs` | 新規 | 開放出入口（0.15 秒連続入力・持ち越し抑止） |
| `Gameplay/Companion/CompanionIds.cs` | 新規 | 犬丸の安定 ID |
| `Infrastructure/World/AreaExitGateDriver.cs` | 新規 | 出入口へ入力を配り遷移を要求する |
| `Gameplay/Session/AreaRoot.cs` | 改修 | 出入口の明示参照 |
| `Infrastructure/World/AreaTransitionService.cs` | 改修 | 採取と受け渡し（static を増やさない） |
| `Infrastructure/World/AreaInitializer.cs` | 改修 | 値の復元・到着配置・出入口の武装解除 |
| `Editor/Phase5/Phase5ExplorationBuilder.cs` | 改修 | 犬丸・HUD・転送窓口・出入口を配置 |
| `Tests/PlayMode/P5ExplorationPlayTests.cs` | 追記 | P02・P03・P04 |
| `Tests/EditMode/P5ContractTests.cs` | 追記 | E06 に Port の採取順の検査 |

### 3. 設計上の判断

**Snapshot は遷移サービスが持つ。** Scene をまたぐので置き場が要るが、`AreaPendingArrival` のような
static をもう 1 つ作らず、遷移の所有者である `AreaTransitionService` が保持して
到着側が 1 度だけ取り出す形にした。

**「止める」と「採る」を 1 か所へ。** §4.4 の落とし穴（中断で生じた CD が落ちる）は順序の問題なので、
`Capture()` が中断と採取を続けて行い、呼び出し側が順序を間違えられないようにした。

**到着配置は Port が持つ。** `AreaInitializer` から `Find*` を 2 つ消し、Builder が配置対象の
<b>Prefab の根</b>を明示参照で渡す形にした。`PlayerStateController` は子に居るので、
それを動かしても Rigidbody に引き戻される（§5 で踏んだ）。

**出入口は「入力の持ち越し」を止める。** 当初は Trigger の占有で判定したが、
入口と出入口が離れている配置では素通りする。§6.1 末尾が言うのは
「別 Scene の Trigger へ押しっぱなしを持ち込まない」＝<b>入力</b>の持ち越しなので、
到着後は出口方向の入力が一度切れるまで溜めを開始しない形へ直した。P18（再開入力を到着先へ流用しない）と同じ考え方。

### 4. テストが出した欠陥・設計ミス

**(a) 到着位置が入口へ置換されない。** `PlayerStateController` が Prefab の子にあり、
その transform を動かしても Rigidbody が古い位置へ引き戻していた。P03 が -8（既定入口）と 9.5（指定入口）の
差として検出した。
切り分けのため「どの入口へ到着したことになっているか」を先に見る Assert を足したところ、
入口の解決は正しく配置だけが誤っていると分かった。Port が Rigidbody ごと動かす形へ直した。

**(b) 出入口の武装条件が誤っていた**（§3 の 4 つ目）。P04 が 2 度の失敗で形を決めた。

**(c) P03 の前提が甘かった。** 犬丸の生存値だけ Down にして状態機を Follow のままにしていた。
実際には起きない組み合わせなので、`ForceHit` という本物の経路で前提を作る形へ直した
（検証対象は遷移であって、Down への入り方ではない）。

### 5. 欠陥注入で分かったこと（`CLAUDE.md` の手順）

| 注入した欠陥 | 実行 | 結果 |
|---|---|---|
| 到着時の入力持ち越し抑止を外す | `negG` | P04 が失敗。「到着直後は武装解除されている」 |
| `Port.Capture` の `CancelAttack` だけ外す | `negH` / `negH2` | **落ちなかった** |
| `Port.Capture` の停止処理を全部外す | `negH3` | E06 が失敗。「採取の前に攻撃が止まっている」 |

**E06 が Port の採取順を見ていなかったので、検査を足した**（`AssertTransferPortCancelsBeforeCapture`）。
Controller 単体で順序を確かめても、実際に採取するのは Port なので、Port が止める処理を飛ばせば
同じ欠陥がそのまま残る。

そのうえで分かったのが、**`CancelAttack` は実質二重**だということ。所有権を解放すると
`ICompanionActionParticipant` 経由で同じ呼び出しの中で攻撃が打ち切られるので、
`CancelAttack` を外しても CD は正しく乗る。両方残しているのは Arbiter 未配線の構成でも
採取順が守られるようにするためで、その関係をコードのコメントへ書いた
（片方を消すと、その構成で中断由来の CD が落ちる）。

### 6. 検証

| 実行 | id | 結果 | 終端 | 所要 |
|---|---|---|---|---|
| `compile-status`（force、復元後） | `cs47` | ok。**警告 0 件** | — | — |
| `run-op build-exploration-trial` | `bx7` | ok。Build Settings +0（冪等） | — | — |
| 絞込み `P5ExplorationPlayTests` | `p5pd` | 6 / 6 Passed、Skip 0 | `completed` | 10.7 秒 |
| EditMode 全件 | `regE9` | **1639 / 1639 Passed、Skip 0** | `completed` | 80.3 秒 |
| PlayMode 全件 | `regP9` | **78 / 78 Passed、Skip 0** | `completed` | 72.2 秒 |
| `validate-project-data` | `vd11` | すべて通過 | — | — |
| `verify-required-tests`（P4） | `vt11` | **226 / 226 Passed、未説明の Skip なし** | — | — |

**PlayMode は 75 → 78（＋3）で、増分は新規テストちょうど。既存の退行は 0。**
EditMode は 1639 のまま（E06 に検査を足しただけ）。最終確認：`isPlaying == false`。

### 7. 必須テスト一覧の更新

P5-P02・P03・P04 を `implemented: true` にした。**実装済みは 19 / 54。**

### 8. GPT 検査の推奨タイミング

**ここ（P5-03b 完了直後）を推奨する。** 理由は 3 つ。

1. 仕様書 §4〜§6 がここで全部通しで動く。残る P5-04〜08 はこの基盤の上に機能を載せるだけなので、
   設計の誤りはここで直すのが一番安い。
2. `AreaTransferSnapshot` が P03 で実 Scene を通ったばかりで、設計の要が検証済みの状態になった。
3. 記録 001 §4 の裁定 6 点を GPT はまだ見ていない。v1.2 を待たずに着手した分の突き合わせが要る。

**特に見てほしい点**（判断が割れうると自認しているもの）：

- `AreaTransitionService.LoadOverride` — テスト専用の差し替え口を本番クラスに置いた（P12 が不備を注入するため）
- `AreaPendingArrival` — Scene をまたぐ static。ID 2 つと完了フラグに絞ってはいる
- 遷移の再入を `AlreadyTransitioning` 優先にした（記録 006 §4-b）
- Loading ゲートの既定を「凍結していない」にした向き（`CompanionActivityProvider` と逆）
- 裁定 5（Away の gameplay 経路を作らない）
- `Port.Capture` の二重停止（§5）

次点は P5-07 完了後（§8 の Encounter まで動いた時点）。

### 9. 残件

- 到着時の向き（`CardinalDirection`）は主人公へ適用していない。犬丸は適用済み。
- 出入口は方向入力だけ。P5-04 の Interact 扉（B→A）へ置き換える予定で、いまは同じ経路を共有している。
- `AreaExitGateDriver` は `Update` で毎フレーム動く。Pause は Gameplay 時計のゲートが止める。
- HUD は `CombatPlayHud` の流用。戦闘固有の参照（Session・Wave・Outcome）は未配線のまま。

### 10. 次の一手

P5-04（Interact 単一選択、調査 Adapter、扉・レバー）。要求 E11〜E14・E30、P05 の探索部分、P10。
GPT 検査を挟むなら、その前に。

---

## 記録 008：GPT レビュー（4f84d07）の是正

作成日：2026-09-23　担当：Claude（claude-opus-5）　工程ゲート：**再通過**

GPT が `phase/5-world-exploration` の先端 `4f84d07` を静的レビューし、
「P5-03b の完了判定は保留、P5-04 の前に是正すべき」との指摘を受けた。
指摘 7 件＋追加 2 件をすべて**実コードで裏取りしてから**是正した。すべて実在の欠陥だった。

指定された順（復旧処理 → 状態復元 → Scene 配線・起動 → テストの不足補完）で作業した。

### 1. 是正した指摘

| 優先度 | 指摘 | 是正 |
|---|---|---|
| 高 | ロード開始失敗後も時計が凍結したまま | `NotifyFailed(id, oldSceneUsable)` へ分け、旧 Scene が生きている失敗では**時計を戻す** |
| 高 | 旧 Scene 破棄後の復旧ロードが未実装 | `RecoverToOrigin` を実装。Snapshot の `OriginAreaId/EntryId` を使って元 Area を **1 回だけ**再ロード |
| 高 | タイムアウト判定が実行側につながっていない | `TryConsumeRecovery` を足し、`NotifyBinding` が **TimedOut 時は受け付けない**。遅れて着いた目的地を活動させない |
| 高 | 中断した仲間の行動状態をそのまま持ち越し | `ResolveRestorableState` で採取時に §4.6 の復元表（Away / Down / Stagger / Follow）へ落とす |
| 高 | 生成 Scene で犬丸の活動・追従配線が不足 | Builder が `CompanionActivityContext`（`MarkAreaWithoutEncounter`）と追従の leader を配線 |
| 中 | 到着通知に世代・所有者の照合がない | `AreaPendingArrival.TryMarkCompleted(id, area, entry)` へ変更。一致しない完了は無視して数える |
| 中 | 統合起動 Scene がまだ目印だけ | `Phase5TrialLauncher` を実装し、Play で A の開始点へ進む |
| 追加 | Snapshot を適用前に消費し切る | `TryPeekPendingTransfer` へ変更。成功か復旧完了まで保持し、`ClearPendingTransfer` で明示的に捨てる |
| 追加 | 必須参照の欠落を成功扱い | `TryValidateReferences` を先に通し、**欠けていたら成功にしない** |

### 2. Claude の 5 点への回答を受けての変更

| 論点 | 対応 |
|---|---|
| `LoadOverride` | **形を変えた。** `IAreaSceneLoader` を注入する方式にし、**本番の読込も同じ口を通る**。テスト専用の別経路が無くなったので、開始失敗・遅延完了を 1 本の経路で検査できる |
| `AreaPendingArrival` | **世代照合と後始末を足した。** static の数ではなく照合の欠如が問題という指摘どおり。完了は「エリア・入口・世代がすべて一致」でのみ記録でき、不一致は `MismatchedCompletionCount` に数える |
| `AlreadyTransitioning` 優先 | 採用済み。実装に合わないコメント（「目的地→状態→排他」）を実装に合わせて直した |
| Loading ゲートの既定が非凍結 | 採用済み。P5 では活動 Context・AreaReady を別途必須にしたので、未配線で活動できる状態は残っていない |
| Away の gameplay 経路 | 採用済み。復元・非表示・非参加の契約検証は E29 に残してある |

`Port.Capture` の二重停止は「冪等で CD を延長しない限り許容」との判断。
実際に冪等で、問題は停止回数ではなく停止後の状態名だった（上表の 4 件目）ので、そちらを直した。

### 3. テストの不足補完（GPT 指摘 4 件）

| 抜け | 補完 |
|---|---|
| P12：通常操作の復帰が未確認 | 時計が戻ること、`AreaReady` が戻ること、**CD が実際に進むこと**、**Warp できること**を追加 |
| E10：実サービスの復旧ロードが未確認 | `TimedOutLoad_RecoversToOriginWithoutActivatingDestination`（PlayMode）を新設 |
| P03：到着後に時計が再開することが未確認 | 到着後に `TickVitals` で復帰時計が進むこと、活動 Context が差さっていることを追加 |
| P13：完了コールバック内からの再入ではない | 徳の変化通知の**購読者の中から**次の遷移を要求する形へ組み直した |

加えて、GPT 指摘 4 件目に対応する `TravelWhileCompanionActing_ArrivesAsFollow`（PlayMode）を新設した。

### 4. 欠陥注入で確かめたこと（`CLAUDE.md` の手順）

| 注入した欠陥 | 実行 | 結果 |
|---|---|---|
| 状態の正規化をやめ生の状態を運ぶ | `negI` | 新テストが失敗。「**仲間の配置状態 Chase を復元できませんでした**」— GPT の予測どおりの壊れ方 |
| 旧 Scene が生きている失敗でも時計を戻さない | `negJ` | P12 が失敗。「失敗後は Gameplay 時計が戻っている。Expected: False But was: True」 |

どちらも核心の Assert で落ちた。復元後、原本と `diff` 一致・警告 0 件を確認。

### 5. 実装中に踏んだこと

**復旧の認可が二重ガードで弾かれた。** タイムアウト経路で `TryConsumeRecovery()` が復旧を消費したあと、
`RecoverToOrigin` がさらに `TryBeginRecovery()` を呼んでいて、すでに消費済みのため false → 失敗終端になっていた。
**認可（1 回だけという判断）と実行を分け**、呼び出し側が認可してから実行する形へ直した。

**E10 のテストで偽 Loader を使ったら、復旧先も読み込まれなかった。** 読込そのものを偽物にすると
「復旧に失敗した」という別の経路を検査してしまう。**本物の読込を走らせて完了の報告だけを遅らせる**形へ直した。

**統合起動 Scene の起動役が Bootstrap 不在で Error を出していた。** 常駐を持たずにこの Scene を開くのは
正常な状態なので、`WarningOnce` へ下げた（PlayMode の後始末が Error でテストを落としていた）。

### 6. 検証

| 実行 | id | 結果 | 終端 | 所要 |
|---|---|---|---|---|
| `compile-status`（force、復元後） | `cs60` | ok。**警告 0 件** | — | — |
| `run-op build-exploration-trial` | `bx8` | ok。Build Settings +0（冪等） | — | — |
| 絞込み `P5ExplorationPlayTests` | `p5pk` | 8 / 8 Passed、Skip 0 | `completed` | 14.1 秒 |
| EditMode 全件 | `regE10` | **1639 / 1639 Passed、Skip 0** | `completed` | 44.8 秒 |
| PlayMode 全件 | `regP10` | **80 / 80 Passed、Skip 0** | `completed` | 74.5 秒 |
| `validate-project-data` | `vd12` | すべて通過 | — | — |
| `verify-required-tests`（P4） | `vt12` | **226 / 226 Passed、未説明の Skip なし** | — | — |

**PlayMode は 78 → 80（＋2）。** 増分は新設 2 本（E10 の実復旧・行動中の遷移）。
既存 6 本は assert を強めた分だけ内容が増えている。EditMode は 1639 のまま。
最終確認：`isPlaying == false`。

### 7. 必須テスト一覧

要求の実装済み数は **19 / 54** のまま（新設 2 本は E10・P03 の `supportingTests` として登録）。
GPT の「P5-03b の完了判定は保留」に対しては、**指摘 9 件すべての是正と全件回帰をもって完了とする**。

### 8. 残件

- 到着時の向き（`CardinalDirection`）は犬丸のみ適用。主人公は未適用。
- `Phase5TrialLauncher.LaunchConditions` は起動時だけの素通し条件。
  到着後は各エリアの条件源へ差し替わるが、この「素通し」自体は Validator の検査対象にしたい（P5-09 の候補）。
- 出入口は方向入力のみ。B→A の Interact 扉化は P5-04。

### 9. 次の一手

P5-04（Interact 単一選択、調査 Adapter、扉・レバー）。要求 E11〜E14・E30、P05 の探索部分、P10。

---

## 内容ハッシュ（記録 008 の対象ファイル）

本記録の最終編集より後に採取した（`CLAUDE.md`「ハッシュ表は記録を書き終えた最後に採り直す」）。
`P5_統合受入結果.md` 自身は対象外。記録 001〜007 で採った分は各記録の表を参照。

| ファイル | 内容 | sha256 |
|---|---|---|
| `P5RequiredTests.json` | P5 必須テスト一覧（19 / 54） | `110fb7c51b8659d18778ee71ef94a4119c6f395908053f2ccf4e7949605e81b2` |
| `Assets/_Project/Scripts/Gameplay/Session/IAreaSceneLoader.cs` | 新規：狭い Loader 契約 | `fe47818fdd3f795768310d34d09c3bd126300ebe31e8a0a9eb9190aae1951db1` |
| `Assets/_Project/Scripts/Gameplay/Session/AreaTransitionCoordinator.cs` | 失敗の種別・タイムアウト門・復旧の受け渡し | `12f2f0d60f6af10c7bdb52860bb755f403a43a9ed7350599800209a57da34b30` |
| `Assets/_Project/Scripts/Gameplay/Session/AreaActorTransferPort.cs` | 状態の正規化・参照検証 | `349de0557a40ce5e9f0e2db2f0cf16b34ae56898a776e9e3fcfac869b8d82145` |
| `Assets/_Project/Scripts/Infrastructure/World/AreaTransitionService.cs` | Loader 注入・復旧ロード・世代照合 | `2dc371b8e1474ed0cb410eff4e1ce733e33b4302a3dbec6a73791c7e4521e0d7` |
| `Assets/_Project/Scripts/Infrastructure/World/AreaInitializer.cs` | Snapshot の覗き見・同一性付き完了 | `fedf76a5697b05d57e5129c7a9575e7582e3818a067dbe8f84f5274e9024a0ac` |
| `Assets/_Project/Scripts/Infrastructure/World/Phase5TrialLauncher.cs` | 新規：統合起動から A へ | `c42fac918e75a7e098d5292cebe367cb6f31b435fe1502e32eae894111346679` |
| `Assets/_Project/Scripts/Editor/Phase5/Phase5ExplorationBuilder.cs` | 活動 Context・追従 leader・起動役 | `46486888cd1c521f6139d76caab80f5aac171bce8f21bb2f88eb4c8c2ee9eb6f` |
| `Assets/_Project/Tests/EditMode/P5ContractTests.cs` | E10 の復旧分岐・時計解凍 | `1a9cd4dae30ec9ec3b308eb36189b101c98e7667414527a37024ac721a4cb1a8` |
| `Assets/_Project/Tests/PlayMode/P5ExplorationPlayTests.cs` | P03/P12/P13 強化＋新設 2 本 | `15148c49438b35e1d22c7119d91abc89cccea65e05b426d61f0110bbe8522469` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaA.unity` | 再生成 | `9395bbff6e7d836640e92b8db404e4cabb4270793fc411dc3ae9a9c09d605a95` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaB.unity` | 再生成 | `4e5caf5694fa9a57b300f51d1e20921feb1ee6653b682bd897f3c59c95feec22` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_ExplorationTrial.unity` | 再生成（起動役を配置） | `a180f9a62c9b44d02c3560a25bd52eadcd20bbcf1ecbcb1dd496dfc144be9b84` |

レビュー対象は `4f84d07`。**記録 008 の是正は未コミット。**

---

## 記録 009：GPT レビュー第 2 回（dd577a3）の是正

作成日：2026-09-23　担当：Claude（claude-opus-5）　工程ゲート：**再通過**

記録 008 の是正後の先端 `dd577a3` を GPT が再レビューし、指摘 5 件を受けた。
指定された順（**①到着確定の一本化 → ②Bootstrap 完了との接続 → ③復旧失敗処理 → ④世代・再入テストの補完**）で作業した。
5 件とも実コードで裏取りし、すべて実在の欠陥だった。

### 1. ①到着確定の一本化（指摘 1）

**何が問題だったか。** 到着側（`AreaInitializer`）が自分で `AreaContext.MarkReady()` を呼んでいた。
監視がタイムアウトしたあとに遅れてロードが完了しても、到着側は「着いた」としか知らないので、
**そのまま操作可能になる**。§6.3 の「遅れてロード完了しても目的地を活動させない」が実装で守られていなかった。

**直し方。** 「準備できた」と「活動してよい」を分けた。

| 役 | できること |
|---|---|
| 到着側 `AreaInitializer` | `AreaContext.MarkPrepared()` と `AreaPendingArrival.TryMarkPrepared(...)` の**報告だけ** |
| 所有者 `AreaTransitionService` | 世代・対象・タイムアウトを確認したうえで `AreaContext.Activate()` と `GameMode` 切替 |

直開き（遷移を伴わない起動）だけは所有者が居ないので到着側が自分で許可する。
遷移していない＝捨てられた到着になりようがないので、ここに穴は無い。

併せて `PlayerMotor.FixedUpdate` も Gameplay 時計のゲートに載せた（仲間側だけ止めても主人公が動けてしまう）。

### 2. ②Bootstrap 完了との接続（指摘 2）

**何が問題だったか。** `BootstrapRoot` も Scene 側の初期化担当も `Start()` で動く。
どちらが先かは決まっていないのに、先に動いた側が「まだ居ない」で諦めていた。
テストが常に先に Bootstrap を完了させていたので、**順序の問題を一度も検査していなかった**。

**直し方。** `BootstrapRoot.BootstrapFinished`（成否の確定）を出し、`BootstrapWait` を新設した。

- すでに確定していれば**待たない**（正常系に固定の待ちを足さない。§6.3 末尾）。
- 確定していなければ、確定するまで待つ。上限（既定 10 秒）を超えたら失敗として返す。
- 固定フレーム待ちにはしない。「1 フレーム待てば大丈夫」は環境で崩れるし、失敗と遅延を区別できない。

`AreaInitializer` と `Phase5TrialLauncher` の双方をこれに載せた。

### 3. ③復旧失敗処理（指摘 3）

**何が問題だったか。** 3 つ重なっていた。

1. 復旧ロードの待ちが**無界**（`while (!recovery.IsDone) yield return null;`）。返ってこなければ暗転のまま永久に止まる。
2. 生きているロード操作の上に次のロードを重ねられる。§6.3「古い操作が終端するまで新たなロードを開始しない」に反する。
3. 終端失敗しても**理由も戻り道も無い**。§6.3「Error 表示に留め、既存 Launcher へ戻る操作を提示」が実装に無かった。

**直し方。**

| 追加 | 内容 |
|---|---|
| `RecoveryTimeoutSeconds` | 復旧ロードにも監視を置く。超えたら終端失敗へ落とす（待ち続けない） |
| `BindTimeoutSeconds` | 到着側の準備完了待ちの上限を名前のある設定にした（以前は埋め込みの `10f`） |
| `TryStartLoad` | **ロード開始の唯一の入口。**前の操作が `IsDone` でなければ始めない。断った回数を `DuplicateLoadBlockedCount` に数える |
| `FailTerminal(id, reason)` | 理由を `TerminalFailureReason` に残し、`TerminalFailed` で知らせる。Gameplay 時計は**止めたまま**（壊れた状態で世界を動かさない） |
| `CanReturnToLauncher` / `TryReturnToLauncher()` | 既存 Launcher へ戻る操作。**生きている操作が終端するまでは戻れない**（戻り操作も Scene ロードなので、重ねれば同じ事故になる）。戻るときに時計を解凍する |
| `Phase5TrialLauncher` | 戻り先として**自分の Scene のパスを名乗る**（外から差すのではなく、居場所を知っている側が言う） |

自動再試行はしない。戻るかどうかはプレイヤーが決める（§6.3「無限再試行しない」）。

### 4. ④世代・再入テストの補完（指摘 4・5）

| 抜け | 補完 |
|---|---|
| 指摘 4：到着トークンを報告時に読み直すと照合の意味が無い | `AreaInitializer.CaptureArrivalToken` を切り出し、EditMode で「**開始時に捕まえたトークンは、新しい世代を準備済みにできない**」を固定 |
| 指摘 5：P13 の再入が本物の完了経路ではない | `AreaTransitionService.ArrivalCompleted` を新設し、**その購読者の中から**次の遷移を要求する形へ組み直した |
| 指摘 1 のテスト要件 | `DelayedDestination_IsNeverActivatedBeforeRecovery`：復旧が終わるまでの**毎フレーム**、目的地が Ready でも移動可能でもないことを見る |
| 指摘 2 のテスト要件 | `DirectOpenAreaA/B_WaitsForLateBootstrap`、`TrialLauncher_EntersFirstAreaAfterLateBootstrap`：**常駐を立てないまま Scene を開き**、あとから立てる |
| 指摘 3 のテスト要件 | `RecoveryLoadThatNeverCompletes_FailsWithReasonAndOffersLauncherReturn`：復旧ロードが返ってこない状況を作り、理由・戻り道・重複ロード禁止まで見る |

**`ArrivalCompleted` は後始末をすべて終えてから出す。** 先に出すと、排他がまだ `Ready` なので
通知の中からの要求が「遷移中」で弾かれ、通ったとしても戻ってきた先の後始末が
新しい遷移の到着要求（`AreaPendingArrival`）を消してしまう。**順序がそのまま正しさになる**箇所。

### 5. 設計上の判断

**監視上限を「作る前に設定する」形をやめた。** `AreaTransitionCoordinator.TimeoutSeconds` を
可変にし、`AreaTransitionService` 側の設定を既存の調停役へ伝えるようにした。
「作る前に設定しないと効かない」形だと、設定と生成の順序が入れ替わったことに**誰も気づけないまま既定値で動く**。
実際にテストの並びでこれを踏んだ（下記 6）。

### 6. 途中で踏んだこと

**待ちを入れたら既存 PlayMode が 10 本落ちた。** `Start()` から `yield return BootstrapWait.Wait(...)` に
しただけで、初期化が 1〜2 フレーム遅れた。**すでに確定しているなら待たない**早道を入れて解消。
「待てるようにする」ことと「常に待つ」ことは別。

**前のテストの起動役が次のテストの遷移を勝手に始めていた。** 統合起動 Scene の起動役は
常駐を待つ Coroutine を持つので、後始末で消しておかないと、**次のテストが常駐を立てた瞬間に待ちが解けて**
そのテストが要求していない遷移が走る。全テストが「完了回数が 1 多い」形で落ちた。
後始末と `CreateBootstrap` の双方で起動役を消し、さらに
「まだ誰も遷移サービスへカタログを渡していない」を**前提として Assert** するようにした（次に起きたら一目で分かる）。

**縮めた監視上限を戻さずにテストを終えると、後始末の遷移が失敗する。** 戻り先の Scene には起動役が居り、
常駐が生きている以上そこから通常の遷移が始まる。検査が終わったら上限と Loader を戻してから終える。

### 7. 欠陥注入で確かめたこと（`CLAUDE.md` の手順）

| # | 注入した欠陥 | 実行 | 落ちたテストと Assert |
|---|---|---|---|
| D1 | 復旧ロードの監視を外して無界に待つ | `d1c` | `RecoveryLoadThatNeverCompletes…`「Expected log did not appear: Area transition failed terminally」 |
| D2 | 重複ロードの門を外す | `d2c` | 同上「**押しても始まらない。** Expected: False But was: True」 |
| D3 | 終端失敗の状態・理由を残さない | `d3c` | 同上「**Error 表示の状態に留まる。** Expected: True But was: False」 |
| D4 | 到着側が自分で `Activate()` する | `d4c` | `DelayedDestination…`「**遅れて着いた目的地を活動させない。** Expected: False But was: True」 |
| D5 | 完了通知を後始末の**前**へ移す | `d5f` | `CompletionCallback…`「**通知の中からでも受理される。理由=AlreadyTransitioning**」 |
| D6 | 到着報告の世代照合を外す | `d6c` | `ArrivalToken_CapturedAtStart…`「**古い世代の準備完了は効かない。** Expected: False But was: True」 |
| D7 | Bootstrap を待たずに初期化する | `d7c` | `DirectOpenAreaA_WaitsForLateBootstrap`「Area initialization failed: Session サービスが見つかりません」 |

7 件すべて、**核心の Assert** で落ちた。D5 は最初「通知を 1 つ増やす」形で注入してしまい
「通知は 1 回」で落ちた（それは別の性質の検査）ので、**移動させる**正しい注入に直してやり直した。
復元後、`compile-status`（force）で警告 0 件を確認。

### 8. 検証

| 実行 | id | 結果 | 終端 | 所要 |
|---|---|---|---|---|
| `compile-status`（force、復元後） | `cs81` | ok。**警告 0 件** | — | — |
| `run-op build-exploration-trial` | `b80` | ok。Build Settings +0（冪等） | — | — |
| 絞込み `P5ExplorationPlayTests` | `t77` | **13 / 13 Passed、Skip 0** | `completed` | 22.9 秒 |
| EditMode 全件 | `f81` | **1640 / 1640 Passed、Skip 0** | `completed` | 67.8 秒 |
| PlayMode 全件 | `f82` | **85 / 85 Passed、Skip 0** | `completed` | 84.5 秒 |
| `validate-project-data` | `v83` | すべて通過 | — | — |
| `verify-required-tests`（P4） | `v85` | **226 / 226 Passed、未説明の Skip なし** | — | — |
| P5 一覧の照合（実装済み分） | `f81`/`f82` の記録から | **29 / 29 Passed**（要求 19 件＋補助テスト） | — | — |

**EditMode 1639 → 1640（＋1）**、**PlayMode 80 → 85（＋5）。** 増分は新設 6 本。
P5 必須一覧に対する自動ゲートは P5-09（V01〜V05）の範囲なので、今回は実行記録から機械照合した。
最終確認：`isPlaying == false`。

### 9. 必須テスト一覧

要求の実装済み数は **19 / 54** のまま。新設 6 本は `supportingTests` として登録した。

| 要求 | 追加した補助テスト |
|---|---|
| P5-E09 | `ArrivalToken_CapturedAtStart_DoesNotMarkANewerGeneration`（EditMode） |
| P5-E10 | `DelayedDestination_IsNeverActivatedBeforeRecovery` |
| P5-P01 | `DirectOpenAreaA_WaitsForLateBootstrap`、`DirectOpenAreaB_WaitsForLateBootstrap`、`TrialLauncher_EntersFirstAreaAfterLateBootstrap` |
| P5-P12 | `RecoveryLoadThatNeverCompletes_FailsWithReasonAndOffersLauncherReturn` |

### 10. 残件

- **Error UI そのものは未実装。** `TerminalFailed` / `TerminalFailureReason` / `TryReturnToLauncher` という
  窓口までを用意した。表示は暗転・Error 表示をまとめて扱う工程（P5-06 以降）で載せる。
- **仲間の活動 Context が `AreaReady` を参照していない**（GPT の追記）。
  いまは遷移サービスが `CloseForTransition` / `Activate` で明示的に開け閉めしているので実害は出ていないが、
  条件源を 1 本化するかは P5-04 以降で判断する。
- 到着時の向き（`CardinalDirection`）は犬丸のみ適用。主人公は未適用。
- 出入口は方向入力のみ。B→A の Interact 扉化は P5-04。
- **Scene の再生成が毎回 fileID を振り直す。** 内容は同じでも差分が全面になる（A で 1579 行）。
  機能上の害は無い（E25 の補助テストが入口とカタログの一致を実際に読んで確認している）が、
  レビューで「何が変わったか」が読めない。P5-09 の V03（2 回生成で重複なし）で
  **生成の決定性**まで見るかは、そこで判断する。

### 11. 次の一手

P5-04（Interact 単一選択窓口、調査 Adapter、扉・レバー）。要求 E11〜E14・E30、P05 の探索部分、P10。

---

## 内容ハッシュ（記録 009 の対象ファイル）

本記録の最終編集より後に採取した（`CLAUDE.md`「ハッシュ表は記録を書き終えた最後に採り直す」）。
`P5_統合受入結果.md` 自身は対象外。

| ファイル | 内容 | sha256 |
|---|---|---|
| `P5RequiredTests.json` | P5 必須テスト一覧（19 / 54。補助テスト 6 本を追加） | `c3cd08111aae08d4c243580d87094a625fb10c6bfb0e48ec2c03df8471ecd227` |
| `Assets/_Project/Scripts/Gameplay/Session/AreaContext.cs` | 準備完了と活動許可を分離 | `431a1c105ce461f427d1bfb8ece3543acfc53d13f4acd67ff3dedd5e176eed3c` |
| `Assets/_Project/Scripts/Gameplay/Session/AreaTransitionCoordinator.cs` | 監視上限を可変に | `3a32eca69ba4484ebd090e8dd31537af46f3d28d3f828342b9c909018e80b864` |
| `Assets/_Project/Scripts/Gameplay/Player/PlayerMotor.cs` | 主人公も Gameplay 時計のゲートへ | `9c9aa88c6bbd75877218dd2fd6a44a23d89b195ac86a15c8179a0b48be721fbe` |
| `Assets/_Project/Scripts/Infrastructure/Bootstrap/BootstrapWait.cs` | 新規：起動の確定を待つ | `7758715349523fa05e349021f06f5b8030550b5e8c327e44a5e80095915048a8` |
| `Assets/_Project/Scripts/Infrastructure/Bootstrap/BootstrapRoot.cs` | 起動完了の確定と失敗理由 | `ab5a2ad2149bfabadc9de541852fbbe281760bc2838946794ee16d7c2ce39932` |
| `Assets/_Project/Scripts/Infrastructure/World/AreaTransitionService.cs` | 活動許可の所有・復旧監視・戻り道・重複ロード禁止 | `3075dd182db852283cb8f528ccee573efc071579e7275658e501d1bc2a40eb71` |
| `Assets/_Project/Scripts/Infrastructure/World/AreaInitializer.cs` | 報告だけに縮小・到着トークンの捕捉 | `5b9fcf65616538c440481737b1a57c6b0eef511e72b7c3527a35bf1b92c0aa29` |
| `Assets/_Project/Scripts/Infrastructure/World/Phase5TrialLauncher.cs` | 起動待ち・戻り先の自己申告 | `e7e0dddcaf2628f20312eabf110dd0b93b57a1fc39da26d59ec764dac78815d0` |
| `Assets/_Project/Tests/EditMode/P5ContractTests.cs` | 到着トークンの世代テスト | `55487c8249ef723389e400a9cbe0d466b3a3201b06640611a3cd1113e34ce096` |
| `Assets/_Project/Tests/PlayMode/P5ExplorationPlayTests.cs` | 新設 5 本＋P13 の組み直し | `07c10aa194a6abe0c5ef019c941775d2ad06465094394d0a8094aac861723227` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaA.unity` | 再生成 | `d9bec7fa69537b6c323c6bea5e9ec5ad06a4667ca196ca790bd9b73db90c466f` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaB.unity` | 再生成 | `855becdaf555c8f03060b6ac1aceefa0ca7c5ec539cf84376c72ab9647f46e00` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_ExplorationTrial.unity` | 再生成 | `34b851968502ff91beb863f420dd311129110191b9ee491a29d5d6ecec956696` |

レビュー対象は `dd577a3`。**記録 009 の是正は未コミット。**


---

## 記録 010：GPT レビュー第 3 回（8c7ec93）の是正

作成日：2026-09-23　担当：Claude（claude-opus-5）　工程ゲート：**再通過**

記録 009 の是正後の先端 `8c7ec93` を GPT が再レビューし、**完了判定を妨げる問題 3 件**を受けた。
指定された順（1 → 2 → 3）で作業し、加えて
「**API 追加をもって『理由と戻り操作を提示済み』とは扱わない**」という指摘に応えて仮の表示まで実装した。
3 件とも実コードで裏取りし、すべて実在の欠陥だった。

### 1. 指摘 1：終端失敗後の遅延到着が「直開き」扱いになる

**何が問題だったか。** `FailTerminal()` が `AreaPendingArrival.Clear()` を呼んでいた。
Unity の非同期ロードはキャンセルできないので、そのあとで Scene は着く。着いた側は
「到着トークンが無い＝直開き」と判断して `Activate()` と Exploration への変更を実行していた。
**失敗して止めたはずのエリアが、自分で動き出す。**

前回の復旧停止テストは 2 回目のロードを Scene を生成しない操作に差し替えていたので、
この経路をまったく通っていなかった（テストの作り方が経路を隠していた）。

**直し方。** 要求を<b>消さずに放棄として残す</b>。`AreaPendingArrival` を 3 状態にした。

| 状態 | 意味 | 直開きの自己許可 |
|---|---|---|
| `None` | 遷移が 1 つも走っていない | **許す** |
| `Pending` | 遷移中。許可を出すのは遷移サービス | 出さない（報告のみ） |
| `Abandoned` | 終端失敗で放棄した | **断る**（誰も許可を出さない） |

到着側は `SelfActivationAllowed`（`State == None`）を見る。
**「トークンが無い」を直開きの証拠にしない。** 断った回数は `BlockedSelfActivationCount` に数える。

### 2. 指摘 2：主人公の凍結処理が残留速度を消していない

**何が問題だったか。** `PlayerMotor.FixedUpdate()` は凍結中に `return` するだけで、
直前の `Rigidbody.linearVelocity` が残る。移動中に遷移を始めると、
**旧 Scene が生きている間ずっと滑り続ける。** 仲間の Motor は同じ条件で速度をゼロにしていたので、
主人公だけが滑る形になっていた。

**直し方。** 仲間と同じく XZ 速度をゼロにする。Y は物理値のまま残す
（重力・接地は Gameplay の時計ではなく物理の話で、ここで消すと浮く）。
反応（ヒットバック）の残り時間は従来どおり進めない（`Tick` まで到達しない）。

### 3. 指摘 3：Launcher への戻りを、ロード開始だけで成功扱いにしている

**何が問題だったか。** 2 つあった。

1. `TryReturnToLauncher()` が `TryStartLoad()` の成功＝「操作を発行できた」をもって、
   Error 状態の解除・Snapshot の破棄・時計の解凍・回数の加算をすべて行っていた。
   **開始に失敗した操作でも戻れた扱いになる。**
2. 戻り先が、開くと**自動で A へ進む**探索試遊の起動 Scene だった。
   「安全に戻る」はずの操作が、壊れた流れへ即座に押し戻していた。

**直し方。**

| 変更 | 内容 |
|---|---|
| `TryBeginReturnToLauncher()` へ改名 | 返り値は「戻り終えた」ではなく<b>「戻りを始めた」</b>。名前で取り違えを防ぐ |
| `ReturnToLauncherRoutine` | 完了を監視（`ReturnTimeoutSeconds`）。片付けは**完了してから** |
| 失敗時 | 理由を差し替えるだけで、**Error 表示も時計の凍結も維持**。`ReturnToLauncherFailedCount` に数える |
| `ReturnedToLauncherCount` | **戻り終えたときだけ**増える |
| 戻り先 | 既存の `SCN_System_Launcher` に統一（何も自動で始めない Scene）。起動役が自分を名乗るのをやめた |

`IsReturningToLauncher` は Coroutine のハンドルでは持たない。
即座に終わる Coroutine は `StartCoroutine` が返る前に走り切るので、ハンドルの代入と実際の進行がずれる（実際に踏んだ）。

### 4. 仮の Error 表示（受入範囲を閉じる）

§6.3 は「Error 表示に留め、既存 Launcher へ戻る操作を提示する」と言っている。
**API を生やしただけでは提示したことにならない**ので、`AreaTransitionFailureView` を常駐に置いた。

- **常駐に置く。** 失敗したときは Scene 側が壊れているのが普通で、Scene に置いた表示では出せない。
- **IMGUI で描く。** Canvas・Prefab・フォント資産を必要としないので、どの Scene でも・素材が無くても出る。
- 契約は `IsShowing` / `Message` / `TryPressReturn` の 3 つ。**正式版は P5-06 で置き換えるが、この 3 つは残す**
  （テストはボタンの座標ではなくここを見るので、見た目が変わっても検査が壊れない）。
- 生きているロードが終端するまで、ボタンは押せない状態にする。

### 5. テストの補完

| 新設 | 見ていること |
|---|---|
| `ArrivalAfterAbandonedTransition_DoesNotSelfActivate` | 復旧 Scene を**実際に遅延到着させて**、Ready・Exploration への復帰が起きないこと |
| `FrozenDuringTransition_PlayerDoesNotSlide` | 非ゼロ速度で遷移を開始し、12 回の物理更新を挟んでも位置が変わらないこと |
| `RecoveryLoadThatNeverCompletes…`（組み直し） | 仮の表示に理由が出ること、**戻りロードが失敗したら停止状態を維持**すること、再試行はプレイヤーの操作で行うこと、戻り先が既存 Launcher であること |

遅延到着を起こすために `DeferredRecoveryLoader` を足した。
2 回目（復旧）のロードは**パスを覚えるだけで読み始めず**、`ReleaseDeferred()` で本当に走らせる。
前回の「Scene を生成しない操作」では、この経路そのものが存在しなかった。

### 6. 欠陥注入で確かめたこと（`CLAUDE.md` の手順）

| # | 注入した欠陥 | 実行 | 落ちたテストと Assert |
|---|---|---|---|
| 1 | 「トークンが無い＝直開き」に戻す | `i1c` | `ArrivalAfterAbandoned…`「**トークンが無いことを直開きの証拠にしない**（自己許可を断っている）」 |
| 2 | 終端失敗で要求を消す（放棄として残さない） | `i2c` | 同上「**要求は消さずに放棄として残る。** Expected: Abandoned But was: None」 |
| 3 | 凍結中に速度を残す | `i3c` | `FrozenDuringTransition…`「**凍結中に XZ 速度を残さない（x）。** Expected: 0.0 But was: 6.0」 |
| 4 | 戻りロードの完了を見ずに成功扱いにする | `i4c` | `RecoveryLoadThatNeverCompletes…`「Expected log did not appear: Return to launcher failed」 |
| 5 | 生きているロードがあっても戻れることにする | `i5c` | 同上「**終端していない操作の上に重ねない。** Expected: False But was: True」 |
| 6 | Error 表示を出さない | `i6c` | 同上「**プレイヤーに Error が出ている。** Expected: True But was: False」 |

6 件すべて、**核心の Assert** で落ちた。復元後、`compile-status`（force）で警告 0 件を確認。

### 7. 検証

| 実行 | id | 結果 | 終端 | 所要 |
|---|---|---|---|---|
| `compile-status`（force、復元後） | `r3l` | ok。**警告 0 件** | — | — |
| `run-op build-exploration-trial` | `r3m` | ok（冪等） | — | — |
| 絞込み `P5ExplorationPlayTests` | `r3j` | **15 / 15 Passed、Skip 0** | `completed` | 26.6 秒 |
| EditMode 全件 | `g81` | **1640 / 1640 Passed、Skip 0** | `completed` | 46.0 秒 |
| PlayMode 全件 | `g82` | **87 / 87 Passed、Skip 0** | `completed` | 87.7 秒 |
| `validate-project-data` | `g83` | すべて通過 | — | — |
| `verify-required-tests`（P4） | `g84` | **226 / 226 Passed、未説明の Skip なし** | — | — |
| P5 一覧の照合（実装済み分） | `g81`/`g82` の記録から | **31 / 31 Passed** | — | — |

**PlayMode 85 → 87（＋2）。** EditMode は 1640 のまま。最終確認：`isPlaying == false`。

### 8. 必須テスト一覧

要求の実装済み数は **19 / 54** のまま。新設 2 本は `supportingTests` として登録した。

| 要求 | 追加した補助テスト |
|---|---|
| P5-E07 | `FrozenDuringTransition_PlayerDoesNotSlide` |
| P5-P12 | `ArrivalAfterAbandonedTransition_DoesNotSelfActivate` |

### 9. 残件

- **Error 表示は仮のもの（IMGUI）。** 正式版は P5-06（カメラ・画面まわり）で置き換える。
  そのとき `IsShowing` / `Message` / `TryPressReturn` の 3 つは残すこと（テストがここを見ている）。
- 仲間の活動 Context が `AreaReady` を参照していない（GPT 第 2 回の追記）。
  遷移サービスが明示的に開け閉めしているので実害は出ていないが、条件源の一本化は P5-04 以降で判断する。
- 到着時の向き（`CardinalDirection`）は犬丸のみ適用。主人公は未適用。
- 出入口は方向入力のみ。B→A の Interact 扉化は P5-04。
- Scene の再生成が毎回 fileID を振り直す（記録 009 の残件のまま）。

### 10. 次の一手

P5-04（Interact 単一選択窓口、調査 Adapter、扉・レバー）。要求 E11〜E14・E30、P05 の探索部分、P10。

---

## 内容ハッシュ（記録 010 の対象ファイル）

本記録の最終編集より後に採取した（`CLAUDE.md`「ハッシュ表は記録を書き終えた最後に採り直す」）。
`P5_統合受入結果.md` 自身は対象外。

| ファイル | 内容 | sha256 |
|---|---|---|
| `P5RequiredTests.json` | P5 必須テスト一覧（19 / 54。補助テスト 2 本を追加） | `772c0028b2896a1695f99bcf9606ec233607ff74356059ef4a08cb6c6ec87fe1` |
| `Assets/_Project/Scripts/Infrastructure/World/AreaTransitionService.cs` | 放棄状態・戻りの完了監視・戻り先の統一 | `2278cdb715076d95471e317e3a2cc4612ca4baaf8e72749760cef98dd1305c26` |
| `Assets/_Project/Scripts/Infrastructure/World/AreaInitializer.cs` | 直開きの自己許可を状態で判断 | `6dc2566c5c0a0b9d810eca336c717e08dc8eb5512af7547338afe2c259de8ff8` |
| `Assets/_Project/Scripts/Infrastructure/World/AreaTransitionFailureView.cs` | 新規：終端失敗の仮の表示と戻る操作 | `b876b3217e8fd15ff0271375c55903f06b8c6664e6cdd842829b309db6efda8b` |
| `Assets/_Project/Scripts/Infrastructure/World/Phase5TrialLauncher.cs` | 戻り先の自己申告をやめた | `9570364a8c11cb0fd940891d7f7ccf7ae6639f7dd53cdd1bbf15e5137073e535` |
| `Assets/_Project/Scripts/Infrastructure/Bootstrap/BootstrapRoot.cs` | 仮の表示を常駐へ組み込み | `52f7ea9d88ada0accc4795ffeb59a733906028c0582452eaf52f5f4aca63f902` |
| `Assets/_Project/Scripts/Gameplay/Player/PlayerMotor.cs` | 凍結中は XZ 速度をゼロに | `1e1c468b4f3f5ce165bd91f84e0ad21ab14bed39443a68d57177f73ec18bf7d3` |
| `Assets/_Project/Tests/PlayMode/P5ExplorationPlayTests.cs` | 新設 2 本＋戻り契約の組み直し | `2a0a5aea849fa287f71ab8a67f4a7a7eac3e2fdc4094bed3e37b46b036ec3362` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaA.unity` | 再生成 | `1aa95cb9bdd5af973653f6ae22eaf03ac28dd9729a94c9d35395bdd7c1092286` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaB.unity` | 再生成 | `7c5c5875d043af396c6a64fcdc401d71f6801a6c14ba7ad6630e5489b262c6d7` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_ExplorationTrial.unity` | 再生成 | `faf092810c2522f1af4f4b577030fd56875ae9c1c1b9292ffca84fa79081dea9` |

レビュー対象は `8c7ec93`。**記録 010 の是正は未コミット。**


---

## 記録 011：P5-04a（Interact 単一選択窓口・調査 Adapter・レバーと門の契約層）

作成日：2026-09-23　担当：Claude（claude-opus-5）　工程ゲート：**契約層のみ通過**

P5-04 のうち、**Scene に依存しない契約と純粋規則**をここで閉じた。
要求 E11・E12・E13・E14・E30 を実装済みにした（19 → **24 / 54**）。
Scene 配線と実入力経路（P10）、P05 の探索部分は P5-04b で続ける。

### 1. 作ったもの

| 型 | 層 | 役割 |
|---|---|---|
| `IAreaInteractable` / `AreaInteractableRegistry` | Gameplay | 扉・レバー・調査が<b>同じ形</b>で候補になるための契約と登録簿 |
| `AreaInteractionSelector` | Gameplay（純粋） | §7.1 の候補条件と順位。Scene も入力も触らない |
| `AreaInteractionController` | Gameplay | <b>単一選択窓口</b>。`Peek`（表示）と `TryInteract`（実行）が同じ規則で選ぶ |
| `AreaInteractInput` | Infrastructure | 押下エッジを<b>1 回だけ</b>消費して窓口へ渡す |
| `IExplicitInvestigationRequest` | Gameplay | 地点を指定してだけ依頼できる<b>狭い入口</b> |
| `InvestigationInteractable` | Gameplay | 調査地点 1 つを共通候補として見せる Adapter |
| `AreaFlagLever` / `AreaFlagDoor` | Gameplay | 1 レバー＝1 FlagId。門は Flag から復元される |

`InvestigationCoordinator` に<b>指定地点モード</b>（`ExplicitTargetOnly`）と `RequestAt` / `PeekAt` を足した。
`AreaRoot` は門の一覧を持ち、`AreaInitializer` が到着時に記録から開通を復元する。

### 2. 設計上の判断

**選択の規則を純粋クラスへ出した。** §7.1 の候補条件は距離・遮蔽の<b>境界</b>で検査したいので、
Scene も入力も要らない形にしてある。窓口は「集める・実行する」だけで、規則を持たない。

**P4 と P5 で選択規則が違うことを、そのまま受け入れた。** P4 は距離 → 前方 → ID、
P5 は距離 → ID（§7.1「向きの厳密な円錐条件は P5 で追加しない」）。
<b>前方順位と ID 順位が逆転する配置で答えが違う</b>ので、E30 はまさにそこを見ている。
P4 の規則と既存テストはそのまま維持した。

**遮蔽判定は調査の `IObstacleProbe` を再利用した。** 壁の越え方を 2 通り持つと、
「調査では壁越しなのに扉は押せる」が起きる。同じ判定を共有すれば、ずれようがない。

**門は「通行 → 見た目」の順で開ける。** §7.3 の「見た目だけ開いて通行不能な状態を受入にしない」は、
順序を決めれば構造的に守れる。逆順だと、失敗したときに一番たちの悪い状態が残る。

**レバーは内部 Flag を先に確定してから通知する。** 通知を先に出すと、購読者が見る世界がまだ閉じている。
この順序のおかげで、<b>通知の中から引き直されても変更は 1 回</b>で済む（`TryOpen` が 1 回しか通さない）。

### 3. 欠陥注入で確かめたこと（`CLAUDE.md` の手順）

| # | 注入した欠陥 | 実行 | 落ちたテストと Assert |
|---|---|---|---|
| A | 同距離の ID 順位を外す | `itA` | E11「**同距離は StableId の辞書順**（lever_a < lever_b）」と E30 の両方 |
| B | 遮蔽判定を無視する | `itB` | E12「壁越しは候補にしない」 |
| C | 対象固有の受付距離で伸ばせるようにする | `itC` | E12「**対象固有の受付距離が短ければ、そちらで弾く**」 |
| D | 押下エッジを消費しない | `itD` | E13「**押下は使い切られている**。Expected: False But was: True」 |
| E3 | 開通の単一確定ガードをすべて外す | `itE3` | E14「**開通は 1 回。** Expected: 1 But was: 2」 |
| F | 指定地点モードでも引数なし入口を通す | `itF` | E30「**誤呼出と分かる理由で拒否する**（NotWired 等に紛れさせない）」 |

**テストを 1 度直した。** 最初、レバーの早期 `IsOpen` チェックだけを外しても<b>何も落ちなかった</b>。
`AreaRuntimeState.TryOpen` が 1 回しか通さないので、早期チェックは冗長だったためで、
これは欠陥ではなく<b>二重の守り</b>と分かった（記録 008 の `Port.Capture` と同じ形。両方とも残す）。
そこで「すべてのガードを外す」注入に切り替えたところ、今度は **StackOverflow** で落ちた。
落ちてはいるが「変更が 2 回起きた」という核心を見ていないので、
テスト側の再入を<b>1 回だけ</b>に縛り直した。いまは `OpenedCount` が 2 になって落ちる。

### 4. 検証

| 実行 | id | 結果 | 所要 |
|---|---|---|---|
| `compile-status`（force） | `q1` | ok。**警告 0 件** | — |
| 絞込み `P5ContractTests` | `p4m` | **21 / 21 Passed、Skip 0** | 2.3 秒 |
| EditMode 全件 | `q2` | **1645 / 1645 Passed、Skip 0** | 70.0 秒 |
| PlayMode 全件 | `q3` | **87 / 87 Passed、Skip 0** | 89.2 秒 |
| `validate-project-data` | `q4` | すべて通過 | — |
| `verify-required-tests`（P4） | `q5` | **226 / 226 Passed、未説明の Skip なし** | — |
| P5 一覧の照合（実装済み分） | `q2`/`q3` の記録から | **36 / 36 Passed** | — |

**EditMode 1640 → 1645（＋5）。** PlayMode は 87 のまま（Scene 配線は P5-04b）。

### 5. 必須テスト一覧

実装済みは **24 / 54**（＋5：E11・E12・E13・E14・E30）。

### 6. 残件（P5-04b で続ける）

- **Scene 配線がまだ。** 生成される A の「門」「レバー」「調査」「調査(壁越し)」は<b>まだ目印</b>で、
  実物の `AreaFlagLever` / `AreaFlagDoor` / `InvestigationInteractable` に置き換える。
  `AreaInteractionController` と `AreaInteractInput` を AreaSystems へ載せ、
  `InvestigationCoordinator` を指定地点モードで構成し、P4 の `InvestigationInteractInput` は置かない。
- **P10（実 Action 経路）が未実装。** E とゲームパッド South で各 1 回、長押し・複数対象でも二重なし、
  Step を発動しない、を実入力で通す。
- **P05 の探索部分**は Encounter（P5-07）が要るので、P05 自体はまだ implemented にしない。
- Validator（モード・Adapter 配線・旧入力仲介の不在の検査）は P5-09 の V01／V02。

### 7. 次の一手

P5-04b：Scene 配線と P10。

---

## 内容ハッシュ（記録 011 の対象ファイル）

本記録の最終編集より後に採取した。`P5_統合受入結果.md` 自身は対象外。

| ファイル | 内容 | sha256 |
|---|---|---|
| `P5RequiredTests.json` | P5 必須テスト一覧（24 / 54） | `f08c949a8660f4c29829ca0ec823bc4e28b2a15bfc68b58d40847653cdad6162` |
| `Assets/_Project/Scripts/Gameplay/Interaction/AreaInteractionContracts.cs` | 新規：Interact 対象の契約と登録簿 | `b5d2a4ba90491f743bd58893e9a9d18a5c219eaed08831abc7d54c25148997fe` |
| `Assets/_Project/Scripts/Gameplay/Interaction/AreaInteractionSelector.cs` | 新規：候補条件と順位（純粋） | `d1eba0a7c2e4ba89ffddfea953f28e13da1ee353e136854dd7098ddf49c68caa` |
| `Assets/_Project/Scripts/Gameplay/Interaction/AreaInteractionController.cs` | 新規：単一選択窓口 | `3aabdd252012b9dbf503c75fc3451305f08402ad8bb7aa9282fbc0b6549917a8` |
| `Assets/_Project/Scripts/Gameplay/Interaction/InvestigationInteractable.cs` | 新規：調査地点の Adapter | `216a31fdebc99ab99b1dc4139a028f67a45a5c33e1a68cf187598b065a13574c` |
| `Assets/_Project/Scripts/Gameplay/Interaction/AreaFlagDoor.cs` | 新規：Flag で恒久開通する門 | `a93f3f39d766576eed40aa5d205ef8ed8ca5d18e29096f78fbf185ad59651e8f` |
| `Assets/_Project/Scripts/Gameplay/Interaction/AreaFlagLever.cs` | 新規：1 Flag を 1 回だけ確定するレバー | `6eb4a2de2e9ff466b1bac21b70d4e0a945385e9abb48ff102f3801c8c6f1df71` |
| `Assets/_Project/Scripts/Gameplay/Companion/Investigation/IExplicitInvestigationRequest.cs` | 新規：地点指定の狭い入口 | `d621234ae3214c2c4060062ed169796c1528cf3b6b3480d3dcb8a643da1f6a08` |
| `Assets/_Project/Scripts/Gameplay/Companion/Investigation/InvestigationCoordinator.cs` | 指定地点モードと RequestAt | `af4d4df871f51c6a04144b2417dd34805ad942f13ff7bdd79449b89a290d421d` |
| `Assets/_Project/Scripts/Gameplay/Companion/Investigation/InvestigationEvents.cs` | ImplicitSelectionDisabled を追加 | `5c85812bf92d73a6e83c610c2bb3a4f6d97434ef9a761486925e9980a25b88c4` |
| `Assets/_Project/Scripts/Gameplay/Session/AreaRoot.cs` | 門の一覧を持つ | `d95cfc4323f77a5a7032337eeef503fc78ab61de1a2051d505154e48e57a8eaf` |
| `Assets/_Project/Scripts/Infrastructure/Input/AreaInteractInput.cs` | 新規：P5 の Interact 入力仲介 | `0f2b532e075c0ef16c642571580c238558a041f9d97c2213bcc5f18cf4964e4f` |
| `Assets/_Project/Scripts/Infrastructure/World/AreaInitializer.cs` | 到着時に門の開通を復元 | `ed2e59fdad0863d702a0fad905b1ba76cf9f6f484591e9498c0037b38f86bfea` |
| `Assets/_Project/Tests/EditMode/P5ContractTests.cs` | E11・E12・E13・E14・E30 | `d1f7806a16466a05b1f88ffe0a57b285d933af67505737adaafeb509749a87a1` |

**記録 011 は未コミット。**


---

## 記録 012：P5-04b（Scene 配線と実 Action 経路）— **P5-04 完了**

作成日：2026-09-23　担当：Claude（claude-opus-5）　工程ゲート：**通過**

P5-04a で作った契約を、生成される Scene へ実際に配線した。要求 P10 を実装済みにし（24 → **25 / 54**）、
P05 の探索部分（調査地点の配置・レバー・門・扉からの移動）を通した。

### 1. Scene へ入ったもの

A の「門」「レバー」「調査」「調査(壁越し)」、B の「A へ（扉）」は<b>目印から実物になった</b>。

| 生成物 | 中身 |
|---|---|
| A：門 | `AreaFlagDoor`（通行止め Collider ＋ 閉じている見た目）。Flag は `flag_p5_a_gate` |
| A：レバー | `AreaFlagLever`。同じ Flag を差し、Interact の候補として登録される |
| A：調査 ×2 | `CompanionInvestigationPoint` ＋ `InvestigationInteractable`。片方は衝立の向こう側 |
| B：扉 | `AreaTransitionDoor`。Interact 1 回で A への遷移を要求する（§6.1 の 2 行目） |
| 各 Area | `AreaInteractionController` ＋ `AreaInteractInput`。<b>P4 の旧仲介は置かない</b> |
| 各 Area | `InvestigationCoordinator`（**指定地点モード**）＋ `CompanionRosterContext` |

### 2. 設計上の判断

**扉は遷移サービスを直接呼ばない。** Gameplay は Infrastructure を参照しないので、
`AreaTransitionDoor` は<b>要求を置いておく</b>だけにし、既存の `AreaExitGateDriver` が取りに来る。
開放出入口と同じ形にそろえたので、駆動の経路は 1 本のまま増えていない。

**扉の要求は溜めない。** 取りに来られる前に連打されても要求は 1 件のまま。
押下は使い切るが、遷移が 2 回積まれることはない。

**開放出入口と扉を同じ場所に併存させた。** B → A は方向入力でも Interact でも戻れる。
§6.1 は両方の入口を認めているので、どちらかに寄せる理由がない。

### 3. テスト

| 新設 | 見ていること |
|---|---|
| `KeyboardAndGamepadInteract_ActOnceWithoutStep`（**P10**） | E と South の<b>実 Action 経路</b>で各 1 回。長押しでも複数対象でも二重なし。Step を発動しない。P4 の旧仲介が P5 Scene に居ないことも見る |
| `ExplorationRoute_LeverOpensGateAndDoorTravelsBack` | 実入力でレバー → 門が開く → A→B→A → <b>作り直された A でも門が開いたまま</b>。戻りは B の扉を Interact で押す |

**P05（全行程）はまだ実装済みにしない。** Encounter（P5-07）が要るため。
ここで通したのは「調査地点の配置・門・移動」までの探索部分。

### 4. 欠陥注入で確かめたこと（`CLAUDE.md` の手順）

| # | 注入した欠陥 | 実行 | 落ちたテストと Assert |
|---|---|---|---|
| G | 仲介が毎フレーム動かない（`Update` を空に） | `ptG` | P10「**E の押しっぱなしでも実行は 1 回。** Expected: 1 But was: 0」 |
| H | 扉の要求を誰も取りに来ない | `ptH` | 探索経路「遷移が完了する。Expected: 2 But was: 1」 |
| I | 門の開通を復元しない | `ptI` | 探索経路「**記録から開通が復元されている。** Expected: True But was: False」 |

G は「テストが仲介を手で叩いているだけではないか」を潰すためのもの。
実 Scene の配線が生きていることを、この注入が証明している。

### 5. 実装中に踏んだこと

**テストの配置で、遠いはずの対象が近くなっていた。** 主人公をレバーの +X 側へ置いたのに、
「遠い方」も +X 方向へ並べたので、実際には主人公に近づいていた。
P10 が「選ばれた対象だけが 1 回実行される」で落ちて気づいた。
<b>主人公の位置から測って並べる</b>形へ直した（対象どうしの相対位置で考えると間違える）。

### 6. 検証

| 実行 | id | 結果 | 所要 |
|---|---|---|---|
| `compile-status`（force、復元後） | `s2` | ok。**警告 0 件** | — |
| `run-op build-exploration-trial` | `s3` | ok。Build Settings +0（冪等） | — |
| EditMode 全件 | `s4` | **1645 / 1645 Passed、Skip 0** | 46.6 秒 |
| PlayMode 全件 | `s5` | **89 / 89 Passed、Skip 0** | 104.1 秒 |
| `validate-project-data` | `s6` | すべて通過 | — |
| `verify-required-tests`（P4） | `s7` | **226 / 226 Passed、未説明の Skip なし** | — |
| P5 一覧の照合（実装済み分） | `s4`/`s5` の記録から | **38 / 38 Passed** | — |

**PlayMode 87 → 89（＋2）。** EditMode は 1645 のまま（契約層は P5-04a で足し済み）。

### 7. 必須テスト一覧

実装済みは **25 / 54**（＋1：P10）。`ExplorationRoute_…` は E14 の `supportingTests` へ登録した。

### 8. 残件

- **P05（全行程）は Encounter 待ち。** 調査 → 門 → 移動 までは通ったので、
  P5-07 で戦闘と復帰を足せば閉じられる。徳 22・再訪敵 0 の確認もそこで。
- **Validator が未実装。** 指定地点モード・Adapter 配線・旧入力仲介の不在の検査は P5-09（V01／V02）。
  いまは P10 が「旧仲介が居ないこと」を実 Scene で見ているだけ。
- **調査の完走は未検査。** 地点は置いたが、犬丸が実際に歩いて調べ終えるところは P5-05（経路）以降。
- 仲間の活動 Context が `AreaReady` を参照していない（GPT 第 2 回の追記）。
- 到着時の向きは犬丸のみ適用。主人公は未適用。
- Scene の再生成が毎回 fileID を振り直す。

### 9. 次の一手

P5-05（NavMesh 追従・安全な再配置）。要求 E22・E23、P06・P11。

---

## 内容ハッシュ（記録 012 の対象ファイル）

本記録の最終編集より後に採取した。`P5_統合受入結果.md` 自身は対象外。

| ファイル | 内容 | sha256 |
|---|---|---|
| `P5RequiredTests.json` | P5 必須テスト一覧（25 / 54） | `9f8861927b4dc150ad0a45c0d1390d061911f1a7aff44e5de9f1ccbe6dc2ec87` |
| `Assets/_Project/Scripts/Gameplay/Interaction/AreaTransitionDoor.cs` | 新規：Interact 1 回で遷移を要求する扉 | `970c3acaca938fb7c3520f0bcf76ea89adf1df11b8bc4cb1befce22405b97aca` |
| `Assets/_Project/Scripts/Gameplay/Session/AreaRoot.cs` | 門と扉の一覧を持つ | `ba5911e50e61ec70437adba1bfd1bccb2d76093705f0734aa7d17359aeef1e7a` |
| `Assets/_Project/Scripts/Infrastructure/World/AreaExitGateDriver.cs` | 扉の要求も取りに来る | `8d70a90d478accec554c12719cf2184bd505617d83f51d579c1d0c468666d48b` |
| `Assets/_Project/Scripts/Editor/Phase5/Phase5AreaIds.cs` | P5-04 の仕掛けの安定 ID | `ae77c4c816eb76e72f549c9cbeaa9ff7bf04206e83f1e34d198f39d5e8aaba9d` |
| `Assets/_Project/Scripts/Editor/Phase5/Phase5ExplorationBuilder.cs` | 仕掛けと窓口・調査の配線 | `a7e15af2b096a3ae4a30b1345f9c47d9a105ff91253b4255fc1ab4cd1a71258f` |
| `Assets/_Project/Tests/PlayMode/P5ExplorationPlayTests.cs` | P10 と探索経路 | `20e2ecc16ecec184a2097380afa2baed62a27a68be7c1a0e13a3eade7a36306c` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaA.unity` | 再生成（門・レバー・調査） | `2523d29367338f63bb8feb6a2ba7d70f96ffd2ae7b8cb4e84e78c23b58c71f90` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaB.unity` | 再生成（扉） | `4b8b84cad8ccd026199c84ac6ed286bdb46b990cf5c9241b1b72e9527a1b30f6` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_ExplorationTrial.unity` | 再生成 | `000e4b4a47df493ca58bb70fa3dcfe7257b6f62dc2242cd0f3cf46e0e9c72e52` |

**記録 012 は未コミット。**


---

## 記録 013：P5-05a（経路とワープの契約層）

作成日：2026-09-23　担当：Claude（claude-opus-5）　工程ゲート：**契約層のみ通過**

P5-05 のうち、**NavMesh に触らずに決まる規則**をここで閉じた。
要求 E22・E23 を実装済みにした（25 → **27 / 54**）。
NavMesh Adapter・Builder でのベイク・実 Scene の検証（P06・P11）は P5-05b で続ける。

### 1. 作ったもの

| 型 | 役割 |
|---|---|
| `IPathProvider` / `PathQueryResult` | 経路の<b>狭い問い合わせ契約</b>。Complete／Partial／Invalid／Pending を区別する |
| `CompanionPathFollowModel` | 再探索の条件・停滞・再試行上限。純粋 C# で時間は外部注入 |
| `SafeWarpSelector` / `IWarpCandidateProbe` | ワープ候補を固定順で検査し、危険なら使わない |

`CompanionFollowController` に `BindPathProvider` / `BindWarpProbe` を足し、
迂回中は角へ、通常は隊列位置へ移動要求を出す。<b>書き込む口は 1 本のまま</b>
（調停役 → Motor。NavMeshAgent は置かない）。

### 2. 設計上の判断

**Partial を成功にしない。** §10.1 の指示どおりだが、理由を書いておく。
Partial を成功にすると、閉じた門の手前まで歩いては止まり、
止まったことを停滞と数えてワープ資格へ進む、という筋の悪い流れになる。
「行けるところまで行く」は「行ける」ではない。

**停滞の判定は 1 か所に寄せた。** §10.1 の「二重に競合させない」を、
`CompanionFollowInput.PathFollowActive` で実装した。迂回中は角へ向かって歩くので
<b>隊列位置までの距離はしばらく縮まらない</b>。既存 FollowModel の停滞判定をそのまま走らせると、
正しく迂回しているだけなのにワープしてしまう。迂回中は既存判定を止め、
「経路でも届かない」という結論だけがワープ資格へ渡る。

**届かない間は「待つ」。** `Waiting` を設けた。再探索の余地が残っている間に
`Failed` を返すと、まだ試せるのにワープ資格へ落ちる。逆に直線へ倒すと壁へ突っ込む。
どちらでもない状態が要る。

**ワープ候補は近い順に並べ替えない。** §10.2 の「固定順」を素直に守る。
同じ状況なら毎回同じ場所へ出る方が、見ている側には理解しやすい。

**安全性を確かめられない構成ではワープしない。** 供給元が無ければ「分からない」であって
「安全」ではない。`probe == null` は拒否側へ倒した。

### 3. 実装中に踏んだこと

**再探索が停滞の計測を毎回リセットしていた。** 停滞を「次の角までの距離」で測っていたため、
0.5 秒ごとの再探索で基準が入れ替わり、3 秒の停滞を<b>一度も検出できなかった</b>。
E22 の停滞ケースが `Failed` に到達せず気づいた。
**目的地までの距離**で測るように直した（経路を引き直しても基準が変わらない）。

**上限に達したあとも探し続けていた。** `Failed` のあとも間隔ごとに問い合わせ、
再探索回数が上限を超えて増えた。上限到達後は<b>世界が変わるまで探さない</b>ようにし、
逆に門の開通（`NotifyWorldChanged`）では再探索の余地を戻すようにした
（世界が変わったなら、前の「届かなかった」は古い判断）。

### 4. 欠陥注入で確かめたこと（`CLAUDE.md` の手順）

| # | 注入した欠陥 | 実行 | 落ちたテストと Assert |
|---|---|---|---|
| J | Partial を成功扱いにする | `itJ` | E22「**Partial は成功扱いにしない。まだ待つ。** Expected: Waiting But was: MoveToCorner」 |
| K | 再探索の上限を無視する | `itK` | E22「上限まで探し直しても届かなければ失敗。Expected: Failed But was: Waiting」 |
| M | 経路追従中も既存の停滞判定を走らせる | `itM` | E22「**経路で迂回している間は停滞を積まない**（判定を 2 つ競わせない）」 |
| N | 主人公と同じ通行可能側かを見ない | `itN` | E23「**繋がっていない側へ先回りさせない**（§10.2 の 1 行目）」 |
| O | 候補が無くても最後の候補へ逃がす | `itO` | E23 同上（壁内へ押し込む実装が通らない） |

5 件すべて、核心の Assert で落ちた。復元後、`compile-status`（force）で警告 0 件を確認。

### 5. 検証

| 実行 | id | 結果 | 所要 |
|---|---|---|---|
| `compile-status`（force、復元後） | `o2` | ok。**警告 0 件** | — |
| EditMode 全件 | `o3` | **1647 / 1647 Passed、Skip 0** | 100.5 秒 |
| PlayMode 全件 | `o4` | **89 / 89 Passed、Skip 0** | 110.5 秒 |
| `validate-project-data` | `o5` | すべて通過 | — |
| `verify-required-tests`（P4） | `o6` | **226 / 226 Passed、未説明の Skip なし** | — |
| P5 一覧の照合（実装済み分） | `o3`/`o4` の記録から | **40 / 40 Passed** | — |

**EditMode 1645 → 1647（＋2）。** PlayMode は 89 のまま（実 Scene の検証は P5-05b）。

### 6. 必須テスト一覧

実装済みは **27 / 54**（＋2：E22・E23）。

### 7. 残件（P5-05b で続ける）

- **NavMesh Adapter が未実装。** `NavMesh.CalculatePath` を使う Infrastructure 側の実装と、
  `BindPathProvider` への注入がまだ。いまは供給元が無いので経路追従は動かない（設計どおり止まる）。
- **ベイクがまだ。** Builder で NavMeshSurface をベイク・保存し、閉門に通行状態を反映する（§10.2 末尾）。
- **P06・P11 が未実装。** 実 Collider での移動・Step・押し出し（P06）、
  L 字通路の迂回と門開通後の経路更新（P11）。
- ワープ候補の probe（床・塞がり・接続）の実装も Infrastructure 側。

### 8. 次の一手

P5-05b：NavMesh Adapter・ベイク・P06／P11。

---

## 内容ハッシュ（記録 013 の対象ファイル）

本記録の最終編集より後に採取した。`P5_統合受入結果.md` 自身は対象外。

| ファイル | 内容 | sha256 |
|---|---|---|
| `P5RequiredTests.json` | P5 必須テスト一覧（27 / 54） | `7fa6dbdc2c57e5f1b5b0a1a6d6acaaa64d4237c0c7fb5f422e493dec63a45b3c` |
| `Assets/_Project/Scripts/Gameplay/Navigation/PathQuery.cs` | 新規：経路の狭い問い合わせ契約 | `04a12713b3119c6164fe3b791a551021bfea8a82524a6e2d7cec55f374ef3881` |
| `Assets/_Project/Scripts/Gameplay/Navigation/CompanionPathFollowModel.cs` | 新規：再探索・停滞・再試行上限 | `45c8dc500554551bbd46bd00d8a12be63d75b012410de83160347d8e4e2038a6` |
| `Assets/_Project/Scripts/Gameplay/Navigation/SafeWarpSelector.cs` | 新規：固定順の安全ワープ選択 | `9d39d47dfdb6fc7bf1a091e7003f9c18f45d5a2e5d13d8c6e1d9147201d443ce` |
| `Assets/_Project/Scripts/Gameplay/Companion/CompanionFollowController.cs` | 経路と安全ワープの組み込み | `69b73b3f1b025c9482fa591195559ea36e95e9d14c0ccf44de06cbc59875b6cb` |
| `Assets/_Project/Scripts/Gameplay/Companion/CompanionFollowModel.cs` | 迂回中は停滞を積まない | `ee3e31ff6bbb3f0b86505655d38ee76a8c9e118b128ea967fd70b2edf516926d` |
| `Assets/_Project/Scripts/Gameplay/Companion/CompanionFollowSettings.cs` | PathFollowActive を足した | `5a9403d4402fa8836cd8d6e38d9471d253d69b927238451f729010646252eaef` |
| `Assets/_Project/Tests/EditMode/P5ContractTests.cs` | E22・E23 | `f3d96cac42a630e8dfc64eeb67d7208a37cea9a74267643de359e93b60455fa4` |

**記録 013 は未コミット。**


---

## 記録 014：P5-05b（NavMesh Adapter・ベイク・実 Scene の検証）— **P5-05 完了**

作成日：2026-09-23　担当：Claude（claude-opus-5）　工程ゲート：**通過**

P5-05a の契約へ NavMesh を差し込み、生成される Scene で実際に迂回・門・衝突を通した。
要求 P06・P11 を実装済みにした（27 → **29 / 54**）。

### 1. 作ったもの

| 型 | 層 | 役割 |
|---|---|---|
| `NavMeshPathProvider` | Infrastructure | `NavMesh.CalculatePath` で角の列を返す。<b>Agent は置かない</b> |
| `NavMeshWarpCandidateProbe` | Infrastructure | 床か・塞がっていないか・主人公と繋がっているかを別々に答える |
| `AreaNavigationBinder` | Infrastructure | Adapter を追従へ注入し、門の開通を経路へ伝える |

Builder は各エリアで `NavMeshSurface` をベイクして Asset へ保存する。
門は `NavMeshModifier` でベイクから外し、閉じている間は `NavMeshObstacle` の<b>くり抜き</b>で塞ぐ。
開通時にくり抜きを外す（§10.2 末尾「門の通行状態変更を Navigation へ反映」）。

### 2. 設計上の判断

**門を本当に塞ぐ形にした。** P5-02 の門は通路の一部しか塞いでおらず、<b>横から回り込めた</b>。
塞がっていない門は門ではなく、「開通しないと進めない」という §7.3 の意味が消える。
東の通路（仕切り x=6 と外壁 x=12 の間）を 6.2m で塞ぐようにした。

**ベイクは Collider から行う。** 見た目（Renderer）から焼くと、ラベルや半透明の板まで
地形として拾う。通行できるかどうかを決めているのは Collider の方で、
「見た目と通行判定がずれない」という §3.3 の考え方とも揃う。Trigger は拾われない。

**主人公と犬丸はベイクから外した。** 動く物は地形ではない。外さないと、
生成時に立っていた場所に穴の空いた NavMesh が焼き込まれる。

**開通は「くり抜きを外す」で表す。** 門を焼き込んでしまうと、開通しても NavMesh に穴が残る。
焼くのは変わらない地形だけにして、変わるものは動的に扱う。

### 3. 実装中に踏んだこと

**面を子に付けたら、何も焼けなかった。** `CollectObjects.Children` は<b>自分の子</b>を集めるので、
子オブジェクトに付けると空を焼く。NavMesh がどこにも生成されず、経路が全部 `Invalid` になった。
面はエリアの根に付ける。

**Rigidbody へ直接速度を書いても主人公は動かなかった。** Motor が毎 FixedUpdate に上書きするため。
さらに Motor は入力の供給点を<b>最初の 1 回だけ</b>覚えるので、Scene 起動後に提供点を差し替えても効かない。
P06 が見たいのは「物理が壁と境界を止めるか」なので、Motor が持つ入力そのものを差し替えた
（実デバイスから Action を通す経路は P10 が見ている）。

**固定フレーム待ちの実キー検査が、全件実行で落ちた。** 単体では通るのに、
全件では `RealKeyE`・`RealKeyEnter` など<b>P4 の既存テストまで</b>巻き込んで落ちた。
実デバイスの押下が Action を通って届くまでのフレーム数は編集器の負荷で変わる。
新設分を<b>効果が出るまで待つ</b>形へ直したところ、全件 91/91 が通った。
P4 側の実キー検査は固定フレーム待ちのままなので、同じ揺れを抱えている（下記の残件）。

### 4. 欠陥注入で確かめたこと（`CLAUDE.md` の手順）

| # | 注入した欠陥 | 実行 | 落ちたテストと Assert |
|---|---|---|---|
| Q | 経路の供給元を注入しない | `ptQ` | P11「**NavMesh の供給元が注入されている**（未注入だと長距離の迂回を行わない）」 |
| R | 開通を Navigation へ反映しない | `ptR` | P11「**くり抜きが外れる**（§10.2 末尾）」 |
| U | 門の開通を経路へ伝えない | `ptU` | P11「**開通が経路へ伝わる**（§10.1 の再探索条件）。Expected: 1 But was: 0」 |

3 件とも核心の Assert で落ちた。復元後、`compile-status`（force）で警告 0 件を確認。

### 5. 検証

| 実行 | id | 結果 | 所要 |
|---|---|---|---|
| `compile-status`（force、復元後） | `x5` | ok。**警告 0 件** | — |
| `run-op build-exploration-trial` | `w3` | ok。NavMesh を A／B で保存 | — |
| EditMode 全件 | `x7` | **1647 / 1647 Passed、Skip 0** | 84.6 秒 |
| PlayMode 全件 | `x6` | **91 / 91 Passed、Skip 0** | — |
| `validate-project-data` | `x8` | すべて通過 | — |
| `verify-required-tests`（P4） | `x9` | **226 / 226 Passed、未説明の Skip なし** | — |
| P5 一覧の照合（実装済み分） | `x7`/`x6` の記録から | **42 / 42 Passed** | — |

**PlayMode 89 → 91（＋2）。** EditMode は 1647 のまま。

### 6. 必須テスト一覧

実装済みは **29 / 54**（＋2：P06・P11）。

### 7. 残件

- **P4 の実キー検査が固定フレーム待ちのまま。** 全件実行の負荷で取りこぼす揺れを抱えている。
  今回は新設分を直して全件が通ったが、P4 側も「効果を待つ」形へ寄せた方がよい（別工程で扱う）。
- **仲間が実際に迂回して歩くところは未検査。** P11 は NavMesh への問い合わせと門の反映を見ている。
  歩き切るところまでは、経路と移動の両方が絡むので P5-09 の統合（P09）で見る方が確実。
- ワープ候補の probe は実装したが、<b>実 Scene で安全でない候補を断る場面</b>はまだ通していない
  （E23 で規則は固定済み）。Encounter 開始時の再配置（§10.2 末尾）と併せて P5-07 で扱う。
- 仲間の活動 Context が `AreaReady` を参照していない（GPT 第 2 回の追記）。
- 到着時の向きは犬丸のみ適用。主人公は未適用。
- Scene の再生成が毎回 fileID を振り直す。

### 8. 次の一手

P5-06（カメラと表示）。要求 E24、P07。

---

## 内容ハッシュ（記録 014 の対象ファイル）

本記録の最終編集より後に採取した。`P5_統合受入結果.md` 自身は対象外。

| ファイル | 内容 | sha256 |
|---|---|---|
| `P5RequiredTests.json` | P5 必須テスト一覧（29 / 54） | `ee56f5791871c9bdb64bebbc9c9bb43bea5b8b9f2b96abfc0bea21a3abaf6b20` |
| `Assets/_Project/Scripts/Infrastructure/Navigation/NavMeshPathProvider.cs` | 新規：NavMesh の経路供給元 | `ccef12d4efb643394774ccfe7a1d6bbb09d6f04de79b7cac085d09e420d340d1` |
| `Assets/_Project/Scripts/Infrastructure/Navigation/NavMeshWarpCandidateProbe.cs` | 新規：ワープ候補の安全性 | `b23aca2857b41b3b24de4883bf2d4e7c5b253418e43ef87ab9c1485fb724df8f` |
| `Assets/_Project/Scripts/Infrastructure/Navigation/AreaNavigationBinder.cs` | 新規：Adapter の注入と門の反映 | `25641cda47dea8e42b770a2cbeae9f283d338cac2ab96da5c267d9a34aa50f59` |
| `Assets/_Project/Scripts/Gameplay/Interaction/AreaFlagDoor.cs` | 開通をくり抜きへ反映 | `1a277e0e74ebe0378f94557c52b2970cdbd0db922144e3e0def1321aa6d1956c` |
| `Assets/_Project/Scripts/Gameplay/Session/AreaRoot.cs` | レバーの一覧 | `5f4855ef87fd0431c5009d55c6268e4eca24ade1f8298486e62a2c3505050489` |
| `Assets/_Project/Scripts/Editor/Phase5/Phase5ExplorationBuilder.cs` | ベイクと門の実寸 | `5e63c5b5f78b44e539e5a7e6645082d7598486ffc4e3034af34f9afcdaa444d0` |
| `Assets/_Project/Scripts/Editor/Momotaro.Editor.asmdef` | Unity.AI.Navigation 参照 | `612083102c7663bd5c0fd1617f0d8d6c74883dad07c9c36eedd6ae1c639e0bb8` |
| `Assets/_Project/Tests/PlayMode/P5ExplorationPlayTests.cs` | P06・P11 と待ちの見直し | `307c9f2be36a3bcca0b042b8cc2611d4e34a556b27084b95f8aa96e81f30a019` |
| `Assets/_Project/Data/Tests/Phase5/NavMesh_P5_A.asset` | 焼いた NavMesh（A） | `46c99eb84b768b13b1cb919d3139edd07675f2cd1557b3c52b8456992a15affd` |
| `Assets/_Project/Data/Tests/Phase5/NavMesh_P5_B.asset` | 焼いた NavMesh（B） | `2b31949fe1b91765f582b6ce26f3e2d6b409d6f74e557f567ea6751c7524e810` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaA.unity` | 再生成 | `863921357a0fbc909beadd1d67ddded2ac2cce5a14398859e673a0daf4d12283` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaB.unity` | 再生成 | `02f2c7b284b9bd0cbab59f5cf580895568f06732f6f270d7cc9c7b53cbd195cd` |

**記録 014 は未コミット。**


---

## 記録 015：P5-06a（カメラ範囲の契約層）

作成日：2026-09-23　担当：Claude（claude-opus-5）　工程ゲート：**契約層のみ通過**

P5-06 のうち、Scene も Camera も要らずに決まる規則をここで閉じた。
要求 E24 を実装済みにした（29 → **30 / 54**）。
Scene 配線（Rig と揺れの書込み先の分離、Camera・Listener・HUD 各 1 つ）と P07 は P5-06b で続ける。

### 1. 作ったもの

| 型 | 役割 |
|---|---|
| `CameraRegionDefinition` / `CameraRegionSelector` | XZ 軸平行矩形の領域と、重なりの決着（Priority 降順 → RegionId 辞書順） |
| `CameraBoundsMath` | 床で見える範囲と、領域へ収める基準位置 |
| `CameraFocusBlend` | 部屋切替の 0.15 秒補間。途中も収め直す。到着・死亡再開は即時 |

### 2. 設計上の判断

**Z 幅を orthographicSize と同じにしない。** §11 の要点はここ。真上から見ているなら
画面の縦は床の奥行きとそのまま対応するが、俯角が付くと床は <c>1 / sin(俯角)</c> だけ伸びる。
45 度で約 1.41 倍、30 度で 2 倍。取り違えると部屋の端で床の外（仮背景）が見える。
横は床と平行なので伸びず、画面比で変わるのは横だけ。

**入りきらない軸は中央固定。** 無理に clamp すると min が max を超え、
どちらを採るかで画面が跳ねる。§11 の「無理な min/max clamp や自動ズームをしない」はこの形。

**補間の途中も収め直す。** 始点と終点だけ見て線形に動かすと、領域をまたぐ瞬間に
<b>どちらの領域にも収まっていない位置</b>を通る。毎フレーム収め直すのが唯一の守り方。

**即時配置でも範囲は守る。** 補間を飛ばすことと、範囲外に置いてよいことは別。
素通しにすると到着の 1 フレームだけ床の外が見える（テストで気づいて `SnapTo` を収める形へ直した）。

**領域は回転させない。** 斜めの部屋は矩形の組み合わせで表す。回転を許すと
所属判定も clamp も一気に面倒になり、見た目の得より保守の損が大きい。

### 3. 欠陥注入で確かめたこと（`CLAUDE.md` の手順）

| # | 注入した欠陥 | 実行 | 落ちたテストと Assert |
|---|---|---|---|
| V | 俯角で奥行きが伸びない | `itV` | E24「**45 度では 1/sin45 倍に伸びる。** Expected: 14.14 But was: 10.0」 |
| W | 狭い部屋でも無理に clamp する | `itW` | E24「**横が入りきらないので中央固定**（min が max を超えて跳ねたりしない）」 |
| Y | 補間の途中を収め直さない | `cY3` | E24「**補間の途中でも新しい部屋の範囲内に居る**（はみ出さない）。x=33.3」 |

3 件とも核心の Assert で落ちた。

**注入の後始末を 2 度落とした。** 復元をテスト実行と同じコマンドに繋げていたため、
ツールのタイムアウトで<b>復元だけが実行されない</b>状態になり、次の注入が前の欠陥の上に乗った
（Y の結果が W の症状として出た）。注入と復元は別の呼び出しに分け、
復元後に必ず `grep` で戻ったことを確かめてから次へ進む形に改めた。

### 4. 検証

| 実行 | id | 結果 | 所要 |
|---|---|---|---|
| `compile-status`（force、復元後） | `d2` | ok。**警告 0 件** | — |
| EditMode 全件 | `d3` | **1648 / 1648 Passed、Skip 0** | — |
| PlayMode 全件 | `d4` | **91 / 91 Passed、Skip 0** | — |
| `validate-project-data` | `d5` | すべて通過 | — |
| `verify-required-tests`（P4） | `d6` | **226 / 226 Passed、未説明の Skip なし** | — |
| P5 一覧の照合（実装済み分） | `d3`/`d4` の記録から | **43 / 43 Passed** | — |

**EditMode 1647 → 1648（＋1）。** PlayMode は 91 のまま。

### 5. 必須テスト一覧

実装済みは **30 / 54**（＋1：E24）。

### 6. 残件（P5-06b で続ける）

- **Scene 配線がまだ。** 領域の配置、Rig 親に追従・境界／Camera 子に揺れ（§11）、
  Camera・AudioListener・入力・HUD が活動中各 1 つであること。
- **P07 が未実装。** Pause／Loading で経路・探索・戦闘を止め、旧速度・Hitbox を残さずに復帰する。
- 既存 `TopDownCameraFollow` は固定オフセットのままで、領域も補間も使っていない。

### 7. 次の一手

P5-06b：Scene 配線と P07。

---

## 内容ハッシュ（記録 015 の対象ファイル）

本記録の最終編集より後に採取した。`P5_統合受入結果.md` 自身は対象外。

| ファイル | 内容 | sha256 |
|---|---|---|
| `P5RequiredTests.json` | P5 必須テスト一覧（30 / 54） | `bbd616c19b2a5c8cfedba463924fd6017b8f84d3805ea46c3d997b7ab4a3c5c0` |
| `Assets/_Project/Scripts/Presentation/Cameras/CameraRegion.cs` | 新規：領域と重なりの決着 | `c64239ea3b0deda9fbc56ace1b27f1e725a62d4a444f4fccdfa42e872a4fd86e` |
| `Assets/_Project/Scripts/Presentation/Cameras/CameraBoundsMath.cs` | 新規：床で見える範囲と収め方 | `8daeff704806fea846fe6e31a4277a83f41f93236bb2c0c7edbd44679bb43577` |
| `Assets/_Project/Scripts/Presentation/Cameras/CameraFocusBlend.cs` | 新規：部屋切替の補間 | `5ef04f077e35050f661f048e2d3e71dc69414aebd7aaa66f742d5e4971b912df` |
| `Assets/_Project/Tests/EditMode/P5ContractTests.cs` | E24 | `72ebf48079aa8e843f297008e6a85fcee6dd4b30a10e6261dbe4ee9a83f49d54` |

**記録 015 は未コミット。**


---

## 記録 016：P5-06b（カメラの Scene 配線と Pause／Loading の停止）— **P5-06 完了**

作成日：2026-09-23　担当：Claude（claude-opus-5）　工程ゲート：**通過**

P5-06a で閉じた契約層を実 Scene へ配線し、要求 **P17** を実装した。
あわせて P5-05 から持ち越していた **P07**（Pause／Loading の停止と復帰）を閉じた。
実装済みは 30 → **32 / 54**。

### 1. 作ったもの

| 型・場所 | 役割 |
|---|---|
| `AreaCameraRegion`（新規） | Scene に置く領域。中心は自分の位置、大きさは Inspector。回転は見ない（§11） |
| `AreaCameraRig`（新規） | 追従と境界。**自分（Rig）の位置だけ**を書き、Camera は子として付いてくる |
| `AreaContext.Prepared`（追加） | 準備完了の通知。到着時の即時配置はこれを購読して行う |
| `Phase5ExplorationBuilder`（拡張） | Rig 親 → Camera 子（`AudioListener` ＋ `CameraShakePresenter`）の階層、A の領域 3 つ・B の領域 1 つ、Rig への注入 |
| `Phase5Layout`（拡張） | 俯角 55 度・Orthographic 5・高さ 14 と、領域の矩形。**暫定値**（§11「最終カメラサイズは P10b で決める」） |
| `P5ExplorationPlayTests`（拡張） | P17・P07 |

### 2. 設計上の判断

**書込み先を分けるために、階層で分ける。** §11 は「基準追従と CameraShake の書込み先を分ける。
例：Rig 親に追従・境界、Camera 子に揺れ」と決めている。そこで Rig を親の空 GameObject にし、
`AreaCameraRig` は `transform.position` だけを書く。揺れは既存の `CameraShakePresenter` が
Camera 子の `localPosition` を書く。同じ Transform を 2 人で書くと、揺れが追従に食われるか、
揺れの戻り位置がずれる。**どちらも「揺れ終わりに戻った」ようには見えるので、症状が出にくい。**

**カメラの局所位置を目分量の固定オフセットにしない。** 俯角と高さから
`(0, h, -h / tan(俯角))` で決める。固定値にすると俯角を変えたときに注視点がずれ、
`CameraBoundsMath` の計算と食い違う。

**到着の即時配置は通知で行う。** 初期化担当（`AreaInitializer`）は Infrastructure に居て、
Presentation を参照しない（asmdef で禁じてある）。かといって Infrastructure → Presentation の
参照を足すと層の向きが崩れ、カメラを持たない Scene（起動 Scene・テストの最小構成）で
「未配線だから失敗」と言い出す羽目になる。`AreaContext` に準備完了の通知を足し、
**聞いている者だけが反応する**形にした。活動許可（`Activate`）ではなく準備完了に紐づけたのは、
許可は遷移の所有者が後から出すもので、その間もカメラは到着位置を映していてほしいため。

**Orthographic を 8 → 5 にした。** 8 のままだと見える範囲（半分 14.2 × 9.8）がエリア A
（半分 12 × 9）より広く、**どの軸も中央固定**になって追従も境界も一度も働かない。
5 なら奥行き 6.1 < 9 で追従し、東の通路（半分 3）では横が入りきらず中央固定になる。
「収める」と「中央固定」の両方が実 Scene で通る大きさを選んだ。見た目の最終値は P10b。

**起動 Scene には Rig を置かない。** エリアではないので追従対象も領域も無く、
置けば毎回「未配線」を警告することになる。`CreateCommonSceneObjects(withCameraRig)` で分けた。

**Pause と Loading は止め方が違う**（`CompanionActivity`）。Pause は**凍結**で、
進行中の攻撃の段・調査の依頼を保ったまま時計だけ止める。Loading は遷移の受理で
**進行中の行動を同期的に打ち切って**から値を採る（§6.2 手順 4 → 5）。
P07 はこの違いをそのまま検査する形にした。同じ「止まった」で済ませると、どちらかが必ず壊れる。

### 3. 実装中に踏んだこと

**補間の「途中」は追従先と一致しない。** P17 の最初の版は、補間中のフレームでも
`ClampFocus(主人公)` と一致することを求めていて落ちた。§11 が要求しているのは
**有効範囲から出ないこと**であって、追従先に一致することではない。
判定を「範囲内に居る（入りきらない軸は中央固定）」へ直した。

**実フレームの間隔に 0.15 秒の補間を預けない。** 編集器の負荷で 1 フレームが 0.15 秒を
超えると「途中」が存在しなくなり、検査が素通りする。補間の途中を見る区間だけ
切替時間を 2 秒へ差し替え、時間は `Tick` で注入する形にした。

**到着した犬丸に「速度 0」を求めない。** 最初の版は到着直後の犬丸の速度が 0 であることを
求めて落ちた（5.0 ＝ 追従の全速）。犬丸は到着した瞬間から追従を始めてよく、
速度 0 を求めると「動かない犬丸」を通してしまう。見たいのは**旧 Scene の位置と行動を
引きずっていないこと**なので、主人公との距離で見る形へ直した。

**Pause は押しっぱなしの入力を落とす。** `PlayerInputState.SetActive(false)` が `Move` を
ゼロにするため、復帰しても押しっぱなしのままでは動き出さない（Action の再通知が無い）。
テストは押し直す形にした。**実装側の残件として挙げる**（§11 の範囲外だが、体験としては引っかかる）。

**調査の受付距離は 1.5m**（`SO_Investigation_Trial`）。地点 (-3, 0, 1) に対して
1.6m の位置から依頼して `NoPointInRange` で落ちた。

### 4. 欠陥注入で確かめたこと（`CLAUDE.md` の手順）

| # | 注入した欠陥 | 実行 | 落ちたテストと Assert |
|---|---|---|---|
| A | 追従が Camera 子の Transform を書く（書込み先を分けない） | `p506b-ta` | P17「**範囲の内側では主人公をそのまま追う**。Expected: 2.0 But was: -2.896」 |
| B | 補間の途中を収め直さない | `p506b-tc` | P17「補間の途中 0：x：**入りきらない軸は中央固定**。Expected: 9.0 But was: -2.51」 |
| C | 準備完了で即時配置せず補間のまま続ける | `p506b-tf` | P17「**到着で即時配置している**。Expected: ≥1 But was: 0」 |
| D | Loading で進行中の攻撃を打ち切らない（`CancelAttack` と `ResetArbitration` の両方） | `p506b-tg` | P07「**Loading へ Hitbox を持ち越さない**（段=Active）。Expected: False But was: True」 |

**C の落ち先を確かめ直した。** 最初は到着時の回数（`SnapCount`）で落ちたので、
「振る舞いの判定も効いているか」を別に確認した（`p506b-te`：到着の回数判定を一時的に緩めて再実行
→「**即時配置は補間を打ち切る**」で落ちる）。そのうえで、到着と準備完了の両方で
**振る舞い → 回数**の順に並べ替えた。新規 Scene では補間器が最初の `Tick` で必ず即時配置するため、
起動直後の振る舞いだけでは差が出ない。差が出るのは**生きている Scene で置き直す場面**
（死亡再開。§9、P5-08）なので、その場面を P17 の中で直接通してある。

A は書込み先を奪われた結果 Rig が一切動かなくなるため、揺れの判定まで到達せず
手前の追従判定で落ちる。**核心の判定であることに変わりはない**ので、そのままにした。

### 5. 検証

常時許可（オーナーは Unity Editor を触らない）に基づき、確認を取らずに実行した。

| 実行 | id | 結果 | 所要 |
|---|---|---|---|
| `run-op build-exploration-trial` | `p506b-b1` | ok。Scene 3 件・Data 3 件を再生成 | — |
| `compile-status`（force、復元後） | `p506b-cg` | ok。**警告 0 件** | — |
| EditMode 全件 | `p506b-em` | **1648 / 1648 Passed、Skip 0** | 55.5 秒 |
| PlayMode 全件 | `p506b-pm` | **93 / 93 Passed、Skip 0** | 114.8 秒 |
| `validate-project-data` | `p506b-v1` | すべて通過 | — |
| `verify-required-tests`（P4） | `p506b-v2` | **226 / 226 Passed、未説明の Skip なし** | — |
| P5 一覧の照合（実装済み分） | `p506b-em`／`p506b-pm` の記録から | **45 / 45 Passed**（実装済み 32 ＋ 補助 13） | — |

**PlayMode 91 → 93（＋2）。** EditMode は 1648 のまま。

### 6. 必須テスト一覧

実装済みは **32 / 54**（＋2：P07・P17）。P5-06 の要求（E24・P17）はこれで揃った。

### 7. 残件

- **Pause から復帰しても、押しっぱなしの移動が再開しない。** `SetActive(false)` が `Move` を
  ゼロにし、復帰時に Action が再通知しないため。押し直せば動く。体験の問題として別工程で扱う。
- **`TopDownCameraFollow` は固定オフセットのまま。** P5 のエリアは `AreaCameraRig` を使い、
  この部品は P1 の試遊 Scene だけが使っている。統合は P10b（カメラサイズ確定）と併せて。
- **カメラの大きさ・俯角は暫定値**（§11。最終は P10b）。
- 死亡再開の即時配置は、通知の経路だけ通してある（§9 の本体は P5-08）。
- P4 の実キー検査が固定フレーム待ちのまま（記録 014 から継続）。
- 仲間の活動 Context が `AreaReady` を参照していない（GPT 第 2 回の追記、継続）。
- 到着時の向きは犬丸のみ適用。主人公は未適用（継続）。
- Scene の再生成が毎回 fileID を振り直す（継続）。

### 8. 次の一手

P5-07（Encounter と探索復帰）。要求 E15〜E19、P05、P08。


## 内容ハッシュ（記録 016 の対象ファイル）

本記録の最終編集より後に採取した。`P5_統合受入結果.md` 自身は対象外。

| ファイル | 内容 | sha256 |
|---|---|---|
| `P5RequiredTests.json` | P5 必須テスト一覧（32 / 54） | `d7cd095f7d0b51d0b5c838b6a1f39adccba3738ba6c6b87bdadcb9982520a7ee` |
| `Assets/_Project/Scripts/Presentation/Cameras/AreaCameraRig.cs` | 新規：追従と境界（Rig の位置だけを書く） | `7a1df48fdc1548c9ed688a6756fdd532769e44869e1ab5897612e649afddad1c` |
| `Assets/_Project/Scripts/Presentation/Cameras/AreaCameraRegion.cs` | 新規：Scene に置く領域 | `f96cfcec9dc405c142a6db346c6f959034808471ff68b621f46d9a4376e1f76d` |
| `Assets/_Project/Scripts/Gameplay/Session/AreaContext.cs` | 準備完了の通知 | `8d0b58875f436d77ff463d4322757c63d762a4825e6f6f687601d438f0ad0293` |
| `Assets/_Project/Scripts/Editor/Phase5/Phase5ExplorationBuilder.cs` | Rig／Camera 子／領域の配線 | `909483658021fda48aa9e86a44335b30f0c8690c2d6bbe600c75455b96c92fd2` |
| `Assets/_Project/Scripts/Editor/Phase5/Phase5Layout.cs` | カメラの暫定値と領域の矩形 | `103d14e6d2b1844f85b08cf9fd1fc650760c03b2d7c4564ab92f6a3ffca75917` |
| `Assets/_Project/Scripts/Editor/Phase5/Phase5AreaIds.cs` | 領域の安定 ID | `54f030b998d5cc2e3245af8b25acfd44473d80f1b16b920e7ac8f325e85acc87` |
| `Assets/_Project/Tests/PlayMode/P5ExplorationPlayTests.cs` | P17・P07 | `2fe44f314024bb2888657223612732ff1578b6e13dbce28fe865f41188f24ac7` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaA.unity` | 再生成 | `834a08a0467001d35d33c3389b9a17b009f5790f97eda1512adc83f50305df0f` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaB.unity` | 再生成 | `9c3fed1789e06f2020fbf5e428e42712c8a8cd9b6a68366fea28fc02e0fb18bd` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_ExplorationTrial.unity` | 再生成 | `a3ef9686ea3932ccd2b1c9ed68877b85055505f49ac0cd76ddfd5e440f5ebe30` |

**記録 016 は未コミット。**

---

## 記録 017：P5-07a（Encounter の調停層）

作成日：2026-09-24　担当：Claude（claude-opus-5）　工程ゲート：**契約層のみ通過**

P5-07 のうち、Scene も実敵も要らずに決まる規則をここで閉じた。
要求 **E15〜E19** を実装済みにした（32 → **37 / 54**）。
Scene 配線（EncounterBinding・EnemyId→Prefab 表・Arena・Trigger）と **P05・P08** は P5-07b で続ける。

### 1. 作ったもの

| 型 | 役割 |
|---|---|
| `AreaEncounterState`（enum） | Dormant／Starting／Playing／Resolving／Cleared／Defeated／Failed（§8.2） |
| `AreaEncounterMachine` | 状態と<b>実行世代（RunId）</b>。世代が合わない要求は黙って捨てる |
| `EncounterStartRejection`／`EncounterStartDecision` | 拒否理由と結果 |
| `IAreaEncounterConditions` | 受付条件の正本（§8.2 手順 2）。Trigger 側に条件を書かない |
| `IEncounterInterruptSink` | 探索の同期撤収（手順 4） |
| `IArenaBoundary` | 境界の有効化・解除と安全配置（手順 5、§8.4 手順 5） |
| `EncounterPlan` | 開始時に固めた敵構成（§8.1 末尾の Snapshot） |
| `IEncounterSpawner` | 生成と<b>活動許可を分ける</b>（手順 7・8） |
| `IAreaEncounterActivitySource` | 仲間の活動 Context への供給（§8.4 末尾） |
| `AreaEncounterRunner` | 調停窓口。8 手順の開始、勝敗の確定、解放 |
| `CompanionActivityContext`（拡張） | 区画 Encounter を正本にできる配線 |
| `EncounterData`（拡張） | `SpawnPointId` を公開（Data は ID まで。Transform は Scene 側） |

### 2. 設計上の判断

**RunId を撤収より先に確定する**（§8.2 手順 3 → 4）。手順 4 で調査を撤収すると、
撤収の通知を受けた購読者が<b>その場で開始を要求し直す</b>ことがある。先に閉じていないと、
その再要求が新しい Starting を作り、先に走っていた実行の後始末が新しい実行を壊す。
欠陥注入 F で実際にそうなることを確かめた。

**「生成できた」と「予定数そろった」を別に見る**（手順 7 と 8）。生成側が
「作れなかったけれど成功」を返す形は実際にありうる（Prefab が 1 つだけ null、など）。
生成側の申告だけを信じると、1 体だけ出た状態で戦闘が始まり、その 1 体を倒した瞬間に勝利する。
予定数との一致を調停側で確かめる。

**勝敗は刻みの終わりに決める**（§8.3 末尾）。最後の敵の通知でその場で勝利を確定すると、
同じ刻みに届いた主人公の死亡通知を取りこぼして、相打ちが勝利になる。
候補を溜めて <c>Tick</c> で決め、そこで<b>主人公の生存を再確認</b>する
（通知フラグだけでは、通知が来ない形の死亡を拾えない）。

**仲間へは既存 Session の状態をそのまま渡さない**（§8.4 末尾）。渡すと勝利のあと
`CombatSessionController` が Victory のまま残り、仲間が「戦闘の終端＝入力不可」と読んで
永久に停止する。区画 Encounter は解放後に<b>活動中 Session なし（null）</b>を返す。
`CompanionActivityContext` は区画 Encounter が配線されていればそちらを正本にする。

**`WaveRunner` を書き換えて共用しない**（§8.1 末尾）。あれは P3.5／P4 の 4 Wave 用として維持し、
P5 は小さな調停が既存 `CombatSessionController` の敵登録・勝敗遷移を利用する。
敵 AI・命中処理は複製しない。

**失敗しても RunId は戻さない。** 戻すと、後から届いた旧世代の通知が新しい実行に一致してしまう。
再試行は次の世代で走る（Trigger 退出 → 再進入。§8.2）。

### 3. 欠陥注入で確かめたこと（`CLAUDE.md` の手順）

| # | 注入した欠陥 | 実行 | 落ちたテストと Assert |
|---|---|---|---|
| F | 手順 4（撤収）を手順 3（門を閉じる）より先に行う | `p507a-tf2` | E15「**開始する**。理由=AlreadyRunning」＝撤収からの再要求が先に開始し、本来の要求が閉め出された |
| G | 手順 8 の全数確認を行わない | `p507a-tg` | E16「**予定数に届かない生成は開始にしない**（手順 8 の全数確認）。Expected: False But was: True」 |
| H | 最後の敵の通知でその場で勝利を確定する | `p507a-th` | E18「**最後の敵の通知だけでは外へ確定しない**。Expected: Playing But was: Cleared」 |
| I | 刻みの終わりに主人公の生存を再確認しない | `p507a-ti` | E17「**全滅していても主人公が死んでいれば勝利にしない**。Expected: Defeated But was: Cleared」 |

**F は最初 StackOverflow で落ちた。** 門が閉じていない実装では再要求がまた撤収を呼ぶため、
テスト側の再要求が無制限に入れ子になっていた（P5-04 のレバーと同じ形）。
テストの再要求を 1 回に限り、核心の Assert で落ちるようにし直した。

**E19 は既存契約の回帰止め**として置いた。撃破報酬が勝利より先に確定する順序と、
GrantOnce が RewardId 単位である意味は P4-00 で決まっており、P5 で変えない。
新しいのは「初回完勝で 22」（§8.5）で、実 Prefab を通した確認は P05 で行う。

### 4. 検証

常時許可（オーナーは Unity Editor を触らない）に基づき、確認を取らずに実行した。

| 実行 | id | 結果 | 所要 |
|---|---|---|---|
| `compile-status`（force、復元後） | `p507a-c3` | ok。**警告 0 件** | — |
| E15〜E19 の絞込み | `p507a-t1` | **5 / 5 Passed** | 1.9 秒 |
| EditMode 全件 | `p507a-em` | **1653 / 1653 Passed、Skip 0** | 70.9 秒 |
| PlayMode 全件 | `p507a-pm` | **93 / 93 Passed、Skip 0** | 101.8 秒 |
| `validate-project-data` | `p507a-v1` | すべて通過 | — |
| `verify-required-tests`（P4） | `p507a-v2` | **226 / 226 Passed、未説明の Skip なし** | — |
| P5 一覧の照合（実装済み分） | `p507a-em`／`p507a-pm` の記録から | **50 / 50 Passed**（実装済み 37 ＋ 補助 13） | — |

**EditMode 1648 → 1653（＋5）。** PlayMode は 93 のまま。

### 5. 必須テスト一覧

実装済みは **37 / 54**（＋5：E15〜E19）。

### 6. 残件（P5-07b で続ける）

- **Scene 配線がまだ。** EncounterBinding（Arena・Trigger・復帰点・出現点）、
  EnemyId→Prefab 表、`AreaEncounterRunner` の実装差し（実 Spawner・実 Arena・実 Conditions）。
- **P05・P08 が未実装。** 実入力で調査→門→移動→実敵撃破→探索復帰（徳 22）、
  生成失敗と勝利後の残留物なし。
- §13.3 の静的検査（EnemyId→Prefab 表の重複・未解決・`EnemyData.Id` 一致、空の敵構成、
  Boss 指定、到着点の Collider 重なり）は P5-07b の Validator で足す。
- 仲間の安全な内部再配置（§8.2 手順 5）は `SafeWarpSelector`（P5-05）を実 Scene へ繋ぐところが残り。

### 7. 次の一手

P5-07b：Scene 配線と P05・P08。


## 内容ハッシュ（記録 017 の対象ファイル）

本記録の最終編集より後に採取した。`P5_統合受入結果.md` 自身は対象外。

| ファイル | 内容 | sha256 |
|---|---|---|
| `P5RequiredTests.json` | P5 必須テスト一覧（37 / 54） | `d3f9e710f570289a2ee129a85eee794aa75eda0c3152064c3535bdbf9414a177` |
| `Assets/_Project/Scripts/Gameplay/Encounter/AreaEncounterState.cs` | 新規：状態と実行世代 | `bf199dd97dc8ba6ba9ae6d6f783a2a6b927b2e966354a70cae93b34de5b3f855` |
| `Assets/_Project/Scripts/Gameplay/Encounter/EncounterContracts.cs` | 新規：受付条件・生成・境界・構成 Snapshot の契約 | `bfa99484b7482dd5342641a8b393e87f674d8cb320ffa09e8e1a7c6058c5d9f0` |
| `Assets/_Project/Scripts/Gameplay/Encounter/AreaEncounterRunner.cs` | 新規：開始 8 手順・勝敗確定・解放 | `a108cbf2af744c4871e4520b43edd24f7e017867e479b7136c1da1d298008f59` |
| `Assets/_Project/Scripts/Gameplay/Companion/CompanionActivityContext.cs` | 区画 Encounter を正本にする配線 | `17bca5d377f455033c1cac291292f20fc15191611ff4f9a3b77dd4b31f017256` |
| `Assets/_Project/Scripts/Data/Events/EncounterData.cs` | SpawnPointId の公開 | `25c208af05b1f63646a376e9ab288679806b5710e56a20f8b9efe35bbb46408a` |
| `Assets/_Project/Tests/EditMode/P5ContractTests.cs` | E15〜E19 | `6bf110a129dbeaebe2f77db1bc5ac626b1c6b5196a1ce2ef64ea8ad43533781c` |

**記録 017 は未コミット。**

---

## 記録 018：P5-07b（Encounter の Scene 配線と実戦）— **P5-07 完了**

作成日：2026-09-24　担当：Claude（claude-opus-5）　工程ゲート：**通過**

P5-07a の調停層を実 Scene へ配線し、要求 **P05・P08** を実装した。
実装済みは 37 → **39 / 54**。P5-07 の要求（E15〜E19・P05・P08）はこれで揃った。

### 1. 作ったもの

| 型・場所 | 役割 |
|---|---|
| `EnemyPrefabTable`（新規） | EnemyId → Prefab の明示表。キー重複・未解決・`EnemyArchetypeData.Id` 不一致を検査できる |
| `AreaEncounterSpawner`（新規） | 非活動の親の下へ全数生成 → 全数そろってから起こす（§8.2 手順 7・8） |
| `AreaArenaBoundary`（新規） | 封鎖 Collider の有効化・解除。内側の安全配置を確かめてから閉じる |
| `AreaEncounterTrigger`（新規） | 主人公本人の進入だけを見る。条件は持たない（§8.2 手順 1） |
| `AreaEncounterConditionsSource`（新規） | 受付条件を Scene の実体から読む |
| `EncounterInterruptRelay`（新規） | 既存 `InterruptAllForCombat` を呼ぶだけの薄い橋渡し |
| `CompanionFollowController.TryRelocateInside`（追加） | 指定範囲の内側へ安全ワープ規則で置き直す（§8.2 手順 5） |
| `AreaInitializer`（拡張） | Encounter へ Session の世界状態を渡し、クリア済みを復元する |
| `Phase5ExplorationBuilder`（拡張） | `SO_Encounter_P5_B_Road`、アリーナ境界 4 枚、開始 Trigger、B の調査地点、Encounter 一式の配線 |
| `NavMeshWarpCandidateProbe`（修正） | Trigger を塞がりとして数えない |
| `P5ExplorationPlayTests`（拡張） | P05・P08 |

### 2. 設計上の判断

**生成した敵を Area の階層へ吊るさない。** 主人公の攻撃判定は「対象の `transform.root` が
自分と同じなら自分自身」として除外する（`PlayerStateController.PollHitbox`）。
P5 の Area Scene は主人公も地形も `AreaRoot` の子なので、敵をその下へ置くと
<b>主人公と敵の root が同じになり、攻撃が全部「自分」として捨てられる</b>。
生成物は Scene 直下に作る。

**非活動の親の下へ作ってから起こす。** Unity は `Instantiate` した瞬間に `Awake`／`OnEnable` を
走らせるので、作ってから `SetActive(false)` では遅い（索敵レジストリへ登録され、1 フレーム動く）。
§8.2 末尾の「Spawn 中の敵が先に活動・死亡しない」を構造で守る。

**境界は確かめてから閉じる。** 先に封鎖してから配置を直すと、Collider に食い込んだ状態が
1 フレーム作られ、物理が主人公を外へ弾く。内側に居ること・犬丸を内側へ置けることを
確かめてから有効化し、どちらか欠ければ<b>有効化せずに</b>失敗を返す。

**Trigger は通行止めではない。** `NavMeshWarpCandidateProbe` は Trigger も塞がりとして数えていた。
このプロジェクトでは通行止め（壁・水の境界・閉じた門）は必ず実体のある Collider で、
Trigger なのは受付の方（出入口・戦闘開始）である。数えていると<b>出入口と遭遇点の周りが
丸ごと壁になり</b>、そこへ犬丸を一歩も置けない。P08 で踏んで直した。

**B に本物の調査地点を置いた**（目印から差し替え）。アリーナの外（x=-8）に置くことで、
「犬丸が調査している最中に主人公だけが Trigger へ入る」が作れる。
§8.3 の「調査未確定と戦闘開始 → 調査を中断して戦闘へ」を実機で通せる配置にした。

**interface のフィールドは serialize されない。** 受付条件・生成・境界・撤収を interface で
持つと、Builder が `Bind` した参照が保存されず、実行時に「未配線」になる。
具象参照を `[SerializeField]` で持ち、注入があればそちらを優先する形にした。

**戦闘復帰点をアリーナの内側へ移した**（(0,0,-8) → (0,0,-5)）。境界の上に置くと、
封鎖した瞬間に壁へめり込む。

### 3. 実装中に踏んだこと

**「判定は出ているのに HP が 1 も減らない」。** 判定区間は 750 フレーム出ていて、
重なりも 3 件取れているのに命中結果が 0 件だった。原因は上記の `transform.root` 一致。
テストの診断に<b>両者の root 名</b>を出すようにして初めて読めた。

**「一時境界を解放する」を旗だけで見ていた。** 欠陥注入 L（`Disable()` が意図だけ戻す）が
素通りしたので、<b>実際に有効な Collider の数</b>（`ActiveBlockerCount`）で見る形へ直した。
注入で初めて分かる種類の弱さだった。

**直前の一振りの硬直で遷移が断られる。** 勝利直後に移動すると §6.1 の「行動中は遷移しない」で
落ちる。人が操作するときと同じく、手が空くのを待ってから移動する形にした。

**犬丸は実戦で倒れることがある。** P08 の「再活動」を位置の変化だけで見ていたため、
犬丸が Down のまま動かず落ちた（単独実行では通り、P05 の後だけ落ちる揺れ）。
<b>行動できる状態へ戻るのを待ってから</b>追従を見る形へ直した。倒れること自体は正常。

### 4. 欠陥注入で確かめたこと（`CLAUDE.md` の手順）

| # | 注入した欠陥 | 実行 | 落ちたテストと Assert |
|---|---|---|---|
| K | 生成した敵を Area の階層へ吊るす | `p507b-tk` | P08「**実 Hitbox で敵を倒せていない**（… 主人公の root=AreaRoot 敵の root=AreaRoot）」 |
| L | 一時境界を解放しない（意図だけ戻す） | `p507b-tl` | P08「**封鎖 Collider も実際に無効へ戻る**。Expected: 0 But was: 4」 |
| M | 仲間へ既存 Session の状態をそのまま渡す | `p507b-tm` | P08「**犬丸は探索へ戻る**（戦闘中のまま固まらない）」 |
| N | この周期のクリアを記録しない | `p507b-tn` | P05「**この周期のクリアを記録する**」 |

### 5. 検証

常時許可（オーナーは Unity Editor を触らない）に基づき、確認を取らずに実行した。

| 実行 | id | 結果 | 所要 |
|---|---|---|---|
| `run-op build-exploration-trial` | `p507b-b3` | ok。Data 4 件・Scene 3 件を再生成 | — |
| `compile-status`（force、復元後） | `p507b-cr` | ok。**警告 0 件** | — |
| EditMode 全件 | `p507b-em` | **1653 / 1653 Passed、Skip 0** | 43.3 秒 |
| PlayMode 全件 | `p507b-pm` | **95 / 95 Passed、Skip 0** | 115.8 秒 |
| `validate-project-data` | `p507b-v1` | すべて通過 | — |
| `verify-required-tests`（P4） | `p507b-v2` | **226 / 226 Passed、未説明の Skip なし** | — |
| P5 一覧の照合（実装済み分） | `p507b-em`／`p507b-pm` の記録から | **52 / 52 Passed**（実装済み 39 ＋ 補助 13） | — |

**PlayMode 93 → 95（＋2）。** EditMode は 1653 のまま。

### 6. 必須テスト一覧

実装済みは **39 / 54**（＋2：P05・P08）。

### 7. 残件

- **§13.3 の静的検査は P5-09。** EnemyId→Prefab 表の重複・未解決・`EnemyData.Id` 一致、
  空の敵構成、Boss 指定、到着点の Collider 重なり、報酬合計 22 の検査は Validator 工程で足す
  （表の検査そのもの＝`EnemyPrefabTable.Validate` は実装済み。呼ぶ側がまだ）。
- **敵の死亡フェード表示は入れていない**（§8.4 末尾は「残す場合も」の条件付き）。
- **`P5ExplorationPlayTests` の Encounter 検査は 2 本とも実戦を回すため遅い**（合わせて約 47 秒）。
- **Trigger を塞がりとして数えない修正は P5-05 の Adapter に入れた。** E23 は Fake 経由なので
  影響しないが、実 Scene での安全ワープは P05／P08 が初めて通した経路になる。
- Pause から復帰しても押しっぱなしの移動が再開しない（記録 016 から継続）。
- `TopDownCameraFollow` は固定オフセットのまま（継続）。
- P4 の実キー検査が固定フレーム待ちのまま（継続）。
- 仲間の活動 Context が `AreaReady` を参照していない（継続）。
- 到着時の向きは犬丸のみ適用。主人公は未適用（継続）。
- Scene の再生成が毎回 fileID を振り直す（継続）。

### 8. 次の一手

**GPT の中間検査**（オーナーの指示）。そのあと P5-08（本編型死亡再開・New Game・試遊との分離。
要求 E20〜E21、P09・P15・P18）。


## 内容ハッシュ（記録 018 の対象ファイル）

本記録の最終編集より後に採取した。`P5_統合受入結果.md` 自身は対象外。

| ファイル | 内容 | sha256 |
|---|---|---|
| `P5RequiredTests.json` | P5 必須テスト一覧（39 / 54） | `7c86a2371c958171e75653f66bc3c3c8092f5b5fe3a427ade88263f2a46a21c6` |
| `Assets/_Project/Scripts/Gameplay/Encounter/EnemyPrefabTable.cs` | 新規：EnemyId→Prefab の明示表 | `d577c56231348e06f8d9a84891f4291f048f74c822b1cf22aff7799a28cf4774` |
| `Assets/_Project/Scripts/Gameplay/Encounter/AreaEncounterSpawner.cs` | 新規：非活動生成と活動許可の分離 | `7678ec5d4c60b46558fa707f29a18375d70af5d9dcd319b2c215f0e733737e8e` |
| `Assets/_Project/Scripts/Gameplay/Encounter/AreaArenaBoundary.cs` | 新規：アリーナ境界と内側の安全配置 | `77ff1282ed737e47ce06871884f00099b8c1d8ff54d27bf84438d0c4bc73ad45` |
| `Assets/_Project/Scripts/Gameplay/Encounter/AreaEncounterTrigger.cs` | 新規：主人公本人の進入だけを見る | `19c9376bcf6a344977848cbfec4e73bbef66f17828e0296dd3b16c4ff9b1f143` |
| `Assets/_Project/Scripts/Gameplay/Encounter/AreaEncounterConditionsSource.cs` | 新規：受付条件の実体 | `36a583551d446d4873d21b6c480a84b1e32e9c8b3bddf7ab1895fc145a094680` |
| `Assets/_Project/Scripts/Gameplay/Encounter/EncounterInterruptRelay.cs` | 新規：探索の同期撤収 | `f47037960e3c70ec69bbabeea6bac08916cc108a7002ae8246ad1c8c079247e1` |
| `Assets/_Project/Scripts/Gameplay/Encounter/AreaEncounterRunner.cs` | 具象参照の serialize と Session 注入 | `2aaa8aaa9aca23f6f41009bb2818b00c0db43b52f4fa6efafdfa56cc7af083a9` |
| `Assets/_Project/Scripts/Gameplay/Companion/CompanionFollowController.cs` | 範囲の内側への安全な置き直し | `9f1bac2becfeef7d551755a2f65e3cef45b95f10e6c787bb0a9907e8aec73c1e` |
| `Assets/_Project/Scripts/Infrastructure/Navigation/NavMeshWarpCandidateProbe.cs` | Trigger を塞がりとして数えない | `7af6f10f8be88980a8108b9b5685116b8fba14d3c42543f23e2302f866a9d738` |
| `Assets/_Project/Scripts/Infrastructure/World/AreaInitializer.cs` | Encounter への Session 注入と復元 | `4249734b9883173d7f0003aca551cae9a6a6076bb7be4bf5b620611a028e1bd9` |
| `Assets/_Project/Scripts/Editor/Phase5/Phase5ExplorationBuilder.cs` | Encounter Data・境界・Trigger・配線 | `a4d5a3727611f4b18e60ebcd42363408828bb464d1c5b2fd6f315c5b51073d6e` |
| `Assets/_Project/Tests/PlayMode/P5ExplorationPlayTests.cs` | P05・P08 | `8c5215e6f77ba14020ad24897f5f233a1686036d07fd2d53551ae515d1ee0ef9` |
| `Assets/_Project/Data/Tests/Phase5/SO_Encounter_P5_B_Road.asset` | B の通常 Encounter 定義 | `68e1e56a37228185daefd0842a34ca0a0275002e54f6f3b4ba2afbf8e724269b` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaB.unity` | 再生成（Encounter 一式） | `c6c5e09441e4fa64237fedc0992dd78555d03080d361596f1c55cd9516f139eb` |

**記録 018 は未コミット。**

---

## 記録 019：GPT 第 4 回レビューの修正（P5-07 の補修）

作成日：2026-09-24　担当：Claude（claude-opus-5）　工程ゲート：**通過**

GPT 第 4 回レビュー（対象 `501b073`）の 5 点と、追加を求められた補助テストを実装した。
必須要求の数は変わらず **39 / 54**。補助テストが 13 → 18 件に増えた。

### 1. 指摘 1【高】犬丸の内部再配置が通常 Follow の Warp 経路だった

**そのとおりだった。** `TryRelocateInside` は安全候補を選んだあと
`BeginFollowAction(CompanionState.Warp)` で状態遷移を要求し、移動所有権の調停へ Warp を投げ、
**受理も実位置も確かめずに true** を返していた。§10.2 末尾は
「Scene 遷移時および Encounter 開始時の再配置は、通常 Follow Warp とは別の配置処理」と分けている。

| 直したこと | 中身 |
|---|---|
| `CompanionMotor.PlaceAt`（新規） | 状態を見ない・変えない。調停を通らない。`WarpCount` にも数えない配置の口 |
| `TryRelocateInside` | 状態遷移を要求しない。`PlaceAt` で置き、**実位置が内側に収まったこと**を確かめてから成功を返す |

受入（`ArenaRelocation_*`、E16 の補助 2 件）で、境界外の **Down／Away**、同フレームに強制停止された
仲間を含めて、位置・HP・復帰残り・CD の保持と `IllegalTransitionCount == 0` を見る。
安全候補が無ければ配置せず、**動かさない**（境界外に Down の本体を置き去りにしない）。

### 2. 指摘 2【高】§8.3 の調停が実フレームの処理順まで保証していなかった

**2 つとも直した。**

**(a) 確定の実行段。** `Tick` を `Update` から呼んでいたため、自分より後に走る Actor の命中を
取りこぼしうる。確定を `ResolvePending()` として明示し、`LateUpdate` から呼ぶ形にした
（このフレームの命中・死亡が出そろってから決める）。

受入（`SameFramePlayerDeath_WinsOverClearInRealUpdateOrder`、E18 の補助）は
**実行順 -1000 の注入役**が最後の敵の撃破を実チャネルへ通知し、**実行順 1000 の注入役**が
同じフレームのあとで主人公の死亡を通知する。確定が `Update` の段にあると、この 2 つの<b>間</b>で
勝利が確定してしまう——それを落とす形にしてある。

**(b) Starting からの Interact。** 手順 3 で Starting を確定してから手順 6 で Combat へ変えるまで、
モードはまだ Exploration である。`AreaInteractionController` が `IAreaEncounterState` を読み、
`EncounterStarting` で閉じるようにした。E15 は**実 Interact 窓口**から再入する形へ書き直し、
「戦闘前は通る → 撤収の通知からは通らない → 解放後はまた通る」まで見る。

### 3. 指摘 3【中】近距離では壁があっても経路探索を使わなかった

**そのとおりだった。** `distance <= DirectDistance || StraightLineClear` は、薄い壁を挟んだ
2.5m 以内でも直線追従に倒す。§10.1 の 2 行目は「壁に遮られた追従は経路の Corner へ」であって、
距離による例外を置いていない。**遮蔽が無いことだけ**を Direct の条件にした。
`DirectDistance` は「直線が通るときの近距離」を見分ける診断（`LastDirectWasNear`）として残した。
E22 も「近ければ遮蔽ありでも Direct」を捨て、**近くても遮られていれば迂回する**を見る形へ直した。

### 4. 指摘 4【中】P5 の戦闘 Feedback が配線されていなかった

**そのとおりだった。** Builder に既存の `CombatFeedbackDispatcher`／`CombatFeedbackPresenter`／
`HitStopController`／`HitFlashPresenter` を組み、揺れは **Camera 子の既存 ShakePresenter** を使い回す
（§11 の書込み先の分離を崩さない）。

配信役は購読対象を周期（既定 1 秒）で探し直すので、湧いた直後の数発を取りこぼす。
`IEncounterSpawner.SpawnedActivated` を足し、`EncounterFeedbackBinder`（新規）が
活動許可の直後に `Rescan()` を呼ぶ。受入（`EncounterFeedback_IsConnectedBeforeTheFirstHit`、
P08 の補助）は**最初の命中のフレーム番号**と**最初の Feedback のフレーム番号**を比べる。

### 5. 指摘 5【中】「戦闘終了」が表示されていなかった

**そのとおりだった。** `AreaEncounterResultView`（新規・IMGUI の仮表示）を置き、
`AreaEncounterRunner.ResultAnnounced` を購読して実際に出す。時間（unscaled）で自動的に消える
（結果パネルや Enter 待ちで止めない）。P05 が `IsShowing`／`Message`／自動で消えるところまで見る。

### 6. 追加の補助テスト（Pause／Loading）

`PauseAndLoadingRejectNewOperationsAndStaleInput`（P07 の補助）：
見ているのは **Interact 入力の持ち越し抑止**であって、移動入力の保持挙動ではない。
Pause 中の Interact 押下は窓口まで通らない。**解除しただけでは実行されない**
（Pause 中に落ちた押下を解除の瞬間に復活させない）。押し直せば実行される。
受理済み Loading 中の古い Trigger 要求は通らず、敵も湧かず、境界も閉じない。

押しっぱなしの<b>移動</b>が Pause 解除後に再開しない件（記録 016 から継続、§11 の残件）は
別の話で、この補助テストでは扱っていない。

### 7. 実装中に踏んだこと（重要）

**入力の Action Map が PlayMode をまたいで据え置かれる。** 入力は project-wide の
`InputActionAsset`（`InputSystem.actions`）を直接 Enable／Disable する。これは<b>プロジェクトの Asset</b>
なので、Action Map の有効・無効が PlayMode を抜けても残る。Loading・Paused・GameOver のまま
常駐を壊すと Gameplay マップが無効のまま据え置かれ、**以降に走る実キーのテストが一斉に無反応になる**
——このクラスだけでなく P4 の試遊 Scene も含めて。

実際に踏んだ：全件実行が 123 秒／1 失敗から **211 秒／10 失敗**へ変わり、
「主人公が攻撃に入らない」「E が届かない」が P4 のテストから順に出た。
コードは同じで、直前に走らせた単発テスト（主人公を死亡させて終わるもの）が残した状態が原因だった。
`TearDown` で<b>常駐を壊す前に探索モードへ戻す</b>ようにして解消（126.5 秒／0 失敗）。
記録 014 から挙げていた「P4 の実キー検査が固定フレーム待ち」とは別の、**環境側の据え置き**だった。

**欠陥注入 P は最初カラ振りした。** 実 Update 順を作るのに「生存数が 0 になったら」を条件にしていたため、
確定が早すぎる実装では先に登録が解放され、**前提の Assert で落ちて**核心が読めなかった。
注入役の条件を「撃破を通知したか」だけに変え、核心（死亡が優先する）で落ちるようにし直した。

### 8. 欠陥注入で確かめたこと（`CLAUDE.md` の手順）

| # | 注入した欠陥 | 実行 | 落ちたテストと Assert |
|---|---|---|---|
| O | 再配置を通常 Follow の Warp 経路へ戻す（受理も実位置も見ない） | `p5r4-to` | `ArenaRelocation_*`「**Down：不正遷移を出さない**（1）」「**実位置が内側に収まる**（-20 のまま）」 |
| P | 確定を `Update` の段で行う | `p5r4-tp3` | E18 補助「**同じフレームの死亡が勝利に優先する**。Expected: Defeated But was: Cleared」 |
| Q | Starting では Interact を閉じない | `p5r4-tqr` | E15「**撤収の通知からの Interact は実行されない**」 |
| R | 距離だけで遮蔽を無視する | `p5r4-tqr` | E22「**近くても遮られていれば迂回する**。Expected: MoveToCorner But was: Direct」 |
| S | 生成直後に購読し直さない（周期任せ） | `p5r4-ts` | P08 補助「**最初の命中の時点で購読済み**。最初の Feedback=99 最初の命中=29」 |
| T | 結果の短文を値として持つだけで出さない | `p5r4-tt` | P05「**実際に出ている**（§8.4 手順 8）」 |

### 9. 検証

常時許可（オーナーは Unity Editor を触らない）に基づき、確認を取らずに実行した。

| 実行 | id | 結果 | 所要 |
|---|---|---|---|
| `run-op build-exploration-trial` | `p5r4-b2` | ok。Data 4 件・Scene 3 件を再生成 | — |
| `compile-status`（force、復元後） | `p5r4-cn` | ok。**警告 0 件** | — |
| EditMode 全件 | `p5r4-em2` | **1655 / 1655 Passed、Skip 0** | 54.4 秒 |
| PlayMode 全件 | `p5r4-pm4` | **98 / 98 Passed、Skip 0** | 126.5 秒 |
| `validate-project-data` | `p5r4-v1` | すべて通過 | — |
| `verify-required-tests`（P4） | `p5r4-v2` | **226 / 226 Passed、未説明の Skip なし** | — |
| P5 一覧の照合（実装済み分） | `p5r4-em2`／`p5r4-pm4` の記録から | **57 / 57 Passed**（実装済み 39 ＋ 補助 18） | — |

**EditMode 1653 → 1655（＋2）。PlayMode 95 → 98（＋3）。**

### 10. GPT の 3 点への回答に対して

- **Pause／Loading の補助検査**：追加した（§6 のとおり）。
- **静的検査の呼び出し**：P5-09 へ持ち越す。「全検査ロジック実装済み」は言い過ぎでした。
  実装済みなのは `EnemyPrefabTable.Validate`（重複・未解決・`EnemyArchetypeData.Id` 一致）と、
  実行時の空構成・Boss 拒否だけです。**報酬合計 22 の静的検査、入口・復帰点の Collider 重なり、
  Arena 内部の安全配置可否、L 字通路と開通門の到達可否は未実装**で、P5-09 の Validator で作ります。
- **死亡フェード**：追加しません。

### 11. 残件

- §13.3 の静的検査一式は P5-09（上記のとおり、未実装の範囲を明記した）。
- 命中 SE（`CombatSePlayer`）は P5 の Scene に置いていない。手応えの本体（HitStop・点滅・揺れ）は
  繋いだが、音は P3.5 の素材表をそのまま持ち込むかを P10b で決める。
- Pause から復帰しても押しっぱなしの移動が再開しない（記録 016 から継続。§6 の補助テストは
  「解除しただけでは動かない」を<b>正しい挙動として</b>固定している）。
- `TopDownCameraFollow` は固定オフセットのまま（継続）。
- P4 の実キー検査が固定フレーム待ちのまま（継続。今回の据え置き問題とは別）。
- 仲間の活動 Context が `AreaReady` を参照していない（継続）。
- 到着時の向きは犬丸のみ適用。主人公は未適用（継続）。
- Scene の再生成が毎回 fileID を振り直す（継続）。

### 12. 次の一手

P5-08（本編型死亡再開・New Game・試遊との分離）。要求 E20〜E21、P09・P15・P18。


## 内容ハッシュ（記録 019 の対象ファイル）

本記録の最終編集より後に採取した。`P5_統合受入結果.md` 自身は対象外。

| ファイル | 内容 | sha256 |
|---|---|---|
| `P5RequiredTests.json` | P5 必須テスト一覧（39 / 54、補助 18） | `bdf3c7a038bb1efa284cdcf02fdc38820852559ec4c4916e2eec036c3606acf7` |
| `Assets/_Project/Scripts/Gameplay/Companion/CompanionMotor.cs` | 配置の専用口（PlaceAt） | `18c77d5c6ffbb79403be185dfb421dea32cfddde286242d1fe318390c1a3db1a` |
| `Assets/_Project/Scripts/Gameplay/Companion/CompanionFollowController.cs` | 再配置を配置処理へ（状態・所有権に触れない） | `c6d17e9fe989cb2914f5e68b1262c2a4e1cf94ef7c260cfaa82b43041959eeab` |
| `Assets/_Project/Scripts/Gameplay/Encounter/AreaEncounterRunner.cs` | 確定を LateUpdate の段へ／結果の通知 | `be602f97215b49f3722742c88c80884032940beae5a1608c24dedc12d335fc5d` |
| `Assets/_Project/Scripts/Gameplay/Encounter/EncounterContracts.cs` | 生成直後の Feedback 接続の通知 | `51957f0f0d992734b3c4407584cd3df66cc366816a9ed0800d6da445e5fef177` |
| `Assets/_Project/Scripts/Gameplay/Encounter/AreaEncounterSpawner.cs` | 活動許可の直後に通知 | `55798b24dbd85703a84b7db5f0df16350f875eea634f5fba74b1b6124154168a` |
| `Assets/_Project/Scripts/Gameplay/Interaction/AreaInteractionController.cs` | Starting から Interact を閉じる | `c65270d050f2472f8209ca20d8f3ace6b173e0f22bdd5ad441101de98af1dd0e` |
| `Assets/_Project/Scripts/Gameplay/Interaction/AreaInteractionContracts.cs` | EncounterStarting の理由 | `ded83b0c7ecfb5659ef426b7456a037c51b4dc3486df8def0979935d2b6b0f69` |
| `Assets/_Project/Scripts/Gameplay/Navigation/CompanionPathFollowModel.cs` | 距離で遮蔽を無視しない | `83b451fc3d9cce528c1b4110fdc7f4344540e6d071a05b20a9385e0a20d92c0d` |
| `Assets/_Project/Scripts/Presentation/Combat/EncounterFeedbackBinder.cs` | 新規：生成直後の購読し直し | `a5aff556e96a95cf41a17ded7fc4ac6e3420a96a0b9aad78f6dffc62a506b221` |
| `Assets/_Project/Scripts/Presentation/Hud/AreaEncounterResultView.cs` | 新規：結果の短文の仮表示 | `23e342c0a272f53eb9b742005e8803359ff853137ef4b2d00a99084dfec3ddb8` |
| `Assets/_Project/Scripts/Editor/Phase5/Phase5ExplorationBuilder.cs` | Feedback・結果表示・Interact の配線 | `f781dde49522a09ea9fed0ea186e2b040ad8e87f263b00e3e8a7edb908ec8701` |
| `Assets/_Project/Tests/EditMode/P5ContractTests.cs` | E15・E22 の書き直しと再配置の受入 | `4e35a1b45183c333daa49bf500d81dbdbf9df17205e78083f95ea5d0a091971a` |
| `Assets/_Project/Tests/PlayMode/P5ExplorationPlayTests.cs` | 実 Update 順・Feedback・結果表示・Pause／Loading | `9bff18a9efc1f64b6eb699a6fd42a93645e27d271f595c412281203a65b4bf1a` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaB.unity` | 再生成（Feedback・結果表示） | `99b12c22f56ba63e55357dabdbbc89202f17200d80fafa8b6614fdfcfaf6503f` |

記録 019 は `301448b`「P5-07 修正 GPT 第 4 回レビューの 5 点と補助テスト」として commit 済み
（記録本文の後続修正を含めた最終形は `97871f9`）。


## 記録 020：GPT 第 5 回レビューの追加修正（テスト補強と記録の訂正）

作成日：2026-09-24　担当：Claude（claude-opus-5）　工程ゲート：**通過**

GPT 第 5 回レビュー（対象 `97871f9`）は合格。あわせて出た 4 点の追加修正を行った。
本番コードは変更していない（**テストと記録のみ**）。必須要求は **39 / 54** のまま、補助テストも 18 件のまま。

### 1. 再配置テストの初期値を強めた

**指摘のとおりだった。** 再配置の受入（`ArenaRelocation_*`）は、HP・復帰残り・CD が
**初期値のまま**の仲間を境界外へ置いて配置していた。これでは「配置のついでに値を初期化してしまう」
不具合が起きても、初期値と初期値の比較になって通ってしまう。

`MakeRelocationRig` で、配置の前に意味のある非初期値を入れるようにした。

| 値 | 入れた値 | 根拠 |
|---|---|---|
| HP | Down は 0、それ以外は 23 | `CompanionVitals` の取り込みは Down ⟺ HP 0 を要求する |
| 復帰残り | Down は 1.75 秒、それ以外は 0 | 同じく、Down でなければ復帰待ちは 0 でなければ受け付けない |
| 被弾後無敵の残り | 0.4 秒 | データの 0.5 秒以内 |
| ひるみ蓄積 | 12 | 0 と区別が付く値 |
| 攻撃 CD の残り | 2.5 秒 | 途中値 |

最初はこの取り込み自体が拒否され（`前提：負傷・復帰待ちの値を入れられる。 Expected: True But was: False`）、
`CompanionVitals.TryImportTransferSnapshot` が **Down ⟺ HP 0**、**非 Down ⟹ 復帰残り 0**、
**残り時間 ≦ データ秒数** を課していることが分かった。状態に応じた値へ直して通した。

配置の前後で、これらがすべて保たれること（**ひるみ蓄積と Down の別も含む**）を見る。
Down の仲間を配置しても出撃扱いに変えない。

### 2. 最初の命中 Feedback を<b>同じ対象</b>で照合するようにした

**指摘のとおりだった。** `EncounterFeedback_IsConnectedBeforeTheFirstHit` は
「Feedback が 1 件以上届いた」「最初の Feedback のフレーム ≦ 最初の命中のフレーム」しか見ておらず、
**別の敵の別の攻撃**の Feedback をもって「最初の命中も拾えた」と読める余地があった。

敵側で観測した最初のダメージから **HitId・被弾対象・結果種別・フレーム**を控え、
Feedback 側に**同じ HitId かつ同じ被弾対象**の記録があること、その**結果種別が Damage** であること、
**フレームが一致**することを求める形にした。失敗時は届いた Feedback を
`HitId / 対象 / 種別 / フレーム` の並びで出す。

### 3. 命中 SE は未実装のまま残す（§5 の残件へ明記）

`CombatSePlayer` は P5 の Scene に置いていない。手応えの本体（HitStop・点滅・揺れ）は繋がっている。
**P5 統合時に P3.5 の既存 SE を仮流用するかを決め、最終選定と音量調整は P10b** とする。

### 4. 記録 019 を訂正した

| 直したこと | 前 | 後 |
|---|---|---|
| 第 4 回レビューの対象 | `8f01231` | **`501b073`** |
| 末尾の「未コミット」 | 残っていた | `301448b`（最終形 `97871f9`）として commit 済みと明記 |
| Pause 補助テストの範囲 | 押下一般のように読めた | **Interact 入力の持ち越し抑止**であると明記し、押しっぱなしの<b>移動</b>の保持挙動（記録 016 からの残件）とは別だと書き分けた |

### 5. 欠陥注入で確かめたこと（`CLAUDE.md` の手順）

| # | 注入した欠陥 | 実行 | 落ちたテストと Assert |
|---|---|---|---|
| U | 配信役が**別の命中として**配信する（HitId を +1000 して publish） | `p5r5-t5` | P08 補助「**最初の命中そのもの**が Feedback へ届く（別の敵・別の攻撃で代用しない）。命中=HitId=0:0 対象=PF_Enemy_Ranged_Prototype_1 Feedback 側=HitId=1000:0 …」 |

注入直後の実行（`p5r5-t4`）は**素通りした**。原因は注入後にコンパイルを走らせていなかったことで、
古いアセンブリのまま実行されていた。`compile-status --force`（`p5r5-c4`）を挟んでから
`p5r5-t5` で核心に落ちた。**注入のあとは必ずコンパイルを先に通す。**
復元後（`p5r5-c5` → `p5r5-t6`）は再び成功し、注入痕（`INJECT_R5_FB`）が 0 件であることを grep で確かめた。

§1 の補強は、取り込みの拒否そのものが「初期値では検出できない」ことの裏付けになったため、
追加の注入は行っていない（記録 019 の注入 O が同じ受入の核心に当たっている）。

### 6. 検証

常時許可（オーナーは Unity Editor を触らない）に基づき、確認を取らずに実行した。

| 実行 | id | 結果 | 所要 |
|---|---|---|---|
| `ArenaRelocation_`（補強後） | `p5r5-t2` | 2 / 2 Passed | 1.0 秒 |
| `compile-status`（force） | `p5r5-c3` | ok。**警告 0 件** | — |
| P08 補助（補強後） | `p5r5-t3` | 1 / 1 Passed | 3.4 秒 |
| 欠陥 U 注入 | `p5r5-t5` | **失敗 1 件（核心の Assert）** | 3.3 秒 |
| 復元後の確認 | `p5r5-c5`／`p5r5-t6` | ok / 1 / 1 Passed | 3.3 秒 |
| EditMode 全件 | `p5r5-em1` | **1655 / 1655 Passed、Skip 0** | 45.9 秒 |
| PlayMode 全件 | `p5r5-pm1` | **98 / 98 Passed、Skip 0** | 126.4 秒 |
| `validate-project-data` | `p5r5-v1b` | すべて通過 | — |
| `verify-required-tests`（P4） | `p5r5-v2c` | **226 / 226 Passed、未説明の Skip なし** | — |
| P5 一覧の照合（実装済み分） | `p5r5-em1`／`p5r5-pm1` の記録から | **57 / 57 Passed**（実装済み 39 ＋ 補助 18） | — |

件数は記録 019 から変わらない（EditMode 1655、PlayMode 98）。今回は既存テストの**中身**を強めた。

`run-op verify-required-tests` は `manifest` を渡しても P4 の一覧で照合する（`manifest` の明示選択は
`P5RequiredTests.json` の注記どおり **P5-09** で対応する）。P5 の 57 件は実行記録の
`_bridge/runs/p5r5-em1.json`／`p5r5-pm1.json` と一覧を突き合わせて確かめた。

### 7. 残件

- §13.3 の静的検査一式は P5-09（実装済みは `EnemyPrefabTable.Validate` と実行時の空構成・Boss 拒否のみ）。
- **命中 SE（`CombatSePlayer`）は未接続。**P5 統合時に P3.5 の既存 SE の仮流用を決め、
  最終選定・音量調整は P10b（上記 §3）。
- `verify-required-tests` の `manifest` 明示選択が未対応（P5-09）。
- Pause から復帰しても押しっぱなしの移動が再開しない（記録 016 から継続）。
- `TopDownCameraFollow` は固定オフセットのまま（継続）。
- P4 の実キー検査が固定フレーム待ちのまま（継続）。
- 仲間の活動 Context が `AreaReady` を参照していない（継続）。
- 到着時の向きは犬丸のみ適用。主人公は未適用（継続）。
- Scene の再生成が毎回 fileID を振り直す（継続）。
- 敵の死亡フェードは追加しない（オーナー判断）。

### 8. 次の一手

P5-08（本編型死亡再開・New Game・試遊との分離）。要求 **E20・E21**、**P09・P15・P18**。


## 内容ハッシュ（記録 020 の対象ファイル）

本記録の最終編集より後に採取した。`P5_統合受入結果.md` 自身は対象外。

| ファイル | 内容 | sha256 |
|---|---|---|
| `Assets/_Project/Tests/EditMode/P5ContractTests.cs` | 再配置テストの初期値を強めた | `a314a4f808d534be9b1f5c4a06a54318fc034a548cd1b02f4c921ed5f310c402` |
| `Assets/_Project/Tests/PlayMode/P5ExplorationPlayTests.cs` | 最初の命中 Feedback を同じ命中で照合 | `337e290dbfc5affaef3c54f5d7495a6d11b4db71208421837a031b82fa1a1e8d` |


## 記録 021：P5-08 本編型死亡再開・New Game・試遊との分離

作成日：2026-09-24　担当：Claude（claude-opus-5）　工程ゲート：**通過**

要求 **E20・E21・P09・P15・P18** を実装した。必須要求は **39 → 44 / 54**、補助テストは 18 → 19 件。
静的検査の配線は予定どおり P5-09、敵の死亡フェードは追加していない。

### 1. 何を作ったか（§9.1／§9.2）

| 置いたもの | 層 | 役割 |
|---|---|---|
| `CampaignRespawnCoordinator` | Gameplay/Session | 死亡の受理・再開要求 ID・**周期更新の一度限り**。UnityEngine 非依存 |
| `CampaignRespawnContracts` | Gameplay/Session | 段階・拒否理由・`ICampaignRespawnRequest`・`IAreaRespawnTravel` |
| `CampaignRespawnRunner` | Gameplay/Session | Area 単位の死亡購読と手順 1〜5 の実行 |
| `RespawnSubmitState`／`RespawnSubmitProvider`／`IInputReleaseGate` | Gameplay/Session | 再開操作の押下と「離すまで使わせない」の契約 |
| `RespawnSubmitInput` | Infrastructure/Input | UI/Submit を実行役へ仲介し、実デバイスの押下状態を毎フレーム整える |
| `CampaignRespawnView` | Presentation/Hud | 「**再開する**」の仮表示（§9.1 の 1 行目。既存試遊の Retry とは別語） |
| `AreaTransitionService.TryRespawnTravel` | Infrastructure/World | 死亡再開専用の遷移。Actor 値は運ばない |
| `AreaActorTransferPort.RestoreForCampaignRespawn` | Gameplay/Session | 主人公・犬丸の全回復・CD 解除・短時間状態解除 |

**調停役は Session が持つ**（`GameSessionState.Respawn`）。受理から到着までの間に元の Area は破棄されるので、
「周期を進めたか」の記録を Area 側に置くと読込失敗からの再試行で周期が二度進む。

**再開は §6.1 の受付条件を使わない。** 通常の移動は「探索中・生存・行動していない」を求めるが、
再開はその 3 つが<b>すべて成り立たないときにだけ</b>行う操作で、同じ表を当てると自分の前提で拒否されて
死んだまま動けなくなる（実際に `NotReady` で踏んだ）。代わりに「GameOver であること」を求める。

### 2. 受入（5 件）

| 要求 | テスト | 見ていること |
|---|---|---|
| E20 | `CampaignRespawn_PreservesEarnedProgressAndResetsVitals` | 徳 17・GrantOnce・門・調査・訪問・加入を保ち、主人公と犬丸だけを初期化。通常戦のクリア記録だけ消える |
| E21 | `RespawnRequest_IsOnceEvenWhenLoadingFails` | 連打でも受理 1 回。読込失敗の再試行でも**周期は 1 しか進まない**。到着後は次の死まで受け付けない |
| P09 | `DeathAfterOneKill_PreservesVirtueAndReopensNormalEncounter` | 近接 1 体で 10 → 実キーで再開 → 10 のまま → B へ戻ると再出現 → 全滅で 32 |
| P15 | `NewGameAndLegacyTrial_UseCorrectProgressScope` | P5 新規開始 0／往復で保持／束ねた進行は Scene から初期化できない／既存試遊はローカルで Retry も 0 |
| P18 | `RespawnSubmit_DoesNotBecomeArrivalInteract` | 実キー Enter・ゲームパッド South のどちらでも、押しっぱなしの再開が到着先の Interact／Step にならない。離して押せば 1 回効く |

死亡は**実被弾**で起こす（結果を直接セットしない）。探索中は犬丸の肩代わりと被弾後無敵が挟まるので、
届くまで実際に殴り続ける形にした。

### 3. P18 で分かったこと（正直な限界）

Button 型の Action は `initialStateCheck` が無く、押しっぱなしのまま Map を開き直しても<b>何も通知しない</b>。
つまり「押しっぱなしが新しい押下として立ち上がる」現象は、現在の `IA_Momotaro` の設定では起きない。
そのため<b>「到着後の Interact が 0」だけでは、仕様が求める保証を検査できない</b>
（欠陥注入 X＝解放待ちを立てずラッチを 1 回消すだけ、でも素通りした。`p5-08tx2` で確認）。

そこで P18 は**仕組みそのもの**も固定した：押している間は解放待ちが立っていること、押し続けている間は
解けないこと、離したら解けること。設定が変わっても仕様の保証が残る。

### 4. 途中で見つけた本番の不具合 3 件

| # | 不具合 | 直し方 |
|---|---|---|
| 1 | **戦闘から抜けても状態の券を返していなかった。** 移動だけ返して `Chase` の券を握ったままなので、追従の Follow 要求が所有権で弾かれて犬丸が Chase のまま固まる。その状態では `IsWarpAllowed` が false なので、離れた位置で戦闘が終わると追い付く手段が無い（主人公まで 18m、Warp が毎フレーム拒否） | `CompanionCombatController` の離脱時に `_states.Release(_action)` を足した。補助テスト `CombatDisengage_ReleasesStateSoFollowCanResume` で固定 |
| 2 | **覚えたボタンのデバイスが外れたあとに値を読んで例外。** 仮想デバイスを足して外すテストで `InvalidOperationException`。例外が入力の callback の中で起きるため、以後の入力が丸ごと止まり<b>無関係なテストが一斉に無反応</b>になる（全件 101 件中 11 件失敗・286 秒） | `IsPressed` で `device.added` を先に見るようにした。全件 101/101・137 秒へ戻った |
| 3 | **取り消しの文脈だけを信じると押下状態が固まる。** Map を閉じた canceled を「離した」にしない対策だけでは、閉じている間に離されたボタンが「押しっぱなし」のまま残り、次の押下が押下エッジにならない（再開の 2 回目が効かなくなった） | 覚えたボタンを実デバイスで毎フレーム照合して整える（`PollHeldControls`）。Action Map の後始末も TearDown で直接行う |

不具合 1 は<b>既存テストが不安定だった理由</b>でもある（戦闘の終わり方で犬丸の位置が変わるため、
たまたま近ければ歩いて追い付けていた）。記録 019 の残件「P08 の追従 assert が不安定」はこれで解消した。

### 5. 欠陥注入で確かめたこと（`CLAUDE.md` の手順）

| # | 注入した欠陥 | 実行 | 落ちたテストと Assert |
|---|---|---|---|
| V | 周期の更新で要求 ID を見ない | `p5-08tv` | E21「**再試行で再出現周期を二度進めない**。Expected: 1 But was: 2」 |
| W | 犬丸の復帰を省く | `p5-08tw` | E20「**加入済みの犬丸が復帰する**（倒れたままにしない）」 |
| X | 解放待ちを立てず、ラッチを 1 回消すだけ | `p5-08tx` | P18「**押している間は「離すまで使わせない」が立っている**」（§3 のとおり、挙動だけの検査では素通りする） |
| Y | 再開で進行ごと初期化する（試遊 Retry の流用） | `p5-08ty` | P09「**死亡再開でも既得の徳は 10 のまま**。Expected: 10 But was: 0」 |
| Z | 束ねられた進行も Scene 側から初期化させる | `p5-08tz` | P15「**束ねられた進行を Scene 側から初期化できない**」 |
| AA | 離脱時に状態の券を返さない | `p5-08taa` | P08 補助「**状態の券も返す**。Expected: None But was: Combat」 |

注入と復元は毎回 1 件ずつ・別の呼び出しで行い、復元後は grep で注入痕 0 件を確認した。

### 6. 検証

常時許可（オーナーは Unity Editor を触らない）に基づき、確認を取らずに実行した。

| 実行 | id | 結果 | 所要 |
|---|---|---|---|
| `run-op build-exploration-trial` | `p5-08b3` | ok。Data 4 件・Scene 3 件を再生成 | — |
| `compile-status`（force、復元後） | `p5-08c36` | ok。**警告 0 件** | — |
| EditMode 全件 | `p5-08em4` | **1658 / 1658 Passed、Skip 0** | 63.0 秒 |
| PlayMode 全件 | `p5-08pm5` | **101 / 101 Passed、Skip 0** | 137.8 秒 |
| `validate-project-data` | `p5-08v1` | すべて通過 | — |
| `verify-required-tests`（P4） | `p5-08v2` | **226 / 226 Passed、未説明の Skip なし** | — |
| P5 一覧の照合（実装済み分） | `p5-08em4`／`p5-08pm5` の記録から | **63 / 63 Passed**（実装済み 44 ＋ 補助 19） | — |

**EditMode 1655 → 1658（＋3）。PlayMode 98 → 101（＋3）。**

途中経過も残す：不具合 2 を直す前の全件は **90 / 101・286 秒**（実キーのテストが一斉に無反応）。
直したあとは **101 / 101・137.8 秒**。所要時間そのものが症状の目印になる。

### 7. 残件

- §13.3 の静的検査一式は P5-09（実装済みは `EnemyPrefabTable.Validate` と実行時の空構成・Boss 拒否のみ）。
- **命中 SE（`CombatSePlayer`）は未接続。**P5 統合時に P3.5 の既存 SE の仮流用を決め、最終選定・音量調整は P10b。
- `verify-required-tests` の `manifest` 明示選択が未対応（P5-09）。
- Pause から復帰しても押しっぱなしの移動が再開しない（記録 016 から継続）。
- `TopDownCameraFollow` は固定オフセットのまま（継続）。
- P4 の実キー検査が固定フレーム待ちのまま（継続）。
- 仲間の活動 Context が `AreaReady` を参照していない（継続）。
- 到着時の向きは犬丸のみ適用。主人公は未適用（継続）。
- Scene の再生成が毎回 fileID を振り直す（継続）。
- 死亡再開の到着で `NotifyArrived` を出すのは `AreaInitializer`。Scene を作り直さない再開を将来足すなら、
  到着の通知元をここへ揃える必要がある（現状は Scene ロードが必ず伴う前提）。

### 8. 次の一手

P5-09（静的 Validator の実装と配線。§13.3）。


## 内容ハッシュ（記録 021 の対象ファイル）

本記録の最終編集より後に採取した。`P5_統合受入結果.md` 自身は対象外。

| ファイル | 内容 | sha256 |
|---|---|---|
| `P5RequiredTests.json` | P5 必須テスト一覧（44 / 54、補助 19） | `b638f924f3b812612bcc1f0930b84080c05a7cb7c53cfa6aff5fc7f6e6b3272f` |
| `Assets/_Project/Scripts/Gameplay/Session/CampaignRespawnContracts.cs` | 新規：再開の段階・拒否理由・契約 | `71a3992e553cf4d857ede0ca66be2c3abea54e99ff016953109a41953109f161` |
| `Assets/_Project/Scripts/Gameplay/Session/CampaignRespawnCoordinator.cs` | 新規：一度限りの保証（要求 ID） | `91d7450a3b0c960c9eda0e4077105ed00e4c326fea232e3503533b8f092949a3` |
| `Assets/_Project/Scripts/Gameplay/Session/CampaignRespawnRunner.cs` | 新規：Area 単位の死亡購読と再開手順 | `79b479ebc6220a02a3b3a69b6e9970c24d9467f4576bc97c02faa1cd26b6f981` |
| `Assets/_Project/Scripts/Gameplay/Session/RespawnSubmitState.cs` | 新規：再開操作と解放待ちの契約 | `9a8ab3f41db23cb354b0f03b0dd7a3e7f604f599d2d0fbedefcda6be49cf8a81` |
| `Assets/_Project/Scripts/Gameplay/Session/GameSessionState.cs` | 調停役を Session が持つ | `a45b4a281940f156f9a60825e9b51882d5ae8dd00dcb5110860df68f1630505f` |
| `Assets/_Project/Scripts/Gameplay/Session/AreaTransitionContracts.cs` | 再開要求の別（IsRespawn） | `37a9521227c4c0e5100ddd02254a091fd1f1a68bb4588a74549d30abd8dacdec` |
| `Assets/_Project/Scripts/Gameplay/Session/AreaTransitionCoordinator.cs` | 再開の受付条件を分ける | `6d9a6abdf37d4932aa15f97cb99e616353f7ccee6241f897e9fbaf301bfe4c69` |
| `Assets/_Project/Scripts/Gameplay/Session/AreaActorTransferPort.cs` | 再開の全回復（主人公・犬丸） | `9b7627a942a031ce27fcb7b67e9c737a13ed425558134cef698a8f914d260ad9` |
| `Assets/_Project/Scripts/Gameplay/Companion/CompanionCombatController.cs` | 離脱時に状態の券を返す（不具合 1） | `d134257ed32f2231b90096d956310e0894a547ec34c47644001379ac1ab8828f` |
| `Assets/_Project/Scripts/Gameplay/Player/PlayerInputState.cs` | 離すまで使わせない | `f8767fc6378879b090f0959aebc082ffe6ed9c87cc14d5163c65c51bf0db3c36` |
| `Assets/_Project/Scripts/Gameplay/Player/PlayerStateController.cs` | 再開の中立化 | `3091ac5c7829e8965ff88ca9a9df2754c24ffd08f129dab681216afcea0ac36f` |
| `Assets/_Project/Scripts/Gameplay/Player/PlayerVitalsHolder.cs` | 再開の全回復（死亡確定の解除） | `9213df6477deb5544ffe172aa5510729e5fc7ea6a0aa0252f83ded61f9b724ca` |
| `Assets/_Project/Scripts/Infrastructure/Input/PlayerInputAdapter.cs` | UI/Submit と押下状態の照合（不具合 2・3） | `fbdb0c7278549f1f6d7de39eb8f5702508f770433e23f5b833e940e9b6e2c1d3` |
| `Assets/_Project/Scripts/Infrastructure/Input/InputBootService.cs` | 再開入力・解放待ちの提供点 | `b46cd4f9daf8d148c8c5695010d31792ce376ac7017ba006d1bd00af014d9660` |
| `Assets/_Project/Scripts/Infrastructure/Input/RespawnSubmitInput.cs` | 新規：再開操作の仲介 | `86b41963183f7e2465e44e711a63ecdf90d2c6539c7c9711bcfeb5e27adb92d3` |
| `Assets/_Project/Scripts/Infrastructure/World/AreaInitializer.cs` | 到着時の全回復と再開の配線 | `168a3386ef5fc8d12b9319e43cc44b30f80b20f391e3ee21c1e7080c2b0ba1ca` |
| `Assets/_Project/Scripts/Infrastructure/World/AreaTransitionService.cs` | 再開専用の遷移と失敗の戻し先 | `a028265c7d060be6bd853c54106fe21db938d2640ff713c123c592058d75c37b` |
| `Assets/_Project/Scripts/Presentation/Hud/CampaignRespawnView.cs` | 新規：「再開する」の仮表示 | `9a233ae963afa7160eb3185554e59f9120f6624ae54a0b58bd46aab4db4f58af` |
| `Assets/_Project/Scripts/Editor/Phase5/Phase5ExplorationBuilder.cs` | 再開の Scene 配線（A・B の両方） | `8b5b8dca4602791a2537d168425ffb716d623ea3db8c2f7aa6dd94eb6b8661f2` |
| `Assets/_Project/Tests/EditMode/P5ContractTests.cs` | E20・E21 と離脱の補助テスト | `632ecf448a047dc4883ef1387d48a669a73edd140d0f39d954f7f31728fc6041` |
| `Assets/_Project/Tests/PlayMode/P5ExplorationPlayTests.cs` | P09・P15・P18 と後始末の強化 | `725af381d8827613a9cd12043706a6fca1344fcdc869a2f05ee30aa3d52b82dc` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaA.unity` | 再生成（再開の配線） | `e8af1c70f52fea391a04f74d89bc00748bf03807839863a7c6a7d5929ca72a33` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaB.unity` | 再生成（再開の配線） | `6a07cf36e3935fd78e53677574d02140667b10497ce61a68052a3021a7fd158f` |


## 記録 022：P5-09 Builder／Validator／Bridge 公開操作の統合

作成日：2026-09-24　担当：Claude（claude-opus-5）　工程ゲート：**通過**

要求 **V01〜V05・P16** を実装した。必須要求は **44 → 50 / 54**（残り 4 件はすべて P5-10）、補助テストは 19 → 24 件。

### 1. 検査を 2 つに分けた（§13.2）

| 追加したもの | 入力 | 見ること |
|---|---|---|
| `Phase5ExplorationValidator`（Scene） | 渡された Scene だけ | 単一性・配線・混入禁止・入口 ID・調査・カメラ領域・NavMeshAgent 不在・初期状態・遭遇戦・重なり・衛生 |
| `Phase5AssetValidator`（Asset／Build） | Editor 専用 | カタログの経路解決・Scene の実在・Build Settings・共有素材・敵構成と**報酬合計 22** |

<b>Scene Validator に `AssetDatabase` を混ぜない</b>のは §13.2 の要求で、実利もある：混ぜると
「Scene を渡せば検査できる」性質が失われ、テストから素の Scene を流せなくなる。

**Build Settings は「登録はあるが無効」を登録済みと数えない。** 無効な登録では
`LoadScene` が通らず症状は未登録と同じなのに、Inspector を見た人は「入っている」と読む。
Builder 側も、既存の並び順を変えずに<b>無効なら有効へ戻す</b>ようにした（§13.1）。

### 2. Bridge へ登録した（§16.1）

- `run-op validate-exploration-trial` を許可リストに追加。**Scene を開く操作なので、未保存の変更があれば断る。**
  Asset／Build を先に走らせ、そのあと A・B を順に開いて Scene Validator にかけ、最後に空 Scene へ戻す。
- `verify-required-tests` に `manifest` を追加。**選べるのは既知の鍵（`P4` / `P5`）だけ**で、
  任意のパスは読まない。未指定は既定の P4 のままなので、既存の照合は変わらない。

これで P5 の照合が手作業の突き合わせでなく Bridge の 1 操作になった。
本記録の照合（`p5-09v6`）は `manifest=P5 stage=P5-09` で **50/50 Passed**。

### 3. 受入（6 件）

| 要求 | テスト | 見ていること |
|---|---|---|
| V01 | `GeneratedAreas_PassSceneDataAndAssetValidators` | 出荷物が Scene・Data・Asset の 3 つとも通る。どれか 1 つでは遊べないので 1 本にまとめた |
| V02 | `BrokenFixtures_FailWithSpecificDiagnostics` | 入力二重・旧仲介混入・ID 重複・入口欠落・未配線 Context・Floor 不整合・空 Encounter を**それぞれ別の文言で**検出 |
| V03 | `Rebuild_IsRepeatableAndDoesNotModifySharedAssets` | 2 回生成で重複せず、共有 Prefab・調査設定の**バイト列が変わらない**。Build Settings も重複しない |
| V04 | `DirtyScene_BlocksDestructiveGeneration` | 未保存なら生成しない。**断ったあとも編集が残っている**（勝手に保存も破棄もしない） |
| V05 | `ProjectDataValidation_IncludesPhase5GeneratedFolder` | 生成先へ不正 Data を置くと収集され `RunAll` が落ちる。片付けたら全件合格へ戻る |
| P16 | `RebuiltScenes_RunWithoutManualWiring` | **テスト側が一切 Bind しない。** 統合起動から A へ入り、B で Trigger を踏むだけで戦闘が始まる |

V03 の「原本を変えない」はバイト列で見る：参照が同じでも中身が変われば試遊の外へ影響が出るため。

### 4. 検査を書いて分かったこと

| 最初に書いた形 | 何が起きたか | 直した形 |
|---|---|---|
| 入口・出現点の重なりを**足元**で見る | 床が重なりとして出て、全部の入口と出現点が不合格（9 件） | 胴の高さ（0.9m）で見る。壁・柱は拾い、床は拾わない |
| 重なりに Actor も数える | 到着位置に主人公が立っているので不合格 | 主人公・犬丸の当たりは除く（**そこに置くのが正しい**） |
| 生成先の Data は 4 件だけのはず | NavMesh の焼き上がりが「見覚えのない Asset」警告に出た | 正規の出力として期待値へ入れる |
| 経路供給元を Scene で見る | 実行時注入なので Scene には保存されず、必ず未配線に見える | 静的には「注入する役が居て配線されているか」まで。未注入時の停止は実行時の責務（P5-06 が実 Scene で見ている） |

### 5. 欠陥注入で確かめたこと（`CLAUDE.md` の手順）

| # | 注入した欠陥 | 実行 | 落ちたテストと Assert |
|---|---|---|---|
| AD | 調査地点 ID の重複を見ない | `p5-09tad` | V02「**調査地点 ID の重複** を検出する」 |
| AE | 未保存でも生成する | `p5-09tae` | V04「**未保存の変更があれば生成しない**。Expected: False But was: True」 |
| AF | Builder が再開表示の配線を忘れる（置いてあるが繋がっていない） | `p5-09taf`／`p5-09taf2` | V01「**再開操作の表示 が配線されていません**」／P16「**A：再開の表示が配線されている**」 |

AF は<b>生成し直してから</b>検査した（Scene に実際に出力される欠陥なので、コードだけ変えても意味がない）。
復元後も生成し直し、注入痕 0 件を grep で確認した。

### 6. 実キーのテストが一斉に無反応になる症状（再発、未解明）

全件 PlayMode が **91/102・286 秒**になり、実キーを使うテストが 11 件まとめて落ちた。
P5-08 で直した「外れたデバイスの参照で例外」とは別で、今回は例外が出ていない。
新しく足した診断（Action Map の有効・無効、Interact の controls 数と押下、入力ゲート、解放待ち）で見ると
**Gameplay マップは有効・ゲートも開いている**のに押下が届いていなかった。再コンパイル後は同じテストが素通りする。

- 対処：`P5ValidatorTests` の `OneTimeTearDown` で Gameplay マップを明示的に戻すようにした
  （この一式は Scene を作り直して開き直すので、Editor の入力まわりを最も揺らす）。
- 確認：**EditMode 全件 → PlayMode 全件**を続けて実行して **1668 / 1668**・**102 / 102** を確認した（§7）。
- 残件：根本原因は未特定。同じ症状は<b>所要時間の倍増</b>（140 秒 → 286 秒）で見分けられる。
  診断はテストに残してあるので、次に出たら Action 側の状態がそのまま読める。

### 7. 検証

常時許可（オーナーは Unity Editor を触らない）に基づき、確認を取らずに実行した。

| 実行 | id | 結果 | 所要 |
|---|---|---|---|
| `run-op build-exploration-trial`（復元後） | `p5-09b1` | ok。Data 4 件・Scene 3 件を再生成 | — |
| `run-op validate-exploration-trial`（新規） | `p5-09v3` | ok。**エラー 0・警告 1**（A に遭遇戦なし＝想定どおり） | — |
| `compile-status`（force） | `p5-09c21` | ok。**警告 0 件** | — |
| EditMode 全件 | `p5-09em2` | **1668 / 1668 Passed、Skip 0** | 57.8 秒 |
| PlayMode 全件 | `p5-09pm4` | **102 / 102 Passed、Skip 0** | 140.3 秒 |
| `validate-project-data` | `p5-09v4` | すべて通過 | — |
| `verify-required-tests`（P4、既定） | `p5-09v5` | **226 / 226 Passed** | — |
| `verify-required-tests`（**manifest=P5 / stage=P5-09**） | `p5-09v6` | **50 / 50 Passed、未説明の Skip なし** | — |
| P5 一覧の照合（補助を含む） | `p5-09em2`／`p5-09pm4` から | 必須 50・補助 24 すべて Passed、未実装 4（すべて P5-10） | — |

**EditMode 1658 → 1668（＋10）。PlayMode 101 → 102（＋1）。**

### 8. 残件

- P5-10 の 4 件（E26・E27・P14・P19）が未実装。
- **命中 SE（`CombatSePlayer`）は未接続。**P5 統合時に P3.5 の既存 SE の仮流用を決め、最終選定・音量調整は P10b。
- 実キーのテストが一斉に無反応になる症状の根本原因（§6）。
- Pause から復帰しても押しっぱなしの移動が再開しない（記録 016 から継続）。
- `TopDownCameraFollow` は固定オフセットのまま（継続）。
- P4 の実キー検査が固定フレーム待ちのまま（継続）。
- 仲間の活動 Context が `AreaReady` を参照していない（継続）。
- 到着時の向きは犬丸のみ適用。主人公は未適用（継続）。
- Scene の再生成が毎回 fileID を振り直す（継続）。
- Scene Validator の重なり検査は半径 0.45m・高さ 0.9m の決め打ち（Actor の実寸から引いていない）。

### 9. 次の一手

P5-10（残り 4 件と人間試遊の準備。§16.2）。


## 内容ハッシュ（記録 022 の対象ファイル）

本記録の最終編集より後に採取した。`P5_統合受入結果.md` 自身は対象外。

| ファイル | 内容 | sha256 |
|---|---|---|
| `P5RequiredTests.json` | P5 必須テスト一覧（50 / 54、補助 24） | `d383f5532ce5f46fabeeaf236d1025bcdeabe0b79b773d71f7244f5433db3c1c` |
| `Assets/_Project/Scripts/Editor/Phase5/Phase5ExplorationValidator.cs` | 新規：Scene の静的検査 | `bcf0449c2aa6535ed2edc999ab399f8627fb9adb070f4c222847060aafab9af7` |
| `Assets/_Project/Scripts/Editor/Phase5/Phase5AssetValidator.cs` | 新規：Asset／Build の静的検査 | `52f0f4034024812aa7fc18f224d3dd7f6ccf5795a1f24e2307ee0c12ffe94d82` |
| `Assets/_Project/Scripts/Editor/Phase5/Phase5ExplorationBuilder.cs` | Build Settings を有効状態まで見て更新 | `5a3d83e592a05640e56f3c933ad49f42ae1263291c8693166a0ffb249135ed5a` |
| `Assets/_Project/Scripts/Editor/Bridge/EditorBridgeOperations.cs` | `validate-exploration-trial` と manifest 選択 | `aff049f7078979ffe16294e0260081fa306f4aabd4cab1eb2713bda45793224f` |
| `Assets/_Project/Scripts/Editor/Bridge/EditorBridgeProtocol.cs` | `manifest` 引数 | `a89ea40e2679e0b368357e7c047380b24e823596154630cfb5390b420f549c50` |
| `Assets/_Project/Scripts/Editor/Bridge/Phase4RequiredTests.cs` | 許可リストで一覧を選ぶ | `d30edd862e52ee7948fe044756bd6967c65ce5a58fbc339d0c24c3f51d011d5b` |
| `Assets/_Project/Scripts/Gameplay/Encounter/AreaEncounterSpawner.cs` | 出現点を Scene 検査へ公開 | `6f0f079095edfd05e60faf768fde207459f08bf0aaef13e5004fce28052836cb` |
| `Assets/_Project/Scripts/Gameplay/Session/CampaignRespawnRunner.cs` | `IsWired`（Scene 検査用） | `dabf0398100df9f94ab327e88d9c27e070f052144c5988338b75bf1fc6fe18d6` |
| `Assets/_Project/Scripts/Gameplay/Session/AreaActorTransferPort.cs` | `IsWired`（Scene 検査用） | `7cb7f4a20703a5f6a48990998082e91d947db1d12c87cb2f2b1369058dac5b3e` |
| `Assets/_Project/Scripts/Gameplay/Session/AreaTransitionConditionsSource.cs` | `IsWired`（Scene 検査用） | `c2b33cde166dd7a62630425b2a8246d30ca46e203eb08c6f8806cad94c7a5d37` |
| `Assets/_Project/Scripts/Infrastructure/Input/RespawnSubmitInput.cs` | `IsWired`（Scene 検査用） | `50bdc2c3788415567a9493e66b6056ed886eaaeba1d5a2f98a6e3d5483fa8b31` |
| `Assets/_Project/Tests/EditMode/P5ValidatorTests.cs` | 新規：V01〜V05 | `8157c68e0158b14659c24b0e83ea9878c24a4df097b0589882f5d4959de64752` |
| `Assets/_Project/Tests/EditMode/P5RequiredTestsManifestTests.cs` | 新規：manifest の許可リスト | `c8174d02a799f47afbc13ee37e6488fdb145afc05a6fc1a1fd14a2a88ef56bb7` |
| `Assets/_Project/Tests/EditMode/EditorBridgeOperationTargetsTests.cs` | 反射先（P5 の生成・検査）を追加 | `2de1445d314e5a31d590752d77890e2f86ad599aa25bd0da82738ec676381bed` |
| `Assets/_Project/Tests/PlayMode/P5ExplorationPlayTests.cs` | P16 と入力 Asset の診断 | `5a28d30596da7dc9739e3dbb416429d843a9f00dc865c7687228024d4402bf60` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaA.unity` | 再生成 | `758f5b34f201872c5ac8c17692c5f6c5a0dd625dde5f1b5d5178b684338ea7eb` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaB.unity` | 再生成 | `eb538897d8f672385f639e5f1312d4ff3dad5183450daec39cafe4efaa5f0c72` |


## 記録 023：P5-10 全件回帰・連続往復・引渡し文書（P5 の技術ゲート通過）

作成日：2026-09-24　担当：Claude（claude-opus-5）　工程ゲート：**通過（人間試遊のみ未実施）**

要求 **E26・E27・P14・P19** を実装し、**必須 54 / 54 が Passed**。引渡し文書を揃えた。
残るのは §16.2 の人間による短い試遊だけ。

### 1. 受入（4 件）

| 要求 | テスト | 見ていること |
|---|---|---|
| E26 | `NewGame_ClearsSessionButAreaLoadDoesNot` | Area のロードは何度繰り返しても Session を作り直さない。New Game だけが作り直す。束ねた進行は Scene 側から初期化できない |
| E27 | `Lifecycle_DisposeOnlyUnregistersOwnedProviders` | GameMode・Session・Gameplay 時計・活動 Context の 4 つで、**後発破棄が正本の提供点を消さない** |
| P14 | `RepeatedTravelAndRespawn_LeaveOneOwnerAndNoOldColliders` | A↔B 3 往復＋死亡再開。登録されている実体が**いまの Scene 所属**であることまで見る。不正遷移 0 |
| P19 | `RegistryTeardown_LeavesZeroWithoutClearingBeforeAssertion` | 全破棄と unload 完了のあと、**Clear を呼ぶ前に**登録総数 0。隔離の Clear は finally だけ |

P14 は「全体 Count=0」を求めない（§15.5）。活動中の Area に登録があるのは正しく、
全体 0 を要求すると「新しい Scene も壊れている」状態を合格にしてしまう。
代わりに、登録されている実体の `scene.handle` が現在の Scene と一致することを見る。

### 2. 直した本番の不具合

**`GameSessionBootService` が提供点を所有者一致で扱っていなかった。**

| 直した箇所 | 何が起きうるか |
|---|---|
| `Initialize()` が `GameSessionProvider.Current` を無条件に null にしていた | 後から立った重複 Bootstrap の初期化が、**走っている正本の Session を消す** |
| `Dispose()` が無条件に null にしていた | 旧 Scene 側の破棄が、新しい正本の Session を消す |

どちらも §5.2 末尾「Provider の解除は所有者一致で行う」に反していた。
「自分が差したものだけを外す」に直し、E27 で固定した。

### 3. 実キーのテストが一斉に無反応になる症状 — 原因を特定した

記録 022 §6 で「未解明」として残した症状の原因が分かった。

> **Input System の既定では、Game View にフォーカスが無いとキーボード入力はゲームへ届かない**
> （`editorInputBehaviorInPlayMode = PointersAndKeyboardsRespectGameViewFocus`。Editor 側へ回る）。

無人で走らせる自動実行では Game View にフォーカスが無いのが普通なので、実キーのテストが一斉に落ちる。
これで観測されたことがすべて説明できる。

- **キーボードだけが落ちる。** P18 でゲームパッドの半分だけ通ったのはこのため（設定の対象はポインタとキーボード）。
- **Action Map は有効・入力ゲートも開・`timeScale` も 1 なのに押下が届かない。** 届く前の段で止まっている。
- **所要時間が倍増する。** 実キーのテストが全部タイムアウトまで待つため（140 秒 → 286〜328 秒）。
- **再コンパイルで直ることがある。** フォーカスが戻るため。

対処：`PlayModeInputFocusFixture`（`[SetUpFixture]`）を追加し、**PlayMode テストの実行中だけ**
`AllDeviceInputAlwaysGoesToGameView` ＋ `IgnoreFocus` にして、終わったら元へ戻す。
製品側の Project Settings は変更していない。

適用後の全件は **104 / 104・171.9 秒**（直前の同じ構成は 91/104・327.6 秒）。

### 4. 途中で踏んだこと

| 出来事 | 何が起きたか | どうしたか |
|---|---|---|
| ブリッジが 3 回連続で PlayMode に入れなくなった | **アセンブリにコンパイルエラーがあった。** `compile-status` の引数を誤って `true` と渡したため「再コンパイルは発生しませんでした」と出て、エラーに気づかないまま PlayMode を投げ続けていた | 実際に再コンパイルさせてエラーを発見・修正。以後は PlayMode の前に必ず `--force` で通す |
| 仮想キーボードが 2 台になっていた | PlayMode を抜けてもデバイスが残り、同名で足すと `P5Keyboard1` と枝番が付く | 後片付けと SetUp の両方で `P5*` のデバイスを外すようにした |
| EditMode の検査一式が入力設定を揺らすと疑った | 実際の原因は §3 のフォーカスだった | 記録 022 で入れた「Gameplay マップを戻す」後片付けはそのまま残す（害は無く、別の経路の保険になる） |

### 5. 欠陥注入で確かめたこと（`CLAUDE.md` の手順）

| # | 注入した欠陥 | 実行 | 落ちたテストと Assert |
|---|---|---|---|
| AG | 提供点を所有者を見ずに外す | `p5-10tag` | E27「**後発破棄で正本の Session を消さない**。Expected: same as … But was: null」 |
| AH | Area のロードのたびに Session を作り直す | `p5-10tah` | E26「**Area のロードを何度繰り返しても Session は作り直さない**」 |
| AJ | Interact 候補の登録を外さない | `p5-10taj`／`p5-10taj2` | P14「**破棄済みの実体が登録に残っています**」／P19「Interact 候補の登録が残らない。残り=2」 |

AJ は 1 つの欠陥で P14（旧 Scene 由来の残留）と P19（破棄後の総数 0）の両方に落ちた。
復元後は注入痕 0 件を grep で確認した。

### 6. 引渡し文書（§17）

| 文書 | 中身 |
|---|---|
| `README_Phase5_探索試遊版.md` | 起動、操作、2 エリアの導線、徳 22 と死亡後の挙動、**データはアプリ終了で失われること**、人間試遊で見てほしい 6 点 |
| `P5_後続課題.md` | P6〜P10b へ渡す限定事項。**実装済みと未実装を分けて**明記。既知残件と「触ってはいけない既存裁定」 |
| `P5_統合受入結果.md` | 本記録まで。工程・対象コミット・実行結果・残件 |
| `P5RequiredTests.json` | 54 / 54 実装済み、補助 24 件 |

### 7. 検証

常時許可（オーナーは Unity Editor を触らない）に基づき、確認を取らずに実行した。

| 実行 | id | 結果 | 所要 |
|---|---|---|---|
| `run-op build-exploration-trial` | `p5-10b1` | ok。Data 4 件・Scene 3 件を再生成 | — |
| `run-op validate-exploration-trial` | `p5-10v1` | ok。**エラー 0・警告 1**（A に遭遇戦なし＝想定どおり） | — |
| `compile-status`（force） | `p5-10c20` | ok。**警告 0 件** | — |
| EditMode 全件 | `p5-10em1` | **1670 / 1670 Passed、Skip 0** | 82.9 秒 |
| PlayMode 全件 | `p5-10pm2` | **104 / 104 Passed、Skip 0** | 171.9 秒 |
| `validate-project-data` | `p5-10v2` | すべて通過 | — |
| `verify-required-tests`（P4） | `p5-10v3` | **226 / 226 Passed** | — |
| `verify-required-tests`（**manifest=P5、全工程**） | `p5-10v4` | **54 / 54 Passed、未説明の Skip なし** | — |

**EditMode 1668 → 1670（＋2）。PlayMode 102 → 104（＋2）。**

### 8. P5 の終了条件（§17）に対する現状

| 条件 | 状態 |
|---|---|
| §1.1 の一周が成立する | ✅ P05・P16 が実 Scene で通す |
| 通常往復で進行・Actor 値を保つ | ✅ E26・P14・P15 |
| 死亡時に進行を保ち Actor を再初期化する | ✅ E20・E21・P09 |
| P3.5／P4 互換 | ✅ P4 必須 226 / 226、P15 で持ち場の分離を固定 |
| 54 件の要求と既存回帰の合格 | ✅ 54 / 54、EditMode 1670・PlayMode 104 |
| 再生成可能性 | ✅ V01〜V05、`validate-exploration-trial` |
| **人間による短い試遊受入** | ⏳ **未実施**（§16.2。オーナーへ依頼中） |

### 9. 残件

- **人間試遊が未実施**（上記）。これが済めば P5 完了。
- **命中 SE（`CombatSePlayer`）は未接続。**P5 統合時に P3.5 の既存 SE の仮流用を決め、最終選定・音量調整は P10b。
- Pause から復帰しても押しっぱなしの移動が再開しない（記録 016 から継続。正しい挙動として固定）。
- `TopDownCameraFollow` は固定オフセットのまま（継続）。
- P4 の実キー検査が固定フレーム待ちのまま（継続）。
- 仲間の活動 Context が `AreaReady` を参照していない（継続）。
- 到着時の向きは犬丸のみ適用。主人公は未適用（継続）。
- Scene の再生成が毎回 fileID を振り直す（継続）。
- Scene Validator の重なり検査は半径 0.45m・高さ 0.9m の決め打ち（P10b で見直す）。

### 10. 次の一手

オーナーによる 5〜10 分の試遊（`README_Phase5_探索試遊版.md` §6 の 6 点）。
所見をもらい次第、P5 完了として P6 の引き継ぎへ進む。


## 内容ハッシュ（記録 023 の対象ファイル）

本記録の最終編集より後に採取した。`P5_統合受入結果.md` 自身は対象外。

| ファイル | 内容 | sha256 |
|---|---|---|
| `P5RequiredTests.json` | P5 必須テスト一覧（**54 / 54**、補助 24） | `61183b4f001a6a09d223ea1720a4c388cda37c890ec4afeac44ca3e75aa1f103` |
| `README_Phase5_探索試遊版.md` | 新規：試遊版の触りかた（§17） | `165b6ddc9392d7adc733474053e037aaca90b9833d4783c61a7a47c9cc80568e` |
| `P5_後続課題.md` | 新規：P6 以降への引渡し（§17） | `186b6741763bf57f9958ec5f61694ba415314b34cf7301b5e8b21d788a2a39be` |
| `Assets/_Project/Scripts/Infrastructure/World/GameSessionBootService.cs` | 提供点の解除を所有者一致にした | `a44b0cdd3b7439e893c06e0fab0d6e11eab91161efa3f66f582ede48541d7d00` |
| `Assets/_Project/Tests/EditMode/P5ContractTests.cs` | E26・E27 | `9110c9e828cbddec05dcf40980cd470e4e85ee93e7be542e4ec706a67e9ae621` |
| `Assets/_Project/Tests/PlayMode/P5ExplorationPlayTests.cs` | P14・P19、押し直しヘルパ、残留デバイスの後片付け | `8a397b5520b66b401431cf0edb6c0f92a0f69c47a3c648fed3fdb8ac06dbd4d9` |
| `Assets/_Project/Tests/PlayMode/PlayModeInputFocusFixture.cs` | 新規：実キーを Game View のフォーカスに左右されなくする | `7dbee713b22c11c6b67b0f4b302dbf01185fb21f5a5486fedcff151ee8649bcc` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaA.unity` | 再生成 | `ce15c241566a02dc920dd1a729761459d3ea73570770b2d9ca35261c6a0d5c8d` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaB.unity` | 再生成 | `c68bf102e0a4e34c4a4cab67d71d25ce598d5465e0bbbcc2fc24734a5c1177c6` |


## 記録 024：試遊で見つかった A→B 出入口の不発（配線漏れ）

オーナーの試遊（§16.2）で「エリア A から B への遷移が発生しない。青いオブジェクトの上で
→ を押し続けても壁に阻まれて何も起こらない」との報告。**再現し、原因を特定して直した。**

### 1. 何が起きていたか

`AreaExitGate`（開放出入口）は「範囲内で出口方向へ 0.15 秒連続入力」で遷移を要求する。
範囲の出入りは `SetPlayerInside(bool)` で伝える設計だったが、**これを呼ぶ者が実機の Scene に
どこにも居なかった。** 呼び出し箇所は `P5ExplorationPlayTests` の 2 か所だけで、どちらもテストコード。

そのため `_playerInside` は永久に false のままで、`Tick` は必ず false を返す。
出入口に立って東を押し続けても、溜めが 1 フレームも進まない。オーナーが見たのは
「壁際で押しても何も起きない」という、この不発そのもの。

Gate 自身は `BoxCollider(isTrigger)` を持っていたが、**Trigger を受け取るコードが無かった**。
コライダだけ置いて、受け手を書き忘れていた。

### 2. なぜテストが緑だったか

既存の P5-P04（`HeldExitInput_DoesNotBounceBackOrStartEncounter`）は
`gate.SetPlayerInside(true)` と `driver.Tick(...)` を**直接叩いて**いた。
判定ロジックの検査としては正しいが、これだと

- 範囲の出入りを**誰が Gate へ伝えるか**
- 実際の移動入力が**駆動部まで届くか**

という 2 本の配線が一度も通らない。継ぎ目の両側を作り、継ぎ目そのものを試していなかった。
`CLAUDE.md`「テストが本当に欠陥を捕まえるかを 1 回確かめる」の裏返しで、
**通したことがない経路は、いくつテストがあっても通っていない**。

### 3. 直した内容

| 対象 | 変更 |
|---|---|
| `AreaExitGate` | `OnTriggerEnter/Exit` を自分で持ち、`PlayerRoot` 一致の Collider だけを範囲内とする。`AreaEncounterTrigger` と同じ書き方に揃えた。`BindPlayer` と `IsWired` を追加。`SetPlayerInside` は明示呼び出し用に残す |
| `Phase5ExplorationBuilder` | 駆動部の配線の隣で、Area の全出入口へ主人公の根を `BindPlayer` する |
| `Phase5ExplorationBuilder` | 出入口 Trigger の中心を `y = WallHeight/2` へ。従来は y=0 中心で**半分が床下**に潜っており、主人公の Capsule（y 0〜2）との重なりが薄かった（遭遇 Trigger は既に床上に載せてある。そちらへ揃えた） |
| `Phase5ExplorationValidator` | `RequireWired<AreaExitGate>` を追加。配線漏れを Scene 検査で落とす |
| `P5ExplorationPlayTests` | 新規 `WalkingIntoTheExitAndHoldingEast_TravelsToB_ThroughTriggerAndRealInput`。**Gate にも駆動部にも触れず**、主人公を置いて実キー D を押し続けるだけで B へ着くことを見る |
| `P5RequiredTests.json` | 上記を P5-P04 の補助テストへ登録（補助 24 → 25） |

### 4. 欠陥注入で確かめたこと（`CLAUDE.md` の手順）

| # | 注入内容 | 見たもの | 結果 |
|---|---|---|---|
| INJ-1 | 修正前の Scene のまま新テストを実行（＝配線漏れそのもの） | 新 PlayMode テスト | 失敗。「出入口に主人公の根が配線されている」で停止（ex02） |
| INJ-2 | Builder の `BindPlayer` を外して Scene を再生成 | Scene Validator | エラー 2 件。A の `ExitGate_ToB`、B の `ExitGate_ToA` を両方指摘（ex09） |

INJ-2 は復元後に再生成・再検査して通ることを確認した（ex10〜ex12）。
新テストは修正前に**実際に落ちる**ので、この不具合を将来も捕まえる。

### 5. 検証

| id | 内容 | 結果 | 所要 |
|---|---|---|---|
| ex01 | compile-status（force） | 警告 0 件 | — |
| ex02 | PlayMode 新テスト（修正前 Scene） | **失敗＝再現**（配線漏れを指摘） | 1.6 秒 |
| ex04 | run-op `build-exploration-trial` | Data 4・Scene 3 を再生成 | — |
| ex05 | run-op `validate-exploration-trial` | 通過（警告 1 件・想定どおり） | — |
| ex06 | PlayMode 新テスト（修正後） | 成功 1 / 失敗 0 | 3.6 秒 |
| ex09 | validate（INJ-2 注入中） | エラー 2 件（想定どおり） | — |
| ex12 | validate（復元後） | 通過（警告 1 件） | — |
| **ex13** | **EditMode 全件** | **成功 1679 / 失敗 0 / 予定 1679** | **77.1 秒** |
| **ex14** | **PlayMode 全件** | **成功 105 / 失敗 0 / 予定 105** | **147.8 秒** |
| ex15 | run-op `validate-project-data` | すべて通過 | — |
| ex17 | run-op `verify-required-tests`（P5・ex13,ex14） | 必須 54/54 Passed。未説明の Skip なし | — |

PlayMode は 104 → 105 件（新テストぶん）。

### 6. オーナーへの依頼

**Scene を再生成したので、Unity で開き直してから試遊を続けてください。**
`SCN_Phase5_ExplorationTrial` を開いて Play → A の東の出入口で → を 0.15 秒以上押し続けると B へ移ります。
（途中の門は従来どおりレバーで開けてから通ります。）

### 7. 残件

- 同じ型の抜け——「Trigger を置いたが受け手が無い」「Bind を呼ぶ者が実機に居ない」——が
  他にも無いか、`IsWired` を持たないコンポーネントを対象に一度洗い出したい。今回は出入口のみ直した。
- Scene の再生成が毎回 fileID を振り直す件は継続（記録 023 §9）。

## 内容ハッシュ（記録 024 の対象ファイル）

本記録の最終編集より後に採取した。`P5_統合受入結果.md` 自身は対象外。

| ファイル | 内容 | sha256 |
|---|---|---|
| `Assets/_Project/Scripts/Gameplay/Session/AreaExitGate.cs` | Trigger を自分で受ける／BindPlayer・IsWired を追加 | `7a2b26b3d14e4905bbacc3b29d5e3c3a128ae0ed60970e2d91d1e84cfaf82269` |
| `Assets/_Project/Scripts/Editor/Phase5/Phase5ExplorationBuilder.cs` | 出入口へ主人公を配線／Trigger を床上へ | `20a212fe14da0b571f2bfd25313dc6d3f35916f2732a1e5a85467dde7b90f331` |
| `Assets/_Project/Scripts/Editor/Phase5/Phase5ExplorationValidator.cs` | RequireWired<AreaExitGate> を追加 | `0f5b71d1d6b0651e6fc58d1315b1cad5d7e5321dfda5703a96abdc654c0cb282` |
| `Assets/_Project/Tests/PlayMode/P5ExplorationPlayTests.cs` | 実機経路の新テストと押しっぱなしヘルパ | `1e38502631b84c1bc312fdb65a4f519a20748828f5305df0cf8cb7974b375201` |
| `P5RequiredTests.json` | P5-P04 の補助テストへ登録（54 / 54、補助 25） | `d165d23dbee998c40a284f65174974b75707f9ebd0fa07347c44176c191943ea` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaA.unity` | 再生成 | `dd197135c9d0fa6d71aa486c2b8b7f194f8d0f1424a0ba1f798c6f5937ffccfc` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_AreaB.unity` | 再生成 | `705b591ad8b53928c8b8f479d39a0a0a4d0138ab32f7d67ee971323b52db2d93` |
| `Assets/_Project/Scenes/Tests/Phase5/SCN_Phase5_ExplorationTrial.unity` | 再生成 | `09687df093644a90ffc98d8ac9058af2ac3b872f490c853a8199fffa83079f2e` |
