# P5 統合受入結果

Phase 5「マップと探索基盤」の工程ごとの記録。正本仕様は `Momotaro_P5_Detailed_Spec_v1.1.md`、
作業ルールは `CLAUDE.md`。P4 の記録は `P4_統合受入結果.md` に残し、本書へ統合しない。

---

## 記録 001：P5-00 着手（正本・ブランチ・裁定）

作成日：2026-09-21　担当：Claude（claude-opus-5、スレッド 2 本目）

### 1. 現在地

| 項目 | 値 |
|---|---|
| 作業ブランチ | `phase/5-world-exploration` |
| 親 SHA | `4a7650b`（`phase/4-companions-foundation` の先端、"P4"） |
| 本工程のコミット | `cca1876`（"P5事前"、オーナーが Windows 側で作成） |
| main への統合 | 未。P4 修正済み先端を親とする依存ブランチとして開始（仕様書 §0.2） |
| 実装正本 | `Momotaro_P5_Detailed_Spec_v1.1.md` |
| 未コミット | 仕様書 v1.0 削除 → v1.1 追加、引き継ぎ書追加、本書、`CLAUDE.md` 更新 |

P4 のユーザー試遊は 2026-09-21 にオーナーが実施し、**問題は発見されなかった**。
仕様書 §0.2 の「P5 の着手をもって P4 受入済みとはしない」は、これをもって解消する。

### 2. P4 残件（R3-01〜05）の再照合

各修正が実在することをコードで確認した。同じ修正を重ねない（仕様書 §0.2）。

| ID | 確認した実体 |
|---|---|
| R3-01 | 完了通知後の依頼同一性照合（実体参照＋実行世代）。`InvestigationCoordinator` |
| R3-02 | `Scripts/Gameplay/Combat/IIncomingHitObserver.cs`（`IIncomingHitObserver` / `IIncomingHitSource`）。配信元 `PlayerVitalsHolder`、購読 `InvestigationCoordinator` / `CompanionInvestigationController` |
| R3-03 | `Scripts/Gameplay/Companion/ICompanionInvestigationState.cs`。参照元は `CompanionCombatController` / `CompanionDefenseController` / `CompanionFollowController` |
| R3-04 | `InvestigationRequest.MoveSpeed`（受付時 Snapshot） |
| R3-05 | P06 の受入条件が守護の到達順契約と一致。実 Scene の重なりテスト 1 本 |

必須照合（`verify-required-tests`）による再確認は、Editor ブリッジ復旧後に §6 で実施する。

### 3. オーナー裁定（本スレッド冒頭）

1. **v1.1 残指摘 6 点は Claude の裁定で着手**する。GPT の v1.2 を待たない。裁定内容は §4 に記録し、GPT へは報告として渡す。
2. **PlayMode テストと Scene 置換の常時許可**。`CLAUDE.md`「PlayMode テストと Scene 破壊の常時許可」を本作業のあいだ適用する。
   オーナーはそのあいだ Unity Editor を触らない。実行したこと・結果・所要時間は本書に残す。
3. **コミット署名**を現行モデル名へ更新し、`Claude-Session:` はスレッドごとの可変値とする。`CLAUDE.md` を更新済み。

### 4. v1.1 残指摘 6 点の裁定

いずれも設計の方向を変えない。実装前に決めるのが 1〜3、運用で足すのが 4〜6。

#### 裁定 1（E28）：対象型の発見も反射にする

手書きの「対象型リスト」はテストが嘘をつく。マーカー型 `ITransferableRuntime`（仮称）を Gameplay
アセンブリから反射で走査し、台帳の型集合と**双方向**に照合する。台帳にあって型が無い／型があって台帳に
無い、のどちらも失敗させる。

併せて分類規則へ次を明記する。**`readonly` は不変を意味しない。** `CompanionVitals._flinch` が実例で、
`readonly` フィールドが可変の内部 Runtime を指す。分類は「フィールド修飾子」ではなく
「参照先が可変時間値を持つか」で行い、入れ子の時間所有型まで辿る。

#### 裁定 2（`IPlayerHurtReaction`）：契約を拡張しない

調査結果：

- `IPlayerHurtReaction` のメンバは `IsHurt` / `IsPostHitInvincible` / `BeginHurt` の 3 つだけ。
- 消費者は `PlayerVitalsHolder` と `PlayerStateController` の 2 箇所で、**どちらも真偽値しか見ていない**。
- 残り時間の実体は pure class `HitReactionState`（`_hurtRemaining` / `_invincibleRemaining`）で、
  `HurtRemaining` と `InvincibleRemaining` を**既に public で持っている**。
- `PlayerHitReaction` は `HurtRemaining` を公開済み。無敵は `PostHitInvincibleSeconds`（**設定値**）
  しか公開しておらず、残りが外から読めないだけ。

したがって「既定実装付きメンバ」も「Snapshot 専用の狭い契約を別に立てる」も不要で、**`IPlayerHurtReaction`
には手を触れない**。仕様書 §4.5 の「状態を所有するコンポーネントまたは純粋 Runtime クラス 1 つにつき
Snapshot 1 組」にそのまま従い、

- `HitReactionState` に `ExportTransferSnapshot` / `TryImportTransferSnapshot` を置く（pure class、
  MonoBehaviour 非依存でテストできる既存方針どおり）。
- `PlayerHitReaction` はそれを合成し、持ち越しマーカー契約 `ITransferableRuntime` だけを実装する。
- 検証用に `InvincibleRemaining` の委譲 getter を足す。**読み取り専用メンバの追加なので破壊的変更ではない。**

波及：実装の破壊 0 箇所、`PlayerHitReactionPrefabWiringTests` と `PlayerHurtReceiverTests` は無変更。

#### 裁定 3（V05）：不正 Fixture を全件実行の巻き添えにしない

`ProjectAssetIntegrityTests` が `ProjectDataValidator.RunAll()` でプロジェクト全体の合格を要求するため、
`finally` が回らずに不正 Asset が残ると**以後すべての全件実行が落ちる**。次の 3 点をセットで守る。

1. 不正 Fixture のファイル名に既知の接頭辞（`__P5V05Fixture_`）を付ける。
2. `[OneTimeSetUp]` で**その接頭辞に一致する残骸を先に削除**してから作る。前回の異常終了から自力で回復する。
3. `finally` に加えて `[OneTimeTearDown]` でも同じ掃除を行う。

接頭辞は 1 箇所の定数に置き、掃除側とファイル生成側が同じ定数を参照する。

#### 裁定 4（台帳）：P5-03a で凍結しない

持ち越し台帳 `P5_ActorTransferInventory.md` は P5-03a で作るが**凍結しない**。P5-05（再探索間隔・停滞
時間）や P5-07（Encounter）で可変時間値が増える。E28 が後工程で落ちるのは**正常な動作**であり、
そのとき台帳を更新するのが正しい対応。この旨を台帳の冒頭に明記し、E28 の失敗メッセージにも
「台帳を更新せよ」と書く。

#### 裁定 5（Away）：gameplay 経路を新設しない

調査結果：**出荷コードに `CompanionState.Away` を要求する経路は存在しない。**
`CompanionStateMachine` は `reason == Left` の Away をどの状態からも許すが、`RequestState(Away, Left)` の
呼び出しは Tests 側 6 ファイルだけ。非テストの唯一の入口は `CompanionActor` の初期状態
SerializeField（未加入開始用）。

裁定：P5 で Away を作る gameplay 経路を**新設しない**。仕様書 §1.3 が仲間切替を対象外とし、`CLAUDE.md` の
「未使用機能の実処理は先回りして作らない」にも反するため。§4.6 の Away 行は**契約として維持**し
（Away を維持する・通常表示と戦闘参加を有効化しない）、E29 は §4.6 が明示的に認めている
「初期化中の専用 Restore API」と `CompanionActor` の初期状態で検証する。

実 gameplay での Away 発生は **P8（猿・雉、切替、複数仲間の状態持ち越し）へ残件登録**し、
`P5_後続課題.md` に「契約は実装済み・発生経路は未実装」と明記する。

#### 裁定 6（§4.5 と §6.1）：§6.1 が正本

§4.5 の Capture 前提（Break 中・Hurt 中は受付拒否 等）と §6.1 の受付拒否条件が二重管理になっている。
**§6.1 を正本とし、§4.5 はその帰結**として扱う。実装は受付判定を §6.1 に対応する 1 箇所へ集約し、
§4.5 側は「§6.1 が拒否するので Capture 時にこの状態は来ない」という事前条件の表明に留める。
E08 が §6.1 の表を検査し、§4.5 側は別途の受付判定を持たない。

### 5. この工程で変更したファイル

| ファイル | 変更 |
|---|---|
| `CLAUDE.md` | コミット署名をモデル名・セッション URL の可変記述へ。P5 着手時点の実値を併記 |
| `Momotaro_P5_Detailed_Spec_v1.0.md` | 削除（v1.1 へ置換） |
| `Momotaro_P5_Detailed_Spec_v1.1.md` | 追加。実装正本 |
| `引き継ぎ書_P4完了_P5移行.md` | 追加 |
| `P5_統合受入結果.md` | 本書。新規 |
| `P5RequiredTests.json` | 新規。要求 54 件を登録（テストは未実装） |

`P4RequiredTests.json` は**維持**する（置換しない。仕様書 §15.5）。

### 6. 検証

オーナーが Unity Editor を閉じていたためブリッジが停止していた（`aliveAt` が 09:57:41Z で停止）。
10:28Z に再起動を確認し、`phase/5-world-exploration` 上で分岐直後の基準を取り直した。
常時許可のもと、EditMode 全件（Scene を置換する）と PlayMode 全件を確認なしで実行している。

| 実行 | id | 結果 | 終端 | 所要 |
|---|---|---|---|---|
| `compile-status`（force） | `cs1` | ok。**警告 0 件** | — | — |
| EditMode 全件（filter なし） | `regE3` | **1624 / 1624 Passed、Skip 0** | `completed` | 67.8 秒 |
| PlayMode 全件（filter なし） | `regP3` | **72 / 72 Passed、Skip 0** | `completed` | 60.5 秒 |
| `validate-project-data` | `vd4` | ok。Data 検証すべて通過 | — | — |
| `verify-required-tests`（P4、`runIds=regE3,regP3`） | `vt4` | **必須 226 / 226 Passed、未説明の Skip なし** | — | — |

- EditMode・PlayMode とも filter を付けない全件実行のため `expected` が入り、**予定 1624 / 実測 1624**、
  **予定 72 / 実測 72** が一致した。`termination` も両方 `completed`（`CLAUDE.md`「完走は termination で判定する」）。
- 実行記録は `_bridge/runs/regE3.json` / `_bridge/runs/regP3.json` に残る（`result.json` の上書きを受けない）。
- 最終確認：`isPlaying == false`、`isCompiling == false`。

**分岐直後の基準は P4 先端（`4a7650b`）と同一で、退行はない。** P5 の実装はこの基準の上から始める。

### 7. P5RequiredTests.json の内訳

仕様書 §15 の 3 つの表から要求 ID とメソッド名を機械的に抽出し、登録内容と突き合わせた結果、
**仕様書にあって一覧に無いもの 0 件・一覧にあって仕様書に無いもの 0 件・メソッド名の不一致 0 件**。
クラス割当（§15.1）の誤りも 0 件。

| mode / クラス | 件数 |
|---|---|
| `Momotaro.Tests.EditMode.P5ContractTests`（E01〜E30） | 30 |
| `Momotaro.Tests.EditMode.P5ValidatorTests`（V01〜V05） | 5 |
| `Momotaro.Tests.PlayMode.P5ExplorationPlayTests`（P01〜P19） | 19 |
| 合計 | **54** |

工程別：P5-01 が 5、P5-02 が 1、P5-03a が 4、P5-03b が 9、P5-04 が 6、P5-05 が 5、P5-06 が 2、
P5-07 が 7、P5-08 が 5、P5-09 が 6、P5-10 が 4。

**54 件はすべて未実装で、`implemented` は全件 `false`。** 現時点で Passed のものは無く、
P5 manifest での照合は不合格になるのが正しい状態である（仕様書 §14「未実施を Skip 成功に置き換えない」）。

### 8. 既知の残件

- 照合 Runner は `P4RequiredTests.json` 固定。P4／P5 manifest の明示選択は P5-09 で対応する（仕様書 §16.1）。
  それまで P5 の照合は実行できない。
- Away の実 gameplay 発生経路は未実装（裁定 5）。契約のみ。`P5_後続課題.md` へ P8 残件として記載する。
- `P5_ActorTransferInventory.md`（持ち越し台帳）は P5-03a で作成する。未作成。
- `README_Phase5_探索試遊版.md` / `P5_後続課題.md` は P5-10 で作成する。未作成。
- クラウド側の tar 作成スクリプトは前スレッドから引き継がれなかったため作り直した。
  PC 側 `$HOME/tools/`（`br.sh` / `wait.sh` / `tests.sh` / `op.sh` / `apply.sh`）も本スレッドで再作成済み。

### 9. 次の一手

P5-01（Session／Area State、Holder／Record 注入、ローカル互換）。要求 P5-E01〜E05。
徳の二重保持が無いことが工程ゲート。

---

## 記録 002：P5-01（Session／Area State、Holder／Record 注入、ローカル互換）

作成日：2026-09-21　担当：Claude（claude-opus-5）　工程ゲート：**通過**

### 1. 何を作ったか

仕様書 §4.1〜§4.3。徳の正本を Session の 1 個へ寄せ、Scene 上の Holder を「窓口」に変えた。
P3.5／P4 の試遊 Scene は未注入のまま従来どおり動く（ローカル Fallback）。

| ファイル | 種別 | 内容 |
|---|---|---|
| `Scripts/Gameplay/Session/GameSessionState.cs` | 新規 | 本編型 Session の純粋 State。`PlayerProgressState` を **1 個**所有。Area 辞書・訪問済み・加入・再出現周期 |
| `Scripts/Gameplay/Session/AreaRuntimeState.cs` | 新規 | 1 エリア分の世界状態。調査記録・開通 FlagId・Encounter クリア記録 |
| `Scripts/Gameplay/Companion/Investigation/IInvestigationRecordSink.cs` | 新規 | 調査記録への**狭い書込み契約**（§4.3）。`Count` と `TryMarkInvestigated` だけ |
| `Scripts/Gameplay/Progression/PlayerProgressHolder.cs` | 改修 | 外部 `Bind`、ローカル Fallback、共有 State 保護 |
| `Scripts/Gameplay/Companion/Investigation/InvestigationRecordHolder.cs` | 改修 | 外部 `Bind`、ローカル Fallback、`TryMarkInvestigated` を Holder 経由へ |
| `Scripts/Gameplay/Companion/Investigation/InvestigationRecord.cs` | 改修 | `IInvestigationRecordSink` を実装（メンバは既存のまま） |
| `Scripts/Gameplay/Companion/Investigation/InvestigationCoordinator.cs` | 改修 | 完了確定を `_record.Record.TryMark…` から `_record.TryMark…` へ（使用済み判定を通す） |
| `Tests/EditMode/P5ContractTests.cs` | 新規 | E01〜E05 |

### 2. 設計上の判断

**徳を二重に持たせない（工程ゲート）。** `PlayerProgressHolder` は `_localState`（自前）と `_boundState`（外部）を
持つが、`State` はどちらか一方だけを返す。E01 が `LocalState.Virtue == 0` を検査し、注入後にローカルへ
書き込んでいないことを固定した。`AreaRuntimeState` に徳を持たせていない（§4.2 の「World 状態用クラスへ
Virtue を複製しない」）。

**「読み取り」は使用済みにしない。** Bind 保護の判定に使う `_used` は `Grant` と `ResetProgress` でだけ立つ。
HUD が初期化前に `Virtue` を読んでも Bind 不能にならない（§4.2）。E02 がこれを明示的に検査する。

**共有 State は Scene 側から Reset できない。** `ResetProgress()` は `bool` を返すようにし、外部 Bind 済みなら
拒否して `false` を返す。未注入のローカル State では従来どおり動くため、既存の
`ProgressHolder_ResetProgress_ClearsVirtue` と `ResetProgress_ReturnsTotalToZero` はそのまま通る。

**`VirtueChanged` の契約は変えていない。** 「徳が実際に変化した瞬間のみ発火」のまま。実装中に一度
「GrantOnce 記録だけの消去でも通知する」形にしかけたが、徳が 0 のまま 0 を通知することになるため戻した。
field-like event の名前も `VirtueChanged` のままで、反射でバッキングフィールドを数える
`CombatHudViewModelTests` に影響しない。

**再出現周期は「いつクリアしたか」で持つ。** `AreaRuntimeState` はクリア済み Encounter を
`Dictionary<StableId,int>`（クリアした周期）で保持し、`IsEncounterCleared` は現在の周期と一致するときだけ
true を返す。`AdvanceRespawnCycle()` は周期を進めたうえで全 Area の記録を明示的に空にする。
どちらか一方でも成立するが、両方やることで「周期だけ進めて記録が残る」「記録だけ消して周期が古い」の
どちらの実装ミスも E05 が捕まえる。

### 3. テストが本当に欠陥を捕まえるかの確認（`CLAUDE.md` の手順）

「条件を足す」型の修正なので、**ガードだけを外した版を実機でビルドして落ちることを確認**した。
2 つのガードは同じテストの別の箇所で検査されるため、**1 つずつ**外して個別に確認している。

| 注入した欠陥 | 実行 | 結果 |
|---|---|---|
| `Bind` の使用後拒否（`if (_used)` → `if (false)`） | `p5neg` | E02 が失敗。「使用後の別参照への差し替えは拒否する。Expected: False But was: True」 |
| `ResetProgress` の共有保護（`if (_boundState != null)` → `if (false)`） | `p5neg2` | E02 が失敗。「Bind 済みの共有 State は Reset を拒否する。Expected: False But was: True」 |

どちらも**末尾の副次的な判定ではなく、その欠陥そのものを見ている Assert** で落ちた。判定を寄せ直す必要は無かった。
確認後に原本を復元し、差分が無いこと（`diff` 一致）とコンパイル警告 0 件を確認している。

### 4. 検証

| 実行 | id | 結果 | 終端 | 所要 |
|---|---|---|---|---|
| `compile-status`（force、復元後） | `cs5` | ok。**警告 0 件** | — | — |
| 絞込み `P5ContractTests`（`minPassed: 5`） | `p5e1` | 5 / 5 Passed | `completed` | 1.0 秒 |
| EditMode 全件 | `regE4` | **1629 / 1629 Passed、Skip 0** | `completed` | 66.0 秒 |
| PlayMode 全件 | `regP4` | **72 / 72 Passed、Skip 0** | `completed` | 60.7 秒 |
| `validate-project-data` | `vd5` | すべて通過 | — | — |
| `verify-required-tests`（P4、`runIds=regE4,regP4`） | `vt5` | **226 / 226 Passed、未説明の Skip なし** | — | — |

**EditMode は 1624 → 1629（＋5）で、増分は新規テストちょうど。既存の退行は 0。**
PlayMode は 72 のまま変化なし。両方 filter なしなので `expected` と実測が一致し、`termination` は `completed`。
最終確認：`isPlaying == false`。

書込み → `compile-status` → テストの順を毎回守っている（古いアセンブリを検査しない）。

### 5. 必須テスト一覧の更新

`P5RequiredTests.json` の P5-E01〜E05 を `implemented: true` にした。**実装済みは 5 / 54。**
残り 49 件は未実装のままで、Skip 成功や `allowedSkips` へ置き換えていない。

### 6. 残件・次工程への申し送り

- `GameSessionState` に全体 Reset を**置いていない**。新規開始は新しい実体を作る（§9.2）。E26 で固定する。
- `AreaRuntimeState.ClearNormalEncounterRecords()` は `internal`。呼べるのは `AdvanceRespawnCycle()` 経由だけで、
  Scene 側から通常 Encounter の記録を個別に消せない。
- Session を実際に生成・所有するのは Infrastructure（Bootstrap／Game Session 層）で、**P5-03b の作業**。
  本工程は Gameplay 側の純粋 State と注入口までで、まだ誰も `GameSessionState` を生成していない。
- 犬丸加入は `Recruit(StableId)` の語彙だけ置いた。出撃対象の選択は P8（仲間切替）まで不要なので実処理を作っていない。

### 7. 次の一手

P5-02（仮地形、Area／Entry カタログ、最小 Builder）。要求 P5-E25、V01〜V02 の該当部分。
または P5-03a（持ち越し台帳と Snapshot／Import）。仕様書 §14 の依存では P5-02 と P5-03a は並行可能。

---

## 内容ハッシュ（記録 001・002 の対象ファイル）

各記録の最終編集より後に採取した（`CLAUDE.md`「ハッシュ表は記録を書き終えた最後に採り直す」）。
`P5_統合受入結果.md` 自身はこの表を書き加えることで内容が変わるため対象外とする。

| ファイル | 内容 | sha256 |
|---|---|---|
| `CLAUDE.md` | 作業ルール | `6d8648833b02dddbeea8c2dea4e331fb2c5695e4793e027eded005e170c53368` |
| `Momotaro_P5_Detailed_Spec_v1.1.md` | P5 実装正本 | `6a602beedf2d7f2b7188c71da45741d6924b9c3379071460e1d9217fd28768da` |
| `P5RequiredTests.json` | P5 必須テスト一覧（E01〜E05 を implemented へ） | `3bf373f239649cce717b25d32b71abad0152980705041c19333dc1910a1aacc9` |
| `P4RequiredTests.json` | P4 必須テスト一覧（維持・無変更） | `2447add6e20840ef9426bebfa28a6889cb5386459ea28d538f07094237a02e52` |
| `Assets/_Project/Scripts/Gameplay/Session/GameSessionState.cs` | 新規 | `98842cf27c07e52c8f6b461bd85ed402ef5fcc2802baf1bf8d229a2288c37a4a` |
| `Assets/_Project/Scripts/Gameplay/Session/AreaRuntimeState.cs` | 新規 | `3c6ad4a7cb43db341aa35092cbee933a02d0761cdaa09cb575c699322cf44a2d` |
| `Assets/_Project/Scripts/Gameplay/Companion/Investigation/IInvestigationRecordSink.cs` | 新規 | `7a71b1a4e2cd89c26a4942d5cfda89e37f28a8298584c8901621aa43f01e6473` |
| `Assets/_Project/Scripts/Gameplay/Progression/PlayerProgressHolder.cs` | 改修 | `d5e1eb31057ad6f2ad4337ec6943968447cdcc1fb2dfd78c1819300ee8e20a96` |
| `Assets/_Project/Scripts/Gameplay/Companion/Investigation/InvestigationRecordHolder.cs` | 改修 | `e78b9f01a2c524a2d99eb2da6aa31f19f5f0ce37ec0bb5ca958858028d3825ec` |
| `Assets/_Project/Scripts/Gameplay/Companion/Investigation/InvestigationRecord.cs` | 改修 | `b8e5558dd9bedc32134530ba6ccce5c79cea804cf701fdef83a104479720fce8` |
| `Assets/_Project/Scripts/Gameplay/Companion/Investigation/InvestigationCoordinator.cs` | 改修 | `b08c705856685926fd3c6264ffd139b1e7df01e547dfe6801f5bc1af353afdd2` |
| `Assets/_Project/Tests/EditMode/P5ContractTests.cs` | 新規（E01〜E05） | `19bf9cc4c07f79fdd332c0a6c51d2bb9d459a2d2bf07f0642020eb236a7b742f` |

記録 001 の対象は `cca1876`（"P5事前"）。記録 002 の変更（P5-01）と、記録 001 へ後から足した
§6〜§9 は**いずれも未コミット**。次のコミットに含めること。
