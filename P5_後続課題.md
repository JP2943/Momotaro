# P5 後続課題 — P6 以降へ渡すもの

Phase 5（探索と 2 エリアの一周）を終えた時点で、**実装したもの**と**渡すもの**を分けて記録する。
仕様の正本は `Momotaro_P5_Detailed_Spec_v1.1.md`、工程の記録は `P5_統合受入結果.md`。

---

## 1. P5 で実装したもの（渡す前提になる土台）

| 領域 | 実装済みの内容 | 主な型 |
|---|---|---|
| 進行 State | 徳・GrantOnce・調査済み・門の開通・訪問済み・加入を 1 つの Session が持つ。Scene 側から初期化できない | `GameSessionState`／`PlayerProgressState`／`AreaRuntimeState` |
| エリア遷移 | 受付条件・世代・タイムアウト・復旧を分けた調停。Actor 値の採取と復元 | `AreaTransitionCoordinator`／`AreaTransitionService`／`AreaActorTransferPort` |
| 探索 | Interact の単一窓口、調査（指定地点モード）、レバーと門、開放出入口、扉 | `AreaInteractionController`／`InvestigationCoordinator` |
| 遭遇戦 | 区画 Encounter の 8 段階、一時境界、報酬、勝敗の調停（同フレームの死亡優先） | `AreaEncounterRunner`／`AreaArenaBoundary`／`AreaEncounterSpawner` |
| 死亡再開 | 本編型の再開（進行を保ち Actor だけ初期化）、再出現周期の一度限り、再開入力の解放待ち | `CampaignRespawnCoordinator`／`CampaignRespawnRunner`／`RespawnSubmitState` |
| 経路 | NavMesh は経路の供給だけ。書き込みは調停役 → Motor の 1 本 | `CompanionPathFollowModel`／`AreaNavigationBinder` |
| カメラ | 領域ごとの追従と clamp。入りきらない軸は中央固定 | `AreaCameraRig`／`AreaCameraRegion` |
| 生成と検査 | 3 Scene・Data・地形・NavMesh の再生成、Scene／Asset の静的検査、Bridge 操作 | `Phase5ExplorationBuilder`／`Phase5ExplorationValidator`／`Phase5AssetValidator` |

**P5 の受入は 54 / 54。** 一覧は `P5RequiredTests.json`、照合は
`run-op verify-required-tests`（`manifest=P5`）で行う。

---

## 2. P6 へ渡すもの（保存）

P5 のデータは **Play 終了で失われる**。保存は P6 の仕事として、次を前提にしてよい。

- 進行の正本は `GameSessionState` ひとつに集約済み。**保存専用 DTO はまだ作っていない**
  （仕様書 §9.2 の「可変正本を先に作らない」に従い、意図的に未着手）。
- `PlayerProgressState`（徳・GrantOnce）と `AreaRuntimeState`（調査済み・門・通常戦クリア記録）が
  そのまま保存単位になる。**通常戦のクリア記録は再出現周期とセット**で意味を持つ（`RespawnCycle`）。
  保存すると「死亡再開で湧き直す」の周期をまたぐので、保存の粒度は P6 で決める必要がある。
- 欠損・旧版の扱い、章別の戦闘徳上限、Item と成長、完成版の休息・再開点は未着手。
- P5 の再開点は **A の開始点で固定**。お地蔵様の保存・休息は先行実装していない。

## 3. P7 へ渡すもの（会話・イベント）

- 会話・`EventSequence`・恒久イベント進行・加入演出・条件付き扉・章移行はすべて未着手。
- `GameMode` には `Dialogue` / `Event` が既にあり、Pause と同じく**行動を閉じる側**として配線済み。
- 調査の完了は `DiscoveryId` を記録するところまで。**そこから何かが起きる仕組みは無い**。

## 4. P8 へ渡すもの（猿・雉）

- 仲間は犬丸 1 体前提の配線が残っている：`CompanionIds.Inumaru` を直接指す箇所が
  `AreaActorTransferPort`・`Phase5ExplorationBuilder`・`CompanionRosterContext` にある。
- 状態の持ち越し（`CompanionActorTransferSnapshot` 等）は 1 体分の形。複数化は P8 で設計し直す。
- 役割別の経路（飛ぶ・登る）は未着手。`IPathProvider` は 1 種類しかない。

## 5. P9 以降へ渡すもの

- 第一章の量産導線、地図 UI、屋根・壁の透過、複数 Floor。
  **Floor は P5 では 0 のみ対応**で、非 0 は Validator が落とす。
- 実コンテンツの Encounter 種別（Boss・一度限りイベント）。
  P5 は通常 Encounter だけを置く前提で、`IsBossEncounter` は Validator が拒否する。

## 6. P10b へ渡すもの（見た目・演出）

- **命中 SE（`CombatSePlayer`）が未接続。** 手応えの本体（ヒットストップ・点滅・画面揺れ）は繋いである。
  P5 統合時に P3.5 の既存 SE を仮流用するかを決め、最終選定と音量調整は P10b。
- 正式素材に合わせたカメラ距離・画角、VFX、見た目上の間合い、演出値。
- `TopDownCameraFollow` は固定オフセットのまま（P5 は `AreaCameraRig` を別に使う）。

---

## 7. 既知の残件（P5 の中で直していないもの）

| 残件 | 影響 | 扱い |
|---|---|---|
| Pause から復帰しても押しっぱなしの移動が再開しない | 復帰後にスティックを入れ直す必要がある | 記録 016 から継続。現状は**正しい挙動として固定**している |
| ~~実キーのテストが一斉に無反応になることがある~~ | ~~全件 PlayMode の所要時間が倍増して見分けられる~~ | **解決（記録 023 §3）。** 原因は Game View に焦点が無いと Unity が実キーを Editor 側へ回すこと。`PlayModeInputFocusFixture` で PlayMode 中だけ焦点に依存しない設定へ切り替える。併せて、取り外し済みデバイスの参照で入力 callback ごと止まる例外源（`PlayerInputAdapter` の未ガードな `isPressed` 読み出し 3 箇所）を素材差し替え記録 §5 で塞いだ |
| Scene の再生成が毎回 fileID を振り直す | 差分が大きくなる | 継続。生成物なので実害は無い |
| 仲間の活動 Context が `AreaReady` を参照していない | 到着直後の 1 フレームだけ緩い | 継続 |
| ~~到着時の向きは犬丸のみ適用。主人公は未適用~~ | ~~到着直後の主人公の向きが入口依存でない~~ | **解決（記録 025）。** `AreaActorTransferPort.PlaceAt` で主人公の `PlayerFacing` にも入口定義の向きを適用する。次のフレームからは通常どおり入力が勝つ |
| P4 の実キー検査が固定フレーム待ち | たまに不安定 | 継続（P4 側の課題） |
| Scene Validator の重なり検査は半径 0.45m・高さ 0.9m の決め打ち | Actor の実寸から引いていない | P5-09 で導入。素材が決まる P10b で見直す |

---

## 8. 引き継ぎ時に触ってはいけないもの（既存の裁定）

実装の都合で黙って変えないこと。変える必要が出たら、該当箇所・現行契約・影響範囲・推奨案を明記して相談する。

- **既得の徳を死亡で失わせない**（§8.5）。
- **Proxy 探索を自動戦闘で中断させない**（P4 の裁定）。
- **進行 State を Scene 側から初期化させない**（§4.2）。
- **再出現周期の更新は再開要求 ID につき 1 回**（§9.1）。
- **NavMeshAgent を仲間の経路に置かない**（§10.1）。書き込みは調停役 → Motor の 1 本。
