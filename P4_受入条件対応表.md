# P4 受入条件 ↔ 検証手段 対応表

改訂ロードマップ v2.2「P4 終了条件」と、タスク案 P4-00〜P4-08 の各項目に対して、
**何がそれを確かめているか**を並べたもの（順序 6）。

この表の役割は 2 つ。

1. 受入条件のうち**機械で確かめたもの**と、**人が見るしかないもの**を分けて示す。
2. 「テストが通った」ではなく「この条件をこのテストが押さえている」という対応を残す。
   テスト名が消えたときに気付けるよう、**正本は `P4RequiredTests.json`**（実行結果に現れる完全名）とし、
   本表はその読み方を説明する側に徹する。

照合は `run-op verify-required-tests`（`stage` と `runIds` を指定）で機械が行う。要求 ID（E／P／R）と実テストの対応も
`P4RequiredTests.json` の `requirements` と各テストの `requirementIds` が正本で、**対応テストの無い要求は不合格**になる（記録 009 時点：204 件、未対応 0）。
本表を人が読み合わせる運用にはしない（読み落とすため）。

---

## 1. P4 終了条件

| # | 終了条件（v2.2） | 何が確かめているか | 状態 |
|---|---|---|---|
| 1 | 犬丸が既存の敵 AI・命中解決・ヘイト契約へ接続されている | `CompanionThreatAccrualTests`（与ダメージ→獲得ヘイト）、`CompanionHitReceiverTests`（解決順）、`CompanionHitboxReachTests`（物理判定を通した命中）、`CompanionCombatWiringTests`（出荷 Data の配線） | ✅ 機械 |
| 2 | 追従・ワープ・戦闘・ガード／回避・かばう・Down／復帰・**探索行動**が仮表示で識別できる | 状態ごとの色は `CompanionStateColors` ＋ `CompanionPlaceholderPresenterTests`。探索は色に加えて文字で示す：表示代理・進行ラベル・地点マーカー・短文 UI（`InvestigationPresentationTests`）、実 Scene での表示は `CompanionTrialScenePlayTests` | ⚠️ 色・文字の対応は機械、**見え方の是非は人** |
| 3 | Disable・Scene 離脱・Retry で購読・攻撃判定・対象・Coroutine が残らない | 各駆動の `Disable_*` / `*_LeavesNo*` 系（追従・戦闘・防御・守護・被弾・演出登録・探索）、`CompanionFeedbackLifecyclePlayTests`（実破棄） | ✅ 機械 |
| 4 | EditMode／PlayMode テスト・Data 検証・専用 Validator が成功する | 全件実行 ＋ `run-op validate-project-data` ＋ `Phase4CompanionFieldValidator` / `Phase4CompanionTrialValidator`（生成直後の Scene を EditMode テストが検査）＋ **保存した実 Scene を読み直す** `CompanionTrialScenePlayTests`（メモリ上の検査では捕まらない Missing Script を実際に捕まえた。記録 009） | ✅ 機械 |
| 5 | 徳の現在値を試遊 HUD で目視確認できる（累計表示のみ・獲得演出なし） | `CombatPlayHudTests` ＋ `Phase35CombatTrialValidator`（HUD へ `PlayerProgressHolder` が配線されていること）。試遊 Scene は 3.5 のものを土台にしているので、この配線がそのまま載る | ⚠️ 配線は機械、**目視は人** |
| 6 | Scene を Builder で再生成しても表示層へ `PlayerProgressHolder` 参照が復元される | `Phase35CombatTrialBuilderTests` ＋ `Phase4CompanionTrialTests.TrialScene_StillSatisfiesPhase35Acceptance`（仲間を載せても 3.5 の受入を満たす） | ✅ 機械 |

---

## 2. タスク案 P4-00〜P4-08

| ID | 内容 | 主な検証 |
|---|---|---|
| P4-00 | 徳・撃破報酬の受け手、HUD 表示、Builder 配線 | `CombatRewardCollectorTests` / `RewardWaveTotalTests` / `PlayerProgressStateTests` / 3.5 Validator の報酬配線検査 |
| P4-01 | `CompanionData`、仲間状態、所属・対象契約、肩代わりの割込み契約 | `CompanionDataTests` / `CompanionStateMachineTests` / `GuardianHitTransferTests` / `ReceivedHitTrackerTests` |
| P4-02 | 追従・隊列・距離超過／経路失敗ワープ | `CompanionFollowModelTests` / `CompanionFollowControllerTests` / `FormationSlotTests` |
| P4-03 | 戦闘参加・索敵・対象選択・通常攻撃 | `CompanionCombatControllerTests` / `CompanionTargetSelectionTests` / `CompanionEngagementTests` / `CompanionHitboxReachTests` |
| P4-04 | ガード／回避判断・被弾解決 | `CompanionDefenseControllerTests` / `CompanionHitReceiverTests` |
| P4-05 | 守護（かばう）の割込み条件とクールダウン | `CompanionGuardianControllerTests` / `GuardianHitOrderTests` / `PlayerGuardianTransferTests` |
| P4-06 | Hurt／Down／復帰・退場時の後始末 | `CompanionVitals` 系 ＋ 各駆動の後始末テスト |
| P4-07A | 探索の依頼・記録モデル（v1.0 §4〜§6・§8、c8c0ddf §5） | `CompanionInvestigationTests`（E01〜E14・E21・E22）／`CompanionActionRuleTests`（探索中の競合表）／`CompanionGuardianTransactionTests`（E17〜E19） |
| P4-07B | 入力・表示代理・地点マーカー・短文 UI・明示開始入力（v1.0 §4.2・§5.2・§11・§13.1） | `InvestigationPresentationTests`（E03・E20・P03 の EditMode 側）／`Phase4CompanionBuilderTests.Prefab_HasInvestigationProxyWired_*`／`Phase4CompanionTrialTests.Build_WiresInteractInput_*`。**「待て／ついてこい」は P4 の範囲外（§3.2）で撤去** |
| P4-08 | 専用 Scene・Builder・Validator・統合受入 | F01（`Phase4CompanionFieldTests`）＋ P4-08R（`Phase4CompanionTrialTests`、実 Scene の `CompanionTrialScenePlayTests`：P01〜P06・P09〜P11・R10） |

---

## 3. P4-FIX（レビュー由来の是正）

| 工程 | 内容 | 主な検証 |
|---|---|---|
| P4-FIX-02A | 検証基盤の補完・F05（活動 Context）・F02a（移動の調停） | 記録 002。N01〜N18 |
| F02b | 状態要求と正常終了の一本化 | 記録 003。N19〜N24 |
| F02c | 行動許可表（v1.0 §8.3） | 記録 004。N25〜N36 |
| F01 | P4 専用 Scene Builder／Validator | 記録 005。N37〜N66 |
| P4-07A | 探索行動、レビュー §5 の競合表 | 記録 006（撤回）→ 記録 009。N67〜N87（作り直し） |
| P4-07B | プレイヤー指示との優先順位 → **入力・表示（作り直し）** | 記録 007（撤回）→ 記録 009。N145〜N166 |
| P4-08R | 専用試遊環境、移行期の穴の決着 | 記録 008（一部撤回）→ 記録 009。N100〜N118、N167〜N175 |
| P4-FIX-R2 | 守護取引の順序・Guard→Evade・未注入は停止 | 記録 009。N119〜N144 |

---

## 4. 機械で確かめていないもの（人の判断が要る）

ここに挙げたものは**テストが無いのではなく、テストで決められない**もの。
自動検証が緑であることと、これらが済んでいることは別である。

| 項目 | なぜ人が要るか | いつ |
|---|---|---|
| 仮表示で状態を見分けられるか | 色の割り当ては機械で固定できるが、「見て分かるか」は人の目 | 試遊 Scene を Play |
| 徳の HUD 表示 | 配線は機械で固定済み。読めるかどうかは人の目 | 同上 |
| 探索の 6 つの数値 | Interact 範囲・継続範囲・到着距離・移動タイムアウト・調査秒・帰還表示上限は手触り（`SO_Investigation_Trial.asset` の仮値）。犬丸のスプライト完成後に再検証するとオーナー判断 | スプライト完成後 |
| 回避に移動が無い | 「避ける動き」を付けるかは演出・数値の設計（D07） | オーナー判断 |
| 短文 UI の文言と位置 | 文言は `InvestigationTexts` に閉じてある。読みやすさは人の目 | 試遊 Scene を Play |
| 戦闘中・会話中の切り替わりの見え方 | 切り替え自体は PlayMode テストが固定済み。テンポの是非は人 | 試遊 Scene を Play |

---

## 5. 読み方の注意

- **「テストがある」と「その条件を押さえている」は別。** 表の右欄は、その条件が壊れたときに
  赤くなるテストを挙げている。名前が似ているだけのテストは挙げていない。
- **Skip は「検証できていない」を意味する。** `allowedSkips` は空にしてあるので、
  Skip が 1 件でも出た実行は照合で不合格になる（Scene を保存して再実行する）。
- **本表は説明で、正本ではない。** 必須テストの正本は `P4RequiredTests.json`。
  クラス名や正規表現で代替しないのは、そのクラスからテストが 1 本消えても総数で気付けないため。
