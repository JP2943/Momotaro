# 桃太郎プロジェクト P4 統合実装依頼書 v1.0

P4-FIX ＋ P4-07詳細仕様 ＋ P4-08R再統合／Claude引き渡し用

- 作成日：2026-09-09
- 状態：P4-07詳細仕様案v0.1のユーザー承認を反映した、実装用の統合文書。
- 対象リポジトリ：`JP2943/Momotaro`
- 対象ブランチ：`phase/4-companions-foundation`
- レビュー基準コミット：`aa80e05c0ec906ac9d2211ce90d6c446635ee1b1`（P4-08）
- Unity：`6000.3.20f1`
- 本書の作成では、リポジトリ変更・Unityテスト・コミット・Pushを実施していない。
- 修正指摘は上記コミットの既存レビューを統合したもの。実装開始時点の最新状態を新しく検証したという意味ではない。

## A. Claudeへの依頼と作業前提

本書に従い、既存P4の必須修正、保留中のP4-07、専用試遊環境P4-08Rを実装・検証してください。主人公スプライトの手描き修正は待たず、既存Placeholderで進めます。

本書は単独で実装依頼として読める構成です。「B」の修正一覧と「仕様§1〜§17」の詳細仕様は両方とも対象です。後半「C〜F」に修正固有の追加テスト、正式保留、実行順、完了報告を定義します。本文の「§n」は詳細仕様の番号を指します。

### A.1 最初に確認すること

1. 最新の `CLAUDE.md`、該当する `AGENTS.md`、レイヤー規約、テスト・ブリッジ運用規則を読む。
2. 対象ブランチの先端と作業ツリーを確認し、レビュー基準からの差分を確認する。P4ブランチ上の変更を古いmainへ戻さない。
3. 本書の各指摘を最新コードで再照合する。既に直っている項目は再実装せず、維持確認と回帰テストを行う。
4. 既存のユーザー変更・Claude側の未コミット作業を保存する。無関係な変更や素材整理を混ぜない。
5. 本書に含まれる仕様・受入条件をリポジトリ内の適切な仕様書／タスク表へ反映してから実装する。

既存のP4-00〜06等を全面作り直す依頼ではありません。純粋モデルと既存の命中・報酬契約を再利用し、競合と統合上の欠落を直してください。クラス・API名は設計例であり、同じ責務・契約・受入条件を満たす名称変更は可能です。

本書は記載範囲のP4決定を具体化します。上位の実装・権限規約を上書きしません。最新のユーザー決定と実質的に衝突する場合は、その一点だけ確認してください。新しいゲーム仕様へ影響しない内部実装の選択は、理由を記録して進めて構いません。

### A.2 変更しないもの

- 受入済みP3.5の主人公攻撃・コンボ・Guard・JustGuard・Step／JustEvade・必殺技の仕様と数値。
- 主人公通常攻撃SEはStartup開始、敵攻撃SEはActive開始という確定仕様。
- 既存の命中解決順、同一HitIdの排除、守護転送のダメージ計算。今回の行動調停が必要とする接続以外を再設計しない。
- P3.5の既定の自動Wave開始、4 Wave構成、既存の勝敗・Retry。P4専用開始方法は既定動作を維持した小さい拡張とする。
- P4-00の徳報酬・一回性・Retryリセット。4 Wave合計の既存期待値94を変更しない。
- 正式スプライト・Animator全体・P10の素材置換。主人公の手描き制作へ追加素材を要求しない。
- 能動かばう、仲間スタミナ、本編セーブなど「D.正式保留」の項目。

### A.3 テスト・連続実装の権限

UnityテストをClaude側で実行できる既存運用を利用し、許可済みの範囲では小タスクごとに人間の操作待ちを挟まず進めます。ただし、本書の仕様承認を、PlayMode実行・未保存Scene破棄・外部送信・Commit／Push等の新しい包括許可と解釈しないでください。

既に与えられたタスク権限とリポジトリ規約に従います。未許可操作、未保存Sceneの損失、保護された手順、既存変更との両立不能が見つかった場合だけ止め、必要な確認を一つにまとめます。テストの失敗を隠すためにSkipを増やしたり、Validatorを緩めたりしないでください。

## B. 修正指摘一覧

### B.1 必須修正の全体像

以下は「レビュー基準で確認した欠落／不整合」と「その受入に必要な補強」を区別した一覧です。詳しい動作契約は後続の仕様本文を正とします。

| ID | 対象 | 指摘・不足 | 今回の到達点 |
|---|---|---|---|
| F01 | P4-08試遊Scene・Builder・Validator | P3.5 Sceneへの犬丸追加が再生成で復元されず、既存Validatorも犬丸欠落を検出しない | P4専用Sceneを全配線から再生成でき、犬丸0体を確実に不合格にする |
| F02 | 仲間の行動調停 | 状態順位の契約が実行経路を拘束せず、AttackとDefense／Protectが並行し得る | 1体1Driver、明示的な開始・割込み・正常終了、仕様§8の排他 |
| F03 | 守護の登録解除・転送取引 | 古いGuardianの解除が新しいResolverも消し得る。行動中断と被弾結果の整合を保証する必要がある | 所有者一致解除、成立時のみ中断、Down／Staggerを上書きしない |
| F04 | 犬丸の戦闘Feedback | 現行Dispatcherの通常購読対象に犬丸Resultsが含まれない | 既存の単一Feedback窓口へ配線。肩代わりHitStopは犬丸Damage由来のみ |
| F05 | GameMode・活動停止 | 入力停止やtimeScaleだけでは独立Update・Motor・各タイマーが止まらない | 活動Contextで行動・物理移動・時計を統一停止し、安全に再開 |
| F06 | 攻撃Snapshot | 攻撃中の判定で射程等をSOから読み直す経路が残る | 当該攻撃の射程・寸法・時間・威力・終了CDを開始時の値に固定 |
| F07 | Builder EditModeテスト | 一時Prefab／Dataだけを渡すと既定の本番AttackData出力先が使用される | AttackDataも専用一時パス。本番素材はテスト前後で不変 |
| F08 | Editor Bridgeの結果判定 | 失敗0件だけで成功扱いにすると実行0件等が成功に見える | 実行0件・全Skip・必須統合テストSkipをフェーズ合格にしない |
| F09 | 回帰テスト・受入証跡 | 実Prefab全体の同時動作と、コードに対応する保存済み結果が必要 | 実Scene／実Prefab統合、全件実行の独立性、追跡可能な受入記録 |

P4-07未実装は別途の新規実装タスクです。F01〜F09だけでP4-07を完了扱いにしないでください。

### B.2 F01：専用Sceneを再生成可能にする

主な確認先は `Phase35CombatTrialBuilder`、`Phase35CombatTrialValidator`、`Phase4CompanionBuilder`、既存 `SCN_Phase35_CombatTrial.unity` です。既存の `Phase4CompanionBuilder` は仲間Prefab生成用であり、専用試遊Scene Builderがあることの代わりにはなりません。

P4専用のScene Builder／Validatorを追加してください。Scene名は `SCN_Phase4_CompanionTrial` を推奨します。生成対象は犬丸だけでなく、加入資格、活動Context、探索地点、入力、HUD・探索UI、Feedback、Wave開始・勝敗・Retryの配線まで含みます。

P3.5の生成・検査を犬丸必須へ変更するのではなく、P4専用の受入責務を分離します。既存P3.5 Scene内の犬丸を移設／整理する必要がある場合は、対象差分を限定し、P3.5の受入済み動作を維持したことを報告してください。

Sceneを渡す純走査ValidatorはAssetDatabase非依存を維持します。将来のPlaceholder／Master参照パス検査は別Validatorです。未割当でも落ちないRuntimeと、必要な仮表示が揃っていないSceneを不合格にするValidatorは両立させます。

### B.3 F02：同時行動と状態の上書きを止める

確認先：`CompanionStateMachine.TryTransition`、`CompanionStatePriority.CanInterrupt`、`CompanionCombatController.TickCombat / CanEngage`、Defense／Guardian／Followの各駆動箇所。

現行経路では、既に開始した攻撃の更新が防御状態の検査より先に動くこと、攻撃可能判定でProtect／Guard／Evade／Eventを除外し切れていないことが競合につながります。

単に条件式へ状態を追加するだけで終えず、最終的な状態・Motor操作の書き手を一つにします。ただし、状態の数値順位をすべての遷移へ一律適用してはいけません。正常終了による低い順位への復帰は許可し、所有者・実行IDの古い終了通知だけを無視します。

詳細な許可表は仕様§8.3です。AttackActive中の自動Guard／Evadeは不可、守護成立によるAttackActive中断は可、Evade動作中の守護は不可です。ダメージによるDown／Staggerを古いProtect・Attack完了処理で消さないことを必須とします。

### B.4 F03：Guardianの所有権と既存命中契約

`CompanionGuardianController.Unregister` は、自分が登録したResolverが今も有効な場合だけ解除してください。コメントだけでなく参照比較または登録トークンで保証します。犬丸A→犬丸Bの順で登録し、AをDisableしてもBが残るテストを追加します。

行動調停の変更で、P4-01〜05の受入済み契約を壊さないでください。

- 転送でも元のHitIdを維持し、犬丸側の受け手の排除規則を通す。直撃→転送／転送→直撃の両順序を検査する。
- 同一HitIdが既に受理されていた場合の転送成功・不成功の扱いは既存の確定契約を維持する。攻撃側MultiHitTrackerだけで十分とみなさない。
- 主人公基準の方向をそのまま再使用せず、既存の肩代わりヒットバック契約を維持する。
- 主人公側のHitResultKindへ新種別を追加しない。GuardianTransferは専用通知を利用する。
- GuardBreakでGuardが成立しない被弾は、ほかの成立条件を満たせば従来どおり守護対象。
- GuardianTransfer専用演出へ独自HitStopを追加しない。
- 取引の拒否では攻撃中断・守護CD消費・成立通知を発生させない。受理確定前後の再入と被弾結果の扱いは仕様§8.5に従う。

既存契約の実態がレビュー時点と異なる場合は、該当差分を示してください。転送割合、既受理時の成否、主人公側の結果通知を、この統合作業で独自の新仕様へ変更しないでください。

### B.5 F04：犬丸Damageを既存Feedbackへ接続する

確認先：`Assets/_Project/Scripts/Presentation/Diagnostics/CombatFeedbackDispatcher.cs` と `CompanionHitReceiver.Results`。

Player／Dummy／Enemyの購読だけでは犬丸への被害は自動的には表示されません。犬丸の登録・有効化・解除・Scene再読込に合わせて、Resultsから既存Dispatcher／Presenterへ一重で接続します。仲間専用の別HitStop管理器は作りません。

守護専用VFX／SEと犬丸Damageの通知が同じ攻撃で発生しても、守護演出が追加のHitStopを発行しないことを検証します。複数の実被害が起きた場合のHitStop合成は既存Presenterの規則に任せ、新しい独立調停を追加しません。

### B.6 F05・F06：活動停止とSnapshot

`GameMode == Exploration` を非戦闘の意味にしないでください。Wave幕間も戦闘中です。timeScaleが1のままPause／イベントへ移っても、仲間が歩き続けたり、新たな攻撃判定を出したりしないようにします。

通常移動と押し出しを含む仲間の物理経路を点検し、停止中の残留速度を防ぎます。Pauseは同一攻撃の段・HitId・既命中集合を保持し、会話／イベントは古い攻撃を破棄します。タイマー・表示代理の具体的な方針は仕様§7・§9に従います。

攻撃中にSOへ戻ってUseRange等を読み直さず、開始時Snapshotの値を使ってください。探索依頼の時間・距離設定も、受理時Snapshotを使って実行中のSO編集から隔離します。将来の攻撃／依頼が新値を読むことは問題ありません。

### B.7 F07：テストによる本番Asset変更を防ぐ

対象：`Assets/_Project/Tests/EditMode/Phase4CompanionBuilderTests.cs`

一時PrefabPath・DataPathに加えて、一時AttackPathを明示的に渡します。既定引数を通じて本番 `SO_Attack_*` を書き換えないでください。

テストが例外で失敗しても、そのテスト自身が作った一時AssetとSceneを片付けます。本番Assetが元からないケースでも新規生成しないこと、既にあるケースでも内容・参照・GUIDを変えないことを検査します。クリーンアップでユーザーの作成物や広いディレクトリを削除しないでください。

### B.8 F08：0件・Skipを成功と誤認しない

対象：`Assets/_Project/Scripts/Editor/Bridge/EditorBridgeService.cs` の `OnTestRunFinished` 付近。

`failed > 0 ? "failed" : "ok"` のような件数だけの成功判定を見直します。

- テストフィルタ不一致等による実行0件は、明示的なNoTests／失敗等として返す。成功件数0を成功の証拠にしない。
- 全Skipはフェーズ合格にしない。必須の統合テストがSkipなら、それ以外が成功でも受入は未完了。
- 実行エラー、キャンセル、結果欠落、実際の失敗も成功へ丸めない。
- ブリッジの既存クライアントと結果スキーマを確認し、必要なら呼び出し側・テスト・説明も一緒に更新する。
- 意図的に残した非必須Skipは件数と理由を明示する。「全件成功」とは表現しない。

汎用ブリッジがどのテストを必須とするかを知らない場合は、ブリッジで0件／全Skip／失敗を正しく表現し、P4受入ランナー側で必須テストの実行を保証する構成で構いません。

### B.9 F09：実Prefab統合と証跡

既存の物理命中テストは維持します。ただし、Defense／Guardian／Follow等の一部を省いた手組みRigだけでは行動競合の受入を代替できません。実PrefabまたはBuilder生成Sceneを使い、入力・物理命中・状態遷移・Feedbackまで一緒に通すテストを追加します。

レビュー時点でコードに対応する保存済み実行結果が確認できなかったことは、「Claudeがテストを実行していない」と断定するものではありません。今回は結果を上書きされない形で残し、実行した対象とテスト数を追跡可能にしてください。

### B.10 以前の指摘の訂正・解消済み項目

| 項目 | 最新の扱い | 今回やること |
|---|---|---|
| `Recovering`が未使用 | 未使用自体は不具合ではない。P4は復帰待ち全体をDownにする承認済み暫定仕様 | 予約値と明記し、到達をValidatorで必須にしない。形だけの段階追加は不要 |
| InteractとStepのボタン競合 | 既定では競合なし。Interact＝E／南、Step＝Space／東 | 既定マップは維持。Interact購読者間の一回消費を検証 |
| 犬丸Prefabの旧Hitbox項目 | 基準コミットでは幅・高さ等の新しい項目へ修正済み | 旧項目へ戻さない。現行フィールドとBuilder生成結果を確認 |
| P4-00の徳HUD・Builder配線 | 基準コミットでは実装済み | 作り直さず、94・Retry・再生成の回帰を維持 |
| 実物理Update／登録経路のテスト | 追加済み。ただし全Controller同時動作の代替ではない | 既存を残し、F09の実Prefab統合を追加 |
| `CombatTrialReloadPlayTests`のScene後始末 | 修正済み | 後戻りさせず、新規テストにも同じ独立性を要求 |

---

# P4-07 詳細仕様（承認済み）

以下は詳細仕様案v0.1の承認内容を実装用に統合したものです。P4試遊限定の決定と本編保留を区別します。新規初期値は試遊値として採用し、完成版の最終調整まで確定したとは扱いません。

## 1. 結論と確定事項

P4-07は「犬丸へ細かい移動命令を出す仕組み」ではなく、「探索地点への調査依頼と、仲間の自動行動を安全に両立させる仕組み」とする。

採用する体験は、痕跡へ近づく → 「調べる」を押す → 犬丸が短く調査する → 発見を知らせる、である。戦闘中は使えない。加入済みなら、戦闘不能・控え・離脱クールダウンを理由に探索を妨げない。

次の4点をユーザー承認済みの実装前提とする。

| 決定 | 採用内容 | 区分 |
|---|---|---|
| 指示の種類 | 特殊地点への「調べる」。待機／集合／攻撃対象指定コマンドは追加しない | 原仕様に沿う範囲確定 |
| 探索の資格 | 加入済みかで判定。戦闘状態とは分離する | 原仕様の継承 |
| 今回の成果 | 発見通知とScene内の調査済み記録まで。実報酬・本編扉・保存は後続へ接続 | 改訂工程に合わせた分割 |
| P4全体との境界 | 自動守護と戦闘不能復帰の現行試遊仕様を維持し、本編との差分を正式に保留記録する | 承認済みのP4試遊限定仕様 |

P4試遊限定仕様の継続と、本編との差分の正式保留を今回の合意とする。本編仕様まで完成したものとしてP4を無条件に完了扱いにしない。

## 2. 既存資料との整合

### 2.1 原仕様から継承するもの

『ゲーム仕様書v1.4』§4.4、§4.15、§8.14、§14.5には次が定義されている。

- 移動・通常攻撃・回避は自動行動。
- 戦闘の固有スキルは専用ボタンから指示する。
- 探索は特殊地点を調べることで、適切な仲間が行う。
- 犬の探索対象は匂い・足跡・埋蔵物・人物の痕跡。
- 仲間探索は戦闘中不可。未加入ならヒントのみ。
- 加入済みなら、出撃状態・離脱CD・戦闘不能に関係なく探索可能。

したがって、探索指示と戦闘スキル指示は別の用途であり、既存の`CompanionSkill`入力を探索ボタンへ転用しない。

### 2.2 原仕様と現行P4には差分がある

| 項目 | 原仕様v1.4 | 現行P4／今回の採用仕様 |
|---|---|---|
| 守護 | 常時の割合肩代わり | 改訂ロードマップv2.2に基づく、条件付きのHitInfo転送を維持 |
| 指示スキル「かばう」 | 一定時間、割合肩代わりを強化する能動スキル | 自動守護と同一視しない。P4-07では戦闘スキルの効果を追加しない |
| 戦闘不能復帰 | 戦闘終了後に復帰。§4.14では敵なし2秒・HP20% | 現行の時間経過復帰（Data：5秒・50%）をP4試遊限定で維持 |
| 探索完了 | 宝箱／道／報酬／調査済み保存へ接続 | P4は発見通知＋Scene内記録。実体はP5〜P7 |
| 仲間のスタミナ | ガード・回避の消費あり | 現行P4の未実装差分。P4-07で新規導入しない |

原仕様の古い数値を現行P4へ自動的に戻さない一方、現行実装を本編の確定仕様とも扱わない。正式保留台帳に、移行先・着手条件・未受入の項目を残す。

正式保留の移行先は、戦闘終了に基づく復帰と探索済み値のエリア持ち越しがP5、永続保存がP6、固有戦闘スキル・仲間スタミナの共通化がP8着手前の先行タスク。P8の猿・雉実装が始まる前に契約を確定する。

なお、以前のレビューに補足する。`Recovering`が未使用であること自体は不具合ではない。現行の待ち時間全体を`Down`で表すことも可能であり、名前を使うためだけに新しい回復段階を追加しない。また、状態の数値順位を全遷移へ適用してはならない。正常終了で下位状態へ戻る経路と、外部からの割込みは別の規則で扱う。

## 3. 今回のスコープ

### 3.1 実装するもの

1. 犬丸用の探索地点と、地点から受け付ける調査依頼。
2. 加入資格、戦闘・モード・距離・重複実行の可否判定。
3. 調査先への移動、短い調査、発見通知、表示の復帰。
4. 成功・拒否・中断と、理由を識別できる通知。
5. Scene内での調査済み記録と、重複完了の排除。
6. 探索と戦闘・防御・守護・被弾・イベントの行動調停。
7. 同じ入力で複数機能を同時起動しない、最小限の入力仲介。
8. 仮素材による状態表示と、P4-08の統合検証へ必要な接続。

### 3.2 実装しないもの

- 「待て」「ついてこい」、地点クリック移動、攻撃対象の手動指定。
- 自動で周囲の宝物をすべて探す索敵機能。
- プレイヤーのボタンから毎回発動する能動「かばう」の完成版。
- 猿・雉本体、交代UI・交代効果・控え全員の実管理。
- スキルツリー、仲間スタミナ・状態異常の追加。
- 宝箱、アイテム付与、徳報酬の追加、本編扉、会話・クエストの完成実装。
- エリア間の探索記録保持、セーブ／ロード。
- NavMeshや汎用経路探索システムの新規導入。
- 新しい完成スプライト・演出素材。

P4-07のためにP5〜P8を先行実装しない。既存の徳報酬は従来どおり敵撃破からのみ付与される。

## 4. プレイヤーから見た仕様

### 4.1 基本の体験

1. 調査可能な痕跡へ近づくと「調べる」を表示する。
2. 既存`Interact`入力（キーボードE／ゲームパッド南ボタン）を押す。
3. 犬丸が加入済みで、平常探索中なら調査を開始する。
4. 犬丸の仮表示が地点の調査位置へ移動し、約1秒調べる。
5. 地点を調査済みにし、「犬丸が痕跡を見つけた」等の短文とマーカーで知らせる。
6. 犬丸の表示を通常運用へ戻す。戦闘HP・復帰待ち・CDは探索によって変更しない。

主人公の移動は固定しない。大きく離れる、戦闘が始まる、会話・イベントへ入る等の場合は未完了の調査を中断できる。短い調査のために、全体のGameModeを勝手にEventへ切り替えない。

### 4.2 入力と対象の決定

- 入力は押下1回で1依頼。押しっぱなしで連続実行しない。
- 依頼には対象地点を明示する。犬丸自身の索敵で別の地点へ依頼を変更しない。
- 複数地点が候補の場合、使用可能な地点を優先し、主人公とのXZ距離、同距離なら前方への近さ、最後に地点StableIdの辞書順で1件へ決める。
- 到達不能地点や別の壁越しの地点を、表示上の「使用可能」へ含めない。
- 1つのInteractを複数地点・複数の購読者で同時受理しない。受理したInteractだけを消費し、探索対象がないときは既存操作を維持する。現行の割当はInteract＝E／南ボタン、Step＝Space／東ボタンであり、両者に既存の重複はない。将来のリバインドや他の調べる対象の追加を含め、1入力1実行を検証する。
- `CompanionSkill`（U／LB）は戦闘スキルの予約された用途のまま保持する。今回「待て／集合」に割り当てない。

### 4.3 受付条件

| 条件 | 挙動 |
|---|---|
| 犬丸加入済み・平常探索・地点有効・範囲内 | 受理 |
| 犬丸未加入 | 未加入用ヒントを表示。調査済みにしない |
| 犬丸がDown、控え、離脱CD中 | 加入済みなら受理。戦闘参加可能かとは別に判定 |
| 戦闘中・Wave幕間 | 拒否。探索依頼を予約しない |
| Pause／会話／イベント／Loading／結果画面 | 新規入力は受け付けない |
| 別の調査を実行中 | 拒否し、現在の依頼を維持。複数依頼の列を持たない |
| 同一依頼の再送 | 同じRequestIdの結果を再利用し、二重起動しない |
| 既に調査済み | 調査済み文言を表示。発見通知・報酬要求は再発行しない |
| 参照欠落・地点無効・範囲外 | 失敗理由を返す。例外で停止しない |

新規調査は主人公の攻撃・Step・Hurt等の実行中には受理しない。先行入力予約もしない。通常の移動／待機から開始する。

## 5. 探索資格と戦闘状態を分離する

### 5.1 加入資格

`ICompanionAvailability`相当の読み取り契約で、必要な仲間が加入済みかを得る。`CompanionActor`がSceneに存在するか、HPが正か、レジストリに登録されているかを加入判定に使わない。

P4はScene上の小さな供給元で「犬丸加入済み」を注入する。本編の加入イベント・セーブは作らない。供給元が欠落した場合は、未配線として拒否し、Validatorで検出する。

### 5.2 探索用の表示代理

加入済みだがDown／控えの犬丸でも探索を見せられるよう、探索用の表示代理を第一選択とする。

- 既存の本体シルエットと方向矢印を再利用する。戦闘Prefabをそのままもう1体生成しない。
- 表示代理は戦闘Actor、HP、Hurtbox、ヘイト、攻撃判定、守護Resolverを持たない。
- 通常表示の犬丸がいる場合、その表示だけを一時的に抑制する。戦闘ルートの`SetActive(false)`でHP・CD・購読をリセットしない。
- 表示の二重化はしない。通常表示と探索表示を同時に出さない。
- 戦闘側の自動行動は調査中に停止する。戦闘値は保持し、既存の時間進行方針に従ってのみ進む。
- 戦闘開始・直撃・主人公被弾時は先に調査を中断し、通常の命中解決を同じ呼び出し内で続ける。探索を無敵化手段にしない。
- 代理の見た目の座標と進捗は探索ロジックが保持し、Presentationはその表示だけを行う。
- 完了／中断時に表示の所有権を返す。もともとDown等で非表示なら、無条件に表示へ戻さない。

「探索できること」と「戦闘へ復帰すること」を混同しない。同じ契約を保てる、より小さい実装があればClaude側から代案を提示してよい。

## 6. 探索依頼の進行と原子性

### 6.1 進行状態

| 探索状態 | 内容 | 次へ進む条件 |
|---|---|---|
| Idle | 依頼なし | 受付成功 |
| Moving | 調査位置へ移動 | 到着判定成立 |
| Investigating | 向きを合わせ、短い調査 | 調査時間満了 |
| 完了確定 | 地点記録と成功通知を1回だけ確定 | 同期処理。待機状態ではない |
| Returning | 表示の帰還・引き渡し | 帰還完了、表示時間上限、戦闘等による即時撤収 |
| Idle | 次の依頼を受けられる | — |

依頼ID、対象地点ID、実行世代番号を保持する。中断済み依頼の遅延通知が届いても、新しい依頼を完了させない。

### 6.2 移動

- P4用地点には`ApproachAnchor`と調査時の向きを設定する。
- 主人公付近の安全な出現位置からAnchorまで、直線移動と障害物検査で成立する地点だけを作る。
- 動的に遮られた場合は短い上限時間で中断する。壁越しワープで調査を成功させない。
- 帰還不能は既に確定した発見を取り消さない。表示代理を撤収し、通常表示を現在状態から復元する。
- 段差・多層・移動床・落下地点はP4専用Sceneへ配置しない。P5で移動先解決器を拡張する。

### 6.3 完了確定

順序は、依頼の有効性を再検査 → 地点をScene記録へ調査済みとして登録 → 内部依頼を成功終端化 → 完了通知、で固定する。

- 同一地点への再入・通知購読側からの再要求でも、完了は1回だけ。
- 複数の効果購読者は同じPointIdの成功通知を読む。通知購読者の数を地点完了の回数に数えない。
- 完了通知の前に戦闘・破棄・キャンセルが検出されたら未完了のまま中断する。
- 完了確定と競合する場合、同じTick内では戦闘開始・破棄・被弾を優先する。
- 成功後の帰還中に中断しても、地点は調査済みのまま。成功後にCanceledを追加発行しない。
- 通知先が未配線でも調査記録は正常に残る。任意の表示購読の有無を成功条件にしない。
- 完了通知は`PointId / RequestId / CompanionId / DiscoveryId / Position`程度の不変データとする。SO原本を実行中の正本として渡さない。

### 6.4 中断

| 原因 | 未完了依頼 | 再開 |
|---|---|---|
| 敵との戦闘開始・攻撃／被弾 | 直ちに中断、地点は未調査 | 戦闘終了後に再操作 |
| 主人公が地点から継続範囲外へ移動 | 中断 | 再接近して再操作 |
| 地点Disable／破棄、加入資格消失 | 中断 | 条件回復後に再操作 |
| 到達失敗／移動時間超過 | 中断、理由表示 | 再操作 |
| 会話・イベント開始 | 中断、表示と参照を解除 | 終了後に再操作 |
| Pause | 進行・入力を凍結 | Resumeで同じ依頼を再検査して続行 |
| Scene離脱／Retry／主人公死亡 | 終了処理を1回行い、すべて解放 | 新Sceneから開始 |

各失敗で残すものは理由通知だけ。徳やアイテムを減らす罰、スキルCDの消費、永続的な地点ロックは設けない。

## 7. 戦闘判定と時間

### 7.1 GameModeだけで戦闘を判定しない

現行試遊Sceneでは、GameModeがExplorationでもWave戦闘が動作する。`GameMode == Exploration`だけで調査を許可してはいけない。

探索可否の供給契約は、少なくとも次を区別する。

- Gameplay入力・進行を許可するか。
- Pauseか。
- 戦闘／Encounterが継続中か。
- 主人公が生存し、新しいInteractを実行できるか。
- ローカルイベント等が仲間を所有しているか。

P4の戦闘セッションAdapterは、戦闘開始要求から全Waveの終端までを戦闘中とする。Wave幕間3秒を探索可能にしない。Preparing中も、開始待ちのEncounterがあるなら探索を禁止する。

結果画面は戦闘終端でも入力不可なので調査不可。専用Sceneの「自由探索」は、Encounter開始前の別の試遊段階として供給する。

P4自由探索区画には敵を置かない。戦闘開始要求は、探索中断 → 非表示代理の解放 → 敵生成／攻撃許可、の順とする。P5の本編では、知覚・敵対行動・追跡終了の正本から同じ契約を供給する。仲間自身が独立した全敵探索や別の戦闘開始判定を追加しない。

### 7.2 停止は入力を閉じるだけでは不十分

現行GameMode切替は、必ずしも`Time.timeScale = 0`を伴わない。したがって仲間Motorも停止ゲートを持つ。

- Pause中は移動速度・攻撃判定・探索進行・復帰待ち・防御／守護CDを進めない。
- 回転・追従ワープ・索敵に伴う行動変更も停止する。
- Pause中は攻撃の段・HitId・既命中集合を保持し、再開時に一重で継続する。
- 会話／イベントでは攻撃を中断して判定を消す。戻ったとき、古いActiveを復活させない。
- Loading／離脱／結果画面では入力・実行を止め、残留する移動指示を消す。
- モード切替はこの機能が所有する参照・トークンだけを解放する。他機能が後から取得した停止権や新しいGameModeを古い値で上書きしない。
- 未配線の本番／試遊Sceneは安全側に停止し、Validatorでエラーにする。テストは明示的なFakeの活動Contextを注入する。

時間は外部から注入する。各モデルは1フレームに1回だけTickする。状態機の更新順・UnityのコンポーネントUpdate順に成否を依存させない。

## 8. 仲間行動の調停

### 8.1 正本は1つ

`CompanionBrain`相当の調停役を、犬丸1体につき1つ置く。

- Follow／Combat／Defense／Guardianは候補・進捗・結果を供給する。
- 最終的な行動状態と通常移動先を確定するのは調停役だけ。
- 各Controllerの独立Updateから同時に`RequestState`やMotor操作を行う構造を解消する。
- 既存の純粋モデル、AttackSnapshot、命中解決、ヘイト計算は可能な限り再利用する。
- HP減少・Down等の命中結果は権威的な割込みとして扱い、次のTickを待たずに状態・判定停止を確定する。
- 汎用AIフレームワークやプロジェクト全体のイベントバスを新設しない。

### 8.2 遷移の種類

| 種類 | 規則 |
|---|---|
| 開始 | 現在行動のキャンセル可否と依頼側の資格を検査 |
| 割込み | 許可された組合せだけを受理し、旧行動を停止してから新行動を開始 |
| 正常終了 | 終了した行動の所有者・実行IDが一致する場合だけ解放し、現在条件から次行動を選ぶ |
| 被弾 | Down／Stagger等を同期確定。古い攻撃コールバックが上書きしない |
| 退場・破棄 | 全行動を中断、購読・参照・速度・判定を解放 |

順位の高低だけで「すべてのTryTransition」を拒否しない。AttackActive→AttackRecovery、Stagger→Follow、Event終了後の再判断など、正常な下位復帰を通す。

### 8.3 P4の具体的な競合規則

| 現在／要求 | Follow・Chase・Idle | AttackPrepare | AttackActive | AttackRecovery | Guard | Evade中 |
|---|---|---|---|---|---|---|
| 自動Guard | 可 | 攻撃中断して可 | 不可 | 攻撃中断して可 | 継続／終了判定 | 不可 |
| 自動Evade | 可 | 攻撃中断して可 | 不可 | 攻撃中断して可 | ガード解除して可 | 同じ回避を継続 |
| 自動攻撃開始 | 可 | 二重開始不可 | 二重開始不可 | 二重開始不可 | 不可 | 不可 |
| 守護成立 | 条件付きで可 | 中断して可 | 中断して可 | 中断して可 | 旧行動解除して可 | 回避終了まで不可 |
| 探索開始 | 平常探索時のみ可 | 不可 | 不可 | 不可 | 不可 | 不可 |

この表の探索開始は、通常表示の戦闘Actorを探索へ引き渡す場合の規則。Down／Awayの加入済み犬丸による探索は、§5の独立した探索表示で実行するため、戦闘ActorのDown／Awayを解除する必要はない。

表より優先される規則：

- Away／無効化／Scene離脱は常に停止を確定する。
- HP0によるDownはすべての通常行動を中断する。
- DownからEvent・探索のために戦闘上の生存状態へ戻さない。
- Stagger中は通常行動・守護を開始しない。復帰通知で現在条件から再判断する。
- 全体イベント中は自動戦闘・守護を行わない。ただし命中が来た場合のHP0を無視しない。
- Protectによる割込みは実際の転送成立時だけ発生する。不成立で攻撃を止めたりCDを消費したりしない。

### 8.4 攻撃・防御のライフサイクル

- Guardを開始するとき、ガード方向を確定する。Followが同フレームに向きを上書きしない。
- Guard終了時は自動攻撃を即座に強制せず、対象とCDから再判断する。
- Evadeは開始から動作完了まで1行動として所有する。無敵時間が切れただけで攻撃可能にしない。
- P4のその場回避モデルを今回の調停修正だけで移動ステップへ作り替えない。移動回避への発展は別タスクとして記録する。
- 攻撃キャンセル時はHitbox・既命中管理・移動命令を停止し、攻撃Dataの通常CDを開始する。防御によるキャンセル連打で攻撃間隔を短縮しない。
- 攻撃の射程・威力・持続・終了CDは開始時Snapshotで固定する。攻撃途中のSO変更で判定距離が変わらないよう、Hitbox寸法のData由来値も含める。
- 古い攻撃完了／防御完了が、新しいDownや探索状態を上書きしない。

### 8.5 Protectは転送取引と通常AIを分ける

主人公側の既存解決順（死亡後無視 → 被弾後無敵 → Step／JustEvade → JustGuard → Guard → 守護 → Damage）は変更しない。

守護成立は、可否確定 → 旧行動を同期中断 → 元HitIdを維持した転送を1回解決 → CD・専用通知を1回確定、の一連の処理とする。

- 転送で犬丸がDown／Staggerになったら、その状態を維持する。通知後にProtectで上書きしない。
- 非致死・非Staggerの場合のProtectは短い成立表示として扱い、不要な長時間無敵・追加硬直を作らない。専用演出は状態保持時間に依存させず通知で表示する。
- Protect後は再判断する。中断した攻撃のActiveを復活させない。
- 再入中の転送を拒否できる一時的な取引ガードを設け、同一呼び出し内の再入で二重成立しない。
- 直撃／転送の同一HitId排除・既受理時の扱いはP4-01の契約を維持し、到達順の両方で検証する。
- Resolver解除は所有者一致で行う。古い犬丸のDisableで新しいResolverを解除しない。
- 距離外、CD中、Down、Away、Stagger、活動停止中では主人公の通常Damageへ戻す。

転送のダメージ計算方式や割合肩代わりへの変更はP4-07では行わない。

## 9. Down・復帰と探索の関係

P4試遊では既存復帰方式を維持し、次で統一する。

- HP0から復帰時刻まで`Down`を維持する。待ち時間終了後、DataのHP割合で復帰して再判断する。
- `Recovering`は後続拡張用の予約値として明記し、今回のRuntime遷移では使わない。到達可能状態としてValidatorに要求しない。
- 探索依頼ではHP、復帰残時間、守護CD、攻撃CDをリセットしない。
- 探索中に通常の復帰時間が満了した場合、戦闘値の復帰だけを行う。表示を二重化せず、調査を勝手に中断しない。
- Pause／会話／イベントの間は既定のタイマー停止規則を守る。
- Retryは新しいSceneとしてHP・状態・依頼・調査記録・徳を初期化する。
- P5で本編へ接続する前に、原仕様の「戦闘終了後の復帰」へ移行する。P4の試遊値を本編既定値に流用しない。

本編方式への変更はP5の別タスクとする。Downからの復帰条件・Encounter終了判定・Wave幕間の扱いをそこで確定し、探索を動かすためだけに今回の復帰仕様を変えない。

## 10. Data・ID・保存境界

### 10.1 地点ごとの設定

| 項目 | 内容 |
|---|---|
| PointId | 配置された地点固有のStableId。同じPrefabを複数置く場合も地点ごとに別ID |
| RequiredCompanion | 犬丸の安定ID。犬という役割と加入キャラの同定を混同しない |
| DiscoveryId | 発見内容の識別子。報酬そのものではない |
| ApproachAnchor | Scene内の調査位置・向き |
| Prompt／MissingCompanionHint／CompletedText | 仮UI用の短文 |
| Settings | 移動・調査時間・距離設定のData参照 |

PointIdはPrefab/SO共有IDや`GetInstanceID()`で代用しない。Scene内に同じIDが2つあればValidatorでエラーにする。

### 10.2 承認済みの試遊初期値

| 値 | 試遊初期値 | 理由 |
|---|---:|---|
| InteractRange | 1.5 Unity units | 近づいて調べる操作として判別しやすい |
| ContinueRange | 3.0 Unity units | 主人公を短距離は自由に動かせる |
| ArrivalDistance | 0.20 Unity units | 接近完了の許容差 |
| InvestigationSeconds | 1.0秒 | 行動が見え、反復しても長すぎない |
| MoveTimeoutSeconds | 3.0秒 | 到達失敗で進行を止めない |
| ReturnVisualTimeoutSeconds | 1.0秒 | 帰還を待たせすぎない |

これらは今回採用する試遊初期値であり、完成版のバランス確定値ではない。既存の犬丸移動速度はそのまま使う。全値をData化し、負値・非有限値・不正な大小関係を検査する。経過時間はAnimationClipのフレーム数から計算しない。

### 10.3 Runtime記録

- 純粋C#のScene単位の記録に、調査済みPointIdを保持する。
- 地点のDisable／再Enableでは記録を消さない。
- Scene再読込でリセットする。P4ではエリア間持ち越しを保証しない。
- P5で外部の進行Stateへ注入先を変更できるよう、記録へのアクセスを狭い契約で分離する。
- P6で保存するのは調査済みID等の値のみ。Transform、Coroutine、進行中の依頼、表示代理を保存しない。
- P4では探索による徳やアイテムの付与を行わない。後続の付与側はPointIdを使い独立に一回性を保証する。

## 11. 表示とフィードバック

仮表示で識別できる最小要件は、調べられる、移動中、調査中、発見、拒否理由、調査済みの6つ。

- 本体・矢印は既存Placeholder素材を再利用する。
- 状態は色だけに依存させず、文字または小さな形状マーカーも併用する。
- 新しいスプライト制作を実装開始条件にしない。
- 発見VFX／SEは任意割当。未割当でも短文・地点マーカーがあれば受入可能。
- 必須の仮本体・方向表示・テキスト参照はValidatorで厳格検査する。Runtimeのnull耐性と受入基準を混同しない。
- 探索通知ではHitResultKindを増やさず、専用の型付き通知を使う。探索完了にHitStop・カメラ揺れを付けない。

補足：現行`CombatFeedbackDispatcher`の購読対象はPlayer／Dummy／Enemyであり、犬丸のResultsは自動では接続されない。P4-FIX／P4-08Rで、犬丸Resultsを既存の単一Feedback窓口へ明示配線する。GuardianTransfer演出は独自HitStopを出さない。これは探索VFXとは別の統合修正である。

## 12. 推奨実装構成

| 責務 | 所属 | 必須条件 |
|---|---|---|
| 入力仲介 | Infrastructure | 既存Interactを1回消費、GameplayへInputSystem型を漏らさない |
| 探索地点・依頼・調査記録 | Gameplay | 純粋モデルで進行・一回性を検証できる |
| 加入資格・活動Context | Gameplayの契約＋試遊Adapter | Sceneの明示参照から注入。万能staticにしない |
| 仲間行動の調停 | Gameplay | 1体1Driver、Motorと状態の書き手が一意 |
| 調査移動の障害物判定 | Gameplay | 壁を越えない、タイムアウトを返す |
| 探索用表示代理・UI | Presentation | 戦闘Actorを持たず、表示の所有権を確実に返す |
| 地点・試遊Scene生成、受入検査 | Editor | ダイアログなし本体＋メニューラッパー |

既存Controllerを全面廃棄せず、候補生成／純粋モデルを残して駆動の重複を解消する。依頼・状態・通知の三者を一つの汎用文字列イベントにまとめない。

## 13. P4-08Rへの引き渡し要件

### 13.1 専用Scene

`SCN_Phase4_CompanionTrial`相当を新設し、既存P3.5試遊Sceneを手作業で拡張した状態を正本にしない。

- 起動直後は主人公＋犬丸＋探索地点。敵はまだ生成しない。
- 自由探索区画に正常地点、壁で到達できない検証用地点、未加入条件を確認できる検証経路を用意する。
- P4限定の試遊開始操作から、既存の4 Waveを開始できる。Wave内容・徳94の期待値は維持する。
- Wave開始時には探索を解放してから戦闘を起動する。
- 戦闘終了後の勝敗・Retryは既存の方針を維持する。再読込後は探索区画から開始する。
- 追加の故障条件はPlayModeテストやEditor専用検証操作で作り、本編入力へ裏コマンドを増やさない。

P3.5の自動Wave開始を一律変更しない。開始方法の注入等の小さい拡張で、既存Sceneの動作を維持する。

### 13.2 Builder／Validator

- Builderは犬丸、加入供給元、活動Context、地点、入力、UI、Feedbackを再生成・再配線する。
- 純走査ValidatorはSceneを受け取り、仲間の単一性、Driverの単一性、参照一致、地点ID重複、必須仮素材、Scene衛生を検査する。
- 構成が欠けたときも、犬丸0体のまま合格しない。
- 新規のAssetパス検査は既存のAssetDatabase非依存Validatorへ混ぜない。
- BuilderテストはPrefab／CompanionData／AttackDataのすべてを一時パスへ出力する。
- ブリッジへ無人生成を追加する場合はP4専用出力先と実行条件を限定し、未保存Sceneを黙って破棄しない。権限が不足する場合だけユーザーへ確認する。

## 14. 自動受入条件

### 14.1 EditMode

| ID | ケース | 期待結果 |
|---|---|---|
| E01 | 正常な調査依頼 | Moving→Investigating→成功→表示復帰 |
| E02 | 未加入 | ヒントのみ、記録と成功通知なし |
| E03 | Down／Away／離脱CD中の加入済み犬丸 | 探索可。戦闘値の強制初期化なし |
| E04 | 戦闘中・Wave幕間・結果画面 | 依頼拒否、入力予約なし |
| E05 | 複数地点・同距離 | 決定的に1地点だけ選択 |
| E06 | 同一RequestId再送・別依頼連打 | 二重開始なし、現在依頼を壊さない |
| E07 | 完了地点の再調査・Disable再Enable | 成功通知と記録の重複なし |
| E08 | 地点破棄・継続範囲外・加入取消 | 未完了のまま中断、参照解放 |
| E09 | 移動タイムアウト | ロックを残さず中断 |
| E10 | 完了と戦闘開始が同じTick | 戦闘開始が優先、未完了 |
| E11 | 成功後の帰還中断 | 成功は1回、調査済みを取り消さない |
| E12 | 中断した古い依頼からの完了 | 無視。新依頼や新Sceneへ影響なし |
| E13 | Pause→Resume、dt=0 | 時間が停止し、二重開始せず続行 |
| E14 | 会話・イベント・離脱 | 未完了の探索と古い攻撃を再開しない |
| E15 | AttackActive中のGuard／Evade要求 | 防御不可、二重行動なし |
| E16 | Startup／Recovery中の防御要求 | 旧判定と進捗を中断して防御、規定CD |
| E17 | Active中の守護成立／不成立 | 成立時だけ即時攻撃中断。不成立は不変 |
| E18 | 守護で致死／Stagger | Down／StaggerをProtectで上書きしない |
| E19 | 正常終了・割込み後の古い終了通知 | 正常な下位復帰は可能、古い所有者は書き換え不可 |
| E20 | 探索中のHP0・復帰時間満了 | 戦闘状態と探索状態が混同されない |
| E21 | 実行中のSO編集 | 現行依頼／攻撃Snapshotは不変 |
| E22 | 各null・破棄済み参照 | 無例外、安全な拒否／終了 |
| E23 | 地点ID重複・必須参照欠落 | Validatorが確実にエラー |
| E24 | 同一Bodyで複数Driver | Validatorがエラー、暗黙に二重Tickしない |

### 14.2 PlayMode／統合

| ID | ケース | 期待結果 |
|---|---|---|
| P01 | Builder生成の実Scene・実PrefabでInteract | 見た目・入力・完了通知まで到達 |
| P02 | 受付前から実壁がある／受付後に実壁を追加 | 前者は到達不可として拒否、後者は壁抜けせず中断。次の依頼を受付可能 |
| P03 | 実入力の南ボタン／E | 1押下1依頼。Step等の二重起動なし |
| P04 | HP0の犬丸で調査 | 探索表示は動くが戦闘HPは探索によって回復しない |
| P05 | 調査途中から実戦闘開始 | 表示代理・依頼を解放し、戦闘へ正しく戻る |
| P06 | 防御＋守護を含む実Prefabで実Hitbox | 同時行動なし、転送と直撃の重複ダメージなし |
| P07 | 犬丸Damage→Feedback | 既存窓口へ届き、守護専用演出との二重HitStopなし |
| P08 | モード停止、timeScale=1のまま | Rigidbodyも停止。再開後に古い指示で飛び出さない |
| P09 | Scene再読込を3回 | 犬丸・Driver・購読・UIが各1。代理・探索済み・徳の残留なし |
| P10 | 4 Wave通し・Retry | 既存勝敗／徳94／Retryリセットの回帰なし |
| P11 | 全PlayModeを続けて実行 | 読み込んだScene・静的状態を後続テストへ残さない |

P4統合テストは、手で一部のコンポーネントだけを組み立てたRigだけで代替しない。Defense／Guardian／Follow／Presentationを含む実PrefabまたはBuilder生成Sceneを最低1本で通す。

### 14.3 実行結果の記録

- コンパイル状態の確認後にEditMode／PlayModeを実行する。
- フィルタ適用後の実行対象0件、全件Skip、必須統合テストのSkipをフェーズ合格にしない。
- 結果は、対象コードのSHAまたはtree／dirty差分の識別、Unity版、実行日時、フィルタ、成功・失敗・Skip件数、所要時間、Data／Scene Validator結果を記録する。
- 記録を追加するとSHAが変わるため、ログに将来の自己コミットSHAを書く必要はない。実際に検証したコードとの対応を追跡できればよい。
- ブリッジの最新result.jsonだけを証跡にしない。次の実行で上書きされないフェーズ受入記録を残す。
- テスト・Scene書換えの実行権限は、その作業でユーザーが与えた範囲に従う。本書の仕様承認だけで新たな実行権限を付与したことにはしない。

## 15. 人間が最後に確認する項目

数分の試遊で以下に絞る。論理的な網羅検査は自動テストへ寄せる。

1. 痕跡を見て「ここを調べられる」と分かるか。
2. 犬丸が何をしているか、発見したかが仮表示でも分かるか。
3. 約1秒の調査が遅すぎず、行動を認識できるか。
4. 戦闘開始時の切替や表示の復帰が不自然でないか。
5. Attack／Guard／Evade／Protectが同時に見える場面がないか。

主人公や犬丸の完成スプライトの品質は、この受入に含めない。

## 16. Claudeへの実装順序

本書に基づき一括で実装を依頼する。ただし、変更を次の単位へ分けて検証する。

1. 検証基盤修正：Builderテストの一時AttackData化、0件成功防止、受入記録の形式。
2. 行動調停：単一Driver、明示的な割込み／終了、停止ゲート、Guard／Evade／ProtectとAttackの排他。
3. P4-07A：地点・加入資格・活動Context・依頼・完了記録の純粋ロジック。
4. P4-07B：既存Interact接続、調査移動、表示代理、UI・理由通知。
5. P4-08R：専用Scene・Builder・Validator・実Prefabの統合テスト、犬丸Feedback配線。
6. 全回帰：EditMode／PlayMode／Data検証／専用Validator、証跡と保留台帳。

機能開発とは無関係なリポジトリ整理、正式素材差し替え、戦闘数値調整を混ぜない。受入済みP3.5の主人公攻撃・ガード・JG・JustEvade・必殺技の仕様は変更しない。

完了報告では、「P4-07の受入」「P4-08Rの受入」「原仕様からの正式保留」を分ける。探索が完成したことを根拠に、能動かばう・仲間スタミナ・本編復帰仕様まで完成したと報告しない。

## 17. 参照資料

すべて調査対象コミットに固定。原仕様v1.4はDOCX本文を展開し、該当節を確認した。

- [ゲーム仕様書v1.4](https://github.com/JP2943/Momotaro/blob/aa80e05c0ec906ac9d2211ce90d6c446635ee1b1/制作開始パック/桃太郎アクションRPG_ゲーム仕様書_v1.4.docx)：§4.4、4.14、4.15、5.1、8.14、14.5。
- [改訂ロードマップv2.2](https://github.com/JP2943/Momotaro/blob/aa80e05c0ec906ac9d2211ce90d6c446635ee1b1/桃太郎プロジェクト_改訂ロードマップ_v2.2.md)：P4-07、P4終了条件、P5〜P8分割、守護転送契約。
- [Input Actions](https://github.com/JP2943/Momotaro/blob/aa80e05c0ec906ac9d2211ce90d6c446635ee1b1/Assets/_Project/Settings/Input/IA_Momotaro.inputactions)：Interact／CompanionSkill。
- [CompanionStateMachine](https://github.com/JP2943/Momotaro/blob/aa80e05c0ec906ac9d2211ce90d6c446635ee1b1/Assets/_Project/Scripts/Gameplay/Companion/CompanionStateMachine.cs)：現在の遷移制約。
- [CompanionCombatController](https://github.com/JP2943/Momotaro/blob/aa80e05c0ec906ac9d2211ce90d6c446635ee1b1/Assets/_Project/Scripts/Gameplay/Companion/CompanionCombatController.cs)：攻撃・防御判断・Tick・Hitbox。
- [CompanionVitals](https://github.com/JP2943/Momotaro/blob/aa80e05c0ec906ac9d2211ce90d6c446635ee1b1/Assets/_Project/Scripts/Gameplay/Companion/CompanionVitals.cs)：現行の時間経過復帰。
- [CombatFeedbackDispatcher](https://github.com/JP2943/Momotaro/blob/aa80e05c0ec906ac9d2211ce90d6c446635ee1b1/Assets/_Project/Scripts/Presentation/Diagnostics/CombatFeedbackDispatcher.cs)：現行の購読対象。
- [CLAUDE.md](https://github.com/JP2943/Momotaro/blob/aa80e05c0ec906ac9d2211ce90d6c446635ee1b1/CLAUDE.md)：実装・テスト・ブリッジ運用規則。

---

## C. 修正事項に対する追加受入テスト

仕様§14のE01〜E24、P01〜P11に加えて、以下の検査を満たしてください。同一テストで複数IDを満たして構いません。対応表には実際のテスト名を記録します。

| ID | 対象 | 合格条件 |
|---|---|---|
| R01 | F01：Scene再生成 | クリーンなP4専用出力から生成し、犬丸・加入供給・Context・探索UI・Feedbackまで復元。専用Validator合格 |
| R02 | F01：負例 | 犬丸欠落、Driver重複、必須参照欠落、重複PointIdをそれぞれ検出。正常Sceneに戻せば合格 |
| R03 | F03：Resolver所有権 | A登録→B登録→A解除でもBを維持。B自身の解除は成功。Disable／破棄で漏れなし |
| R04 | F03：転送取引 | 直撃→転送／転送→直撃で犬丸の二重被害なし。拒否で旧行動不変、成立で一度だけ中断・CD・通知 |
| R05 | F04：購読の寿命 | 犬丸のEnable／Disable／再読込を繰り返してもResults購読は一重。無効・破棄後の旧通知で現Sceneへ演出しない |
| R06 | F05：停止と再開 | timeScale=1のPauseで位置・向き・時計・攻撃進行が停止。再開後は同じSwingから同じ対象へ再被害を出さない |
| R07 | F06：Snapshot | 実行中にSOの射程・寸法・時間・威力・CDを変更しても現行攻撃は不変。次回攻撃には新値を反映 |
| R08 | F07：Asset汚染 | Builderテスト前後で本番Prefab／CompanionData／AttackData不変。一時Assetだけを生成・後始末 |
| R09 | F08：結果判定 | 成功あり、失敗あり、対象0件、全Skip、必須Skip、キャンセル／結果欠落を識別。後5種をフェーズ合格にしない |
| R10 | F09：全件回帰 | P3.5＋P4を連続実行し、Scene／Collider／登録／静的状態／GameMode／timeScaleの残留で後続を壊さない |

テスト終了処理は、テスト自身が読み込んだSceneをアンロードし、自身が作ったオブジェクト・購読・登録を解除します。もともとのユーザーSceneや未保存変更を一括破棄して独立性を作る方法は使いません。成功経路だけでなく失敗時にも後始末される構造にしてください。

## D. 正式保留台帳

以下は今回の実装漏れではなく、承認済みの工程分割です。リポジトリ内のタスク表にも残し、P4完了報告で明示してください。

| 保留ID | 項目 | P4での扱い | 移行先・着手条件 |
|---|---|---|---|
| D01 | 本編の戦闘不能復帰 | 既存5秒・HP50%のDataを維持、待機全体Down | P5の本編接続前。戦闘終了正本・Wave幕間・原仕様の敵なし2秒／20%との整合を確定 |
| D02 | エリアをまたぐ進行値 | 徳・探索済み記録はP4のScene単位、Retryで初期化 | P5で本編進行と戦闘セッションを分離し、外部State注入へ移行 |
| D03 | 永続保存 | Save／Loadなし | P6入口でRuntime Stateと保存DTOを確定。徳の二重保持を作らない |
| D04 | 能動「かばう」 | 現行の条件付き守護転送を維持。固有スキル完成とは扱わない | P8着手前の先行タスクで効果・入力・CD・既存守護との関係を確定 |
| D05 | 仲間スタミナ | 新規導入しない | P8着手前に防御・回避・他仲間との共通契約を確定 |
| D06 | 発見の本編効果 | 発見通知とScene内記録のみ。徳／アイテム追加なし | P5〜P7の各受け手側で扉・宝箱・会話・クエスト・報酬へ接続 |
| D07 | 移動回避・高度な経路探索 | P4のその場回避と、探索の直線＋障害物判定を維持 | 移動回避は別タスク。段差・複雑な移動先解決はP5で必要範囲を設計 |
| D08 | 正式素材と最終体感 | Placeholder、既存縮尺・接地基準を維持 | P10aで素材キー置換・別Asset Validator、P10bで見た目依存の数値調整 |

P5の進行値移行では、`PlayerProgressHolder.Bind(PlayerProgressState)` 相当の注入APIを設け、外部Stateがあればそれを使い、なければ試遊用の自前Stateを使う既定動作を保つ方針を継承します。収集側が二種類のStateへ同時に付与する構造にはしません。このAPIの本編実装は今回のスコープではありません。

P4の受入表記は、「本書のP4試遊スコープが受入条件を満たす」とし、「ゲーム仕様書v1.4の犬丸全機能が完成」とは表現しません。技術的な試験合格とユーザーの最終受入も分けます。

## E. 実装順とチェックポイント

| 順序 | 作業単位 | 主な対象 | 次へ進む条件 |
|---|---|---|---|
| 0 | 開始確認・仕様反映 | A.1、最新差分、許可範囲、保留台帳 | 指摘の現存／解消を記録、変更対象を限定 |
| 1 | 検証基盤 | F07、F08、F09の証跡形式 | 本番Asset非汚染と0件防止のテスト合格 |
| 2 | P4-FIX | F02〜F06、行動調停・守護所有権・停止・Snapshot | 攻撃／防御／守護競合の単体テスト合格、既存回帰なし |
| 3 | P4-07A | 地点・加入・Context・依頼・完了記録 | 純粋モデルと異常系のEテスト合格 |
| 4 | P4-07B | Interact・移動・表示代理・探索UI | 一入力一依頼、Down探索、キャンセル・復帰を統合確認 |
| 5 | P4-08R | F01、F04最終配線、F09実Prefab、専用Builder／Validator | 再生成Sceneで自由探索→4 Wave→Retry、負例検査を通す |
| 6 | 全回帰・引き渡し | E／P／R対応表、Data／Scene検査、証跡、README | 未実行・失敗・保留を分離して報告、人間の最終確認へ |

上記はチェックポイントであり、各行の完了ごとに新しい承認待ちを必須にする意味ではありません。既存権限内でテストを続け、不具合があればその範囲で修正・再実行してください。Commitを区切る場合も、ユーザーから許可されたGit運用に従います。

専用試遊READMEには、生成・Validatorの実際のメニュー、開くScene、Interact、戦闘開始操作、Retry、未加入／Down等の確認方法、P4試遊の保留事項を記載します。Builder再生成だけで受取人が同じ状態を再現できることを目標とします。

## F. 完了報告・受入記録の必須項目

リポジトリ規約に合う場所へ、次の実行でも上書きされない受入記録を残してください。名前の例は `P4_統合受入結果.md` です。ブリッジの一時的な最新結果だけを根拠にしません。

### F.1 実装の結果

- P4-FIX：F01〜F09ごとの「修正済み／既存で解消／未完了」と変更ファイル。
- P4-07：仕様の実装範囲と、E／P／R番号に対する実際のテスト名。
- P4-08R：専用Scene、Builder、Validator、READMEの実パスと実行方法。
- 正式保留：D01〜D08を別欄に記載。新規の未完了を正式保留へ無断で移さない。
- 内部実装を例示名から変えた場合は、契約を満たす対応関係を短く説明。

### F.2 検証の結果

| 項目 | 記録する内容 |
|---|---|
| 対象コード | 検証時のSHA／tree、ブランチ、dirty差分があれば識別可能な差分情報 |
| 環境 | Unity版、実行日時、Editor／ブリッジ経路 |
| テスト | EditMode／PlayModeそれぞれのフィルタ、対象数、実行数、Pass／Fail／Skip／その他、所要時間 |
| 統合必須 | 実Prefab・実Scene・4 Wave・94・Retry・Scene後始末の実行結果 |
| Validator | Data、P3.5既存、P4専用の結果と検出した警告・エラー |
| 出力物 | 結果XML／ログ等を保存した場合の実パス。巨大な一時アーカイブは追加しない |
| 残件 | 未実行、再現中の失敗、非必須Skipの理由、ユーザー確認待ちを区別 |

将来作成するコミットのSHAを事前に記録する必要はありません。検証対象コードと結果を追跡できることが要件です。

### F.3 最後に人間へ依頼する確認

自動で確認できる配線・重複・状態・数値は自動テストへ寄せます。最後に人間へ依頼するのは仕様§15の短い試遊確認だけです。正式素材の品質判定は含めません。

未実行のテストを合格と書かず、Unityで確認していない体感を確認済みとも書かないでください。「実装・自動検証完了／人間試遊待ち」と「ユーザー受入完了」を明確に区別して報告してください。

