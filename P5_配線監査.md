# 配線監査：「部品は置いたが、使う側・見る側が居ない」型の網羅的な洗い出し

- 日付：2026-09-26
- 動機：P5 のレビューで**同じ形の欠陥が 4 件連続**した。1 件ずつ潰す進め方では 5 件目が同じ形で出る。
- 対象：`Assets/_Project/Scripts`（442 ファイル・約 57,100 行）、`Assets/_Project/Prefabs`、`Assets/_Project/Scenes` 全件。
- 方法：観点ごとに機械的な走査を書き、候補を一件ずつ手で判定した。走査スクリプトは使い捨てではなく、
  結論のうち再発防止に値するものは**テストへ落として常設**した（§5）。

## 1. 起点になった 4 件

| # | 記録 | 症状 | 形 |
|---|---|---|---|
| 1 | 024 | A→B の出入口が一度も反応しない | Trigger の Collider は置いたが、**受け取るコードが無い** |
| 2 | 026 | 到着初期化に失敗すると再試行が NotWired で断られる | 実行役は居るが、**配線が失敗しうる段より後ろ**にある |
| 3 | 027 | 実行役ごと失われると表示も操作経路も無い | 常駐に窓口は作ったが、**それを呼ぶ側が Scene 依存のまま** |
| 4 | 028 | 常駐へ引き継いでも Scene 側が押下を破棄する | 消費する側は切り替えたが、**破棄する側が同じ所有権に従わない** |

共通するのは「**動かしてみないと分からず、テストは継ぎ目の両側だけを触っていた**」こと。
そして 4 件とも、**その部品を見る検査が存在しなかった**。

## 2. 観点と走査結果

### 観点 1：受け手の居ない Trigger／イベント

| 走査 | 結果 |
|---|---|
| Prefab・Scene 全件の `m_IsTrigger: 1` を抽出し、同じ GameObject のスクリプトを突き合わせ | **Trigger は全部で 3 つ**（`ExitGate_ToB`／`ExitGate_ToA`／`EncounterTrigger`）。いずれも `OnTriggerEnter` を持つ型が載っている（1 件目の修正後）。**抜けなし** |
| `public event` / `public Action` を宣言する 24 件について、本番側の購読者数を集計 | 本番購読者ゼロが **7 件**。下記のとおりいずれも欠陥ではない |

購読者ゼロ 7 件の判定：

| イベント | 判定 |
|---|---|
| `GameModeService.ModeChanged` | 配信の本線は `IGameModeListener`（`NotifyListeners`）。イベントは追加の公開 API で、欠落経路ではない |
| `PlayerInputState.GuardStarted` / `GuardCanceled` | ガードは状態のポーリングで成立している。未使用の公開 API |
| `BootstrapRoot.BootstrapFinishedEvent` | 未使用。起動待ちは `BootstrapWait` が担う |
| `InputService.SchemeChanged` | 未使用 |
| `AreaTransitionService.ArrivalCompleted` | 拡張点。P5 で到着に反応する要求は無い |
| `AreaTransitionService.TerminalFailed` | `AreaTransitionFailureView` は `HasTerminalFailure` のポーリングで表示している。二重の経路 |

> **未使用 API を「欠陥」とは呼ばない。** ただし読む側は「誰かが購読しているはず」と誤解するので、
> 後続課題へ「使われていない公開イベントの整理」を残す。

### 観点 2：本番に呼び出し元が無い注入 API

`Bind*` / `Configure*` / `Attach*` / `Inject*` の公開メソッド **90 件**の呼び出し元を集計。

- 本番呼び出し元ゼロ：**2 件**（`CompanionInvestigationPoint.ConfigureText`、`CompanionFeedbackBinder.Attach`）。
  どちらもテスト専用の補助 API で、本番の配線は別メソッド（`Bind`）が担っている。欠陥ではない。

**この観点だけでは 2 件目の欠陥を捕まえられない**（呼び出し元は存在したが、失敗経路で到達しなかった）。
そこで観点 2b を足した。

### 観点 2b：失敗しうる段より後ろにある配線

「早期 return（`return Fail(...)` / `return false`）を持つメソッドの中で、その後ろに配線呼び出しがある」を走査。

| 箇所 | 判定 |
|---|---|
| `AreaInitializer.Initialize`（配線 6 件が失敗点より後ろ） | **2 件目の欠陥そのもの。記録 026 で前倒し済み。** 残る `_encounter.BindSession` は、失敗時にその Area を活動させないので実害なし |
| `Phase5TrialLauncher.TryEnterFirstArea`（1 件） | ガードは「そもそも進めない」条件で、配線は移動直前の最終段。実害なし |

### 観点 3：Scene 側部品が失われたときの代替

常駐（DontDestroyOnLoad）から Scene を実行時に探す箇所を全件確認。

| 箇所 | 欠落時 |
|---|---|
| `AreaTransitionService.CaptureActors` → `AreaActorTransferPort` | null 許容（運ばないだけ）。設計どおり |
| `AreaTransitionService.FindCurrentContext` → `AreaContext` | null なら終端失敗にして Error 表示へ。処理済み |
| `CampaignRespawnResidentView` → `RespawnSubmitInput` | null／無効なら常駐が肩代わり（4 件目の修正） |

Presentation 側の `FindFirstObjectByType` は**同じ Scene 内**の依存解決で、寿命をまたがない。抜けなし。

> Scene 側部品の存在保証は、実質 **Scene Validator の `RequireOne` が唯一の仕組み**である。
> したがって「Validator が見ていない部品」がそのまま単一障害点になる（→ 観点 5）。

### 観点 4：同じ入力を複数が消費・破棄する

`Consume*` / `Discard*` の呼び出し元を集計。複数箇所から呼ばれるのは 3 つ。

| 対象 | 判定 |
|---|---|
| `ConsumeSubmitPressed`（`RespawnSubmitInput`／`CampaignRespawnResidentView`） | **4 件目の欠陥。記録 028 で所有権を共通化済み** |
| `ConsumeInteractPressed`（`AreaInteractInput`／`InvestigationInteractInput`） | 同居は Validator が `ForbidAll<InvestigationInteractInput>` で禁止している。**守られている** |
| `DiscardInteractPressed`（上記 2 つ＋再開手順） | 破棄は冪等で、再開時の破棄は §9.1 末尾が要求する動作。競合しない |

### 観点 5（追加）：Builder が置くのに Validator が見ていない部品

観点 3 の結論から導いた逆向きの走査。**これが一番の収穫だった。**
Builder が Area Scene へ載せる **49 型**のうち、Validator が一度も触れていないものを抽出。

**実在した検査の穴 4 件**（いずれも現時点では Builder が正しく置いているので不具合は出ていないが、
落ちても誰も気付けない状態だった）：

| 部品 | 落ちたときに起きること |
|---|---|
| `CombatRewardCollector` | 敵を倒しても**徳が黙って入らない**。遭遇戦の配線検査には含まれていなかった |
| `CombatPlayHud` | §11 の必須 UI（徳の表示）が消える。試遊の観点 6 が黙って崩れる |
| `CombatFeedbackPresenter` の `HitStop`／`Flash` 参照 | ヒットストップと点滅が消える。戦闘は成立するのでテストでは気付けない |
| `InvestigationInteractable` | 調査地点は在るのに**調べる実体が無く**、Interact の候補に挙がらない |

## 3. 直したこと

Scene Validator に 4 つの検査を追加した（§2 観点 5 の表のとおり）。

## 4. 欠陥注入で確かめたこと

| # | 注入 | 結果 |
|---|---|---|
| 1 | 報酬の受け手を置かない | 「撃破報酬の受け手（CombatRewardCollector） は 1 つであるべきですが 0 です」 |
| 2 | `feedback.HitStop` を繋がない | 「命中 Feedback（ヒットストップ・点滅） が配線されていません」 |
| 3 | 徳の HUD を置かない | A・B 両方で「徳を映す HUD（CombatPlayHud） は 1 つであるべきですが 0 です」 |
| 4 | 調査地点に実体を付けない | 3 地点すべてで「調べる実体（InvestigationInteractable）がありません」 |
| 5 | 検査も免除も無い新部品を Builder へ追加 | カバレッジ検査が「Scene Validator が一度も触れていない部品があります: BoxCollider2D」 |

## 5. 5 件目を出さないための常設化

個別の検査を足しただけでは、**次に部品を足した人がまた検査を書き忘れる**。
そこで「検査の抜けそのもの」を検出するテストを常設した。

`Phase5ValidatorCoverageTests`（EditMode・必須ゲート登録済み）

- Builder の `AddComponent<T>` を走査し、各 T が **Validator から参照されている**か、
  **理由つきで免除表に載っている**かのどちらかであることを求める。
- 免除には**理由の記述を強制**する（空・短すぎは不合格）。「間接的に検査される」は、
  その検査が落ちれば当該部品の欠落も必ず落ちるときだけ認める。
- Builder が置かなくなった型が免除表に残っていること（古い免除が次の抜けを隠す）も不合格にする。

> **この検査自体が一度すり抜けた。** 最初の実装は `AddComponent<(\w+)>` で走査していたため、
> `AddComponent<UnityEngine.BoxCollider2D>` のように名前空間つきで書かれた部品を**拾えなかった**。
> 欠陥注入で素通りして気付き、最後の区切り以降を型名として使う形へ直した。
> 検査を書いたら、その検査が落ちることを確かめる——`CLAUDE.md` の手順がそのまま効いた。

現在の免除は 9 件（Unity 標準 4、間接検出 4、Area Scene に載らない 1）。

## 6. 残件

- **未使用の公開イベント 6 件の整理**（観点 1）。読む側の誤解を招くので、削除か「拡張点」の明記を。
- **統合起動 Scene（`SCN_Phase5_ExplorationTrial`）が Scene 検査の対象外**。
  Area Scene ではないので現在の検査は素通りする。載っているのは Launcher だけだが、
  ここに Interact 入力などが紛れ込むと Area 側と競合する。検査の範囲を広げるかは P6 で判断したい。
- 同種のカバレッジ検査は **P5 の Builder にしか無い**。P3.5／P4 の Builder には同じ仕組みが無く、
  剣閃 VFX が P4・P5 で落ちたまま記録もされていなかった件（記録 025 §7）は、まさにこの穴に当たる。
  P6 以降で同じ形へ揃えるのが望ましい。
